Scope: prompts/ and the six knowledge tools, with contracts verified against novel_editorial/{config,db,llm_client}.py and services/{knowledge,audit,activity}.py; targeted baseline only (compileall 6/6 OK, 37 slice tests pass, all five R8-C fixes re-verified by reproduction). The round-8 fixes are functionally correct, but the slice still contains two P2 defects: the distill CLI exits 0 on ok:false so the weekly chain silently skips lesson distillation (the R8-C-03 no-fake-green goal is defeated at its only automation boundary), and the ai_taste_check 4-char heuristic both false-positives plain narration and misses genuine stacking. The remaining eight P3 items are previously flagged or newly confirmed robustness/doc gaps; none block the current single-novel deployment, but the two P2s should be fixed in the next cycle before the slice is considered clean.

Full review comments:

- Make distill_lessons CLI exit non-zero when distill returns ok:false — E:\code\novel-editorial\tools\distill_lessons.py:260-261
  tools/distill_lessons.py:260-261 — `main()` prints the result dict and returns normally regardless of `ok`, so a failed distillation (e.g. model JSON without a `lessons` key, which now correctly returns `{"ok": false, ...}` at line 187) still exits 0. Reproduced: with `chat_deepseek` mocked to return `{"unexpected": 1}`, `distill_latest` returns ok:false and `main()` completes with exit code 0. The weekly chain `novel_editorial/services/control.py:261` invokes this script via `_run_cli`, which only checks the return code, so the R8-C-03 'no fake green' fix is defeated at the CLI boundary: lesson distillation silently skipped every week with no alert.

- Fix 4-char stacking heuristic that flags plain narration and misses real parallelism — E:\code\novel-editorial\tools\ai_taste_check.py:87-104
  tools/ai_taste_check.py:87-104 — the heuristic counts each contiguous run of >=8 hanzi (adjacent `[\u4e00-\u9fff]{4}` windows) as one 'parallel four-character stacking' occurrence, so it measures run length, not structural parallelism. Reproduced: `她站在窗前看了很久，心里想着明天该如何开口解释这件事。` and `林深走进大厅的时候，所有人都安静下来不敢出声。` each produce note `疑似四字排比堆砌 2 处` (score 12) despite containing no parallelism, while genuine stacking `天崩地裂日月无光山河变色` yields no note (score 6; 4 with punctuation), so it both false-positives normal prose in the anti-AI gate and under-detects real stacking.

- Remove or loudly fail export_agent_prompts dead export path in proxy mode — E:\code\novel-editorial\tools\export_agent_prompts.py:59-68
  tools/export_agent_prompts.py:59-68 — every LLM node in n8n/novel_workflow.json carries `{agent:'` (15 occurrences) and render_workflow.py hardcodes PROXY_MODE=True, so the `if proxy:` branch always fires: `python tools/export_agent_prompts.py` prints the notice and exits 0 writing nothing, while the entire export path (lines 69+) is unreachable. The tool's docstring still promises 'Each agent becomes a markdown file with frontmatter', so this is a fake-green for a documented tool whose only current purpose is a one-line notice.

- Escape LIKE wildcards in novel_knowledge.resolve topic search — E:\code\novel-editorial\tools\novel_knowledge.py:365-373
  tools/novel_knowledge.py:365-373 — the topic is interpolated unescaped into `LIKE '%{topic}%'`, so `%`/`_` in a topic act as wildcards. Reproduced on an in-memory store: `resolve(conn, 1, "%")` returns every row of the novel. The tool is called from the agent loop (tools/agent_tool_loop.py:353) with LLM-generated topics, so a topic like `100%阴谋` or a stray `%` silently broadens the result set beyond the intended entity.

- Guard knowledge_keeper against hot_topics.json parsing to a non-dict — E:\code\novel-editorial\tools\knowledge_keeper.py:50-55
  tools/knowledge_keeper.py:50-55 — `_input_payload` does `hot = json.loads(...)` then calls `hot.get(...)` at lines 67-81 with no isinstance check; if the file ever parses to a list (hand-edited or a corrupted write), the keeper run crashes. Reproduced: writing a JSON array to the file and calling `_input_payload` raises `AttributeError: 'list' object has no attribute 'get'`. First-party writers (novel_editorial/hot_topics.py refresh/from_csv) always emit a dict, so this only triggers on a malformed file, but architect_weekly.load_hot_topics shows the defensive pattern this code is missing.

- Fix _parse_json brace slicing that fails on braces in LLM trailing prose — E:\code\novel-editorial\tools\knowledge_keeper.py:26-30
  tools/knowledge_keeper.py:26-30 (duplicated at tools/distill_lessons.py:26-30) — `text[text.find("{"):text.rfind("}")+1]` cuts from the first `{` to the last `}`; when the LLM appends prose containing braces after the JSON, the slice includes the trailing text and `json.loads` fails. Reproduced: `{"auto_updates": [], "draft_suggestions": [{"title": "a", "content": "用 {X} 替代"}], "deprecations": []}\n（注：格式说明 {参考}）` parses to None, failing the whole keeper/distill run with '不可解析' instead of recovering the JSON object. Both files duplicate the same helper.

- Add sub-second uniqueness to clean_novel_knowledge backup filename — E:\code\novel-editorial\tools\clean_novel_knowledge.py:309-310
  tools/clean_novel_knowledge.py:309-310 — `stamp = datetime.now().strftime("%Y%m%d-%H%M%S")` has 1-second resolution, so two `--apply` runs within the same second write to the same `{stem}-{stamp}.db` and the second backup silently overwrites the first, destroying the pre-apply snapshot that would be needed for restore. Appending a counter or microseconds (e.g. `%f`) would make each apply's backup unique.

- Match double-ellipsis form in ai_taste_check not...but filler check — E:\code\novel-editorial\tools\ai_taste_check.py:30-30
  tools/ai_taste_check.py:30 — FILLER lists `不是…而是` with a single U+2026, but the common written form `不是……而是` (double ellipsis) is not a substring, so both `count_occurrences` and the regex miss it. Reproduced: text containing `这不是……而是因果。` yields 0 hits for this filler while the single-ellipsis form hits once, defeating the anti-translation-ese check the editor/reviewer prompts explicitly call out.

- Fix novel_knowledge docstring CLI example for --snapshot — E:\code\novel-editorial\tools\novel_knowledge.py:11-11
  tools/novel_knowledge.py:11 — the module docstring advertises `python tools/novel_knowledge.py --snapshot --novel-id 1 --db demo.db`, but the CLI takes a positional novel id (`--snapshot N`, line 671). Following the docstring fails with 'unrecognized arguments: --novel-id 1'; it should read `--snapshot 1 --db demo.db`.

- Add regression tests for the round8 knowledge fixes — E:\code\novel-editorial\tools\distill_lessons.py:187-187
  tools/distill_lessons.py:187 — the five R8-C fixes shipped with no regression tests: no test covers invalid-outline degradation (novel_knowledge._outline_bible), string array elements (knowledge_keeper invalid_items / distill skipped_lessons), missing-lessons-key ok:false (distill), WAL backup completeness (clean_novel_knowledge), or empty-text chars:0 (ai_taste_check) — the existing test files (test_knowledge_bible.py, test_knowledge_keeper.py, test_distill_lessons.py, test_clean_knowledge.py, test_ai_taste_check.py) only cover the pre-existing happy paths, as the R8-C fix log itself acknowledged. These are exactly the failure classes that regressed before (sync churn, keeper AttributeError) and are now unprotected.
