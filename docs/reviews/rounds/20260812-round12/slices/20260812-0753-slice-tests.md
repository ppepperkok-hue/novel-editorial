基线全绿：python run_tests.py 497 通过、pytest 497 通过、webapp npm test 20 通过、npm run build 成功；审查范围内未发现 P0-P2 缺陷——词表与消费方一致、编码无 BOM 问题、测试无假绿/污染/漏收集。仅两个 P3 文档与回归守护缺口，不影响正确性。

Full review comments:

- [P3] .env.example 未记录 NOVEL_DATA_DIR 环境变量 — E:\code\novel-editorial\.env.example:56-63
  novel_editorial/config.py:9 读取 `NOVEL_DATA_DIR` 作为 RUNTIME_ROOT，直接决定 n8n_tmp/、exports/、demo_data/、alerts.log、hot_topics.json 的落盘位置；desktop/main.js:115 会自动注入它，但 CLI / web_api 直跑（只读安装目录）时 .env.example 没有任何入口让用户重定向运行时写入。该文件连已废弃的 FANQIE_VOLUME_ID 都专门标注，唯独漏掉这个仍被 config 读取的键，建议在 PANEL_TOKEN/PYTHON_EXE 附近补一行说明。

- [P3] 缺少守护 .env.example 与 config 契约的回归测试 — E:\code\novel-editorial\run_tests.py:10-19
  tests/ 下没有任何测试引用或校验 .env.example（全仓仅 README.md:152 与 scripts/run_review.ps1:161 提到它）。当前 47 个文档化键逐一核对后均被代码消费、无漂移，但此前多轮审查（R3-E2、R6-F-01、R8-E-05）都发现过配置与代码脱节，这类回归现在会静默通过 CI。建议在 run_tests.py 收编的测试里加一个守卫：解析 .env.example 的 KEY= 行，断言每个键都被 config/llm_client 等消费或显式标注已废弃。
