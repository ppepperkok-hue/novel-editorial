基线全绿：python run_tests.py 485 个用例 OK（exit 0，95.4s），webapp vitest 7 文件/16 用例通过，npm run build 成功（仅 charts chunk 527KB 体积警告）；run_tests.py 的零测试守卫与 import 失败守卫均实测有效（空目录 exit 1、坏模块 errors=1 exit 1）。范围内未发现 P0/P1/P2：无静默失败、无硬编码密钥（.gitignore 已覆盖 .env，git 跟踪文件无凭据）、无编码缺陷（三处消费方均显式 utf-8）、ai_flavor_density 重叠双计数的历史问题已修复（实测 '缓缓说道' 计 1 次，四份词表一致）。3 项发现均为 P3 文档/测试卫生问题：数据文件缺内容守卫测试、.env.example 缺两个实际读取的键、一个死配置键。

Full review comments:

- [P3] 为 ai_words.json / compliance_words.txt 增加真实文件内容守卫测试 — E:\code\novel-editorial\tests\test_quality_gate.py:35-51
  两个随仓库发布的词表文件没有任何测试守护其内容：`ai_words.json` 完全没有真实文件测试——tests/test_quality_gate.py:35-51 只用临时文件测缺失/损坏回退，tests/test_editorial_steps.py:211 用硬编码词；而 tests/test_compliance.py:80-86 的 `test_real_words_file_check_runs` 只断言 `_read_custom_words()` 返回 list 且干净文本通过，文件被清空或只剩注释时（返回 `[]`）测试照样通过。一旦 ai_words.json 损坏，novel_editorial/quality_gate.py:24-53 仅发 RuntimeWarning 后回退，tools/editorial_steps.py:321-329 更是无告警静默回退。该 21 词表被复制在 4 处（ai_words.json、quality_gate.py:16、editorial_steps.py:332、n8n/novel_workflow.json 内联回退表），本次实测四者一致（21==21==21==21），但任何一处被单独改动或文件损坏都不会有任何测试失败，质量门行为会无声漂移（此前 ai_flavor_density 双倍计数正是这类漂移造成的）。建议新增读取仓库根真实文件的测试：断言两文件可解析、词表非空且与各处内联回退表逐项一致。

- [P3] .env.example 补录 N8N_WORKFLOW_TRIGGER 与 PYTHONW_EXE 两个被实际读取的键 — E:\code\novel-editorial\.env.example:72-74
  `.env.example` 遗漏了两个被现役代码读取的环境变量：`N8N_WORKFLOW_TRIGGER`（tools/n8n_api.py:110 `config.env_value("N8N_WORKFLOW_TRIGGER", "每日触发")`，决定回退路径触发 n8n 工作流时用哪个触发器）和 `PYTHONW_EXE`（desktop/main.js:15 `process.env.PYTHONW_EXE || "pythonw"`，桌面壳打包环境下依赖它定位解释器）。按 .env.example 配置的用户无法发现这两个可调项；其中 PYTHONW_EXE 在打包（isPackaged）分支下若机器 PATH 中无 pythonw 会直接导致桌面 API 进程启动失败。建议在 N8N_WORKFLOW_* 段与 PYTHON_EXE 段附近补上说明。

- [P3] .env.example 移除无任何引用的 FANQIE_VOLUME_NAME 死配置 — E:\code\novel-editorial\.env.example:21-21
  `.env.example:21` 的 `FANQIE_VOLUME_NAME=第一卷：默认` 在仓库中零引用：`git grep -rn FANQIE_VOLUME_NAME` 只命中 .env.example 自身，novel_editorial/、tools/、scripts/、n8n/ 工作流 JSON 与 desktop 均未读取（n8n 工作流只用了 FANQIE_BOOK_ID / FANQIE_COOKIE / FANQIE_CSRF_TOKEN）。文件上方注释声称「n8n 中 tools 脚本从环境变量读取」与实际不符，属误导性死配置，建议删除或补齐读取方。
