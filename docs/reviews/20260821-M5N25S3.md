# 2026-08-21 N25 导入导出与备份 S3 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 1908a11（fix(archive): require real schema read in sqlite probe and document export-import (N25 S3)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n25-implementation.md / src/novel_editorial/core/archive.py / tests/test_archive.py / docs/usage.md

### 优点

- S2 审查 P2 完整闭合：`_is_sqlite_database` 以真实读 sqlite_master 为唯一判定，magic 前缀垃圾库干净拒绝（USAGE_ERROR、退出码 2、无残留、不冒原始异常）；回归用例覆盖 core 与 CLI 两侧。
- usage.md「导入导出与备份（N25）」与实现逐项一致（目标语义、校验流程、退出码、mock 实跑全链示例）；全量 1074 测试、smoke_m3 SMOKE OK、stress_m3 STRESS OK、ruff / pyright 全绿；CLI 真实全链 18 项核对通过。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- [P3] 0 字节 data.db 会被接受为空库导入 — src/novel_editorial/core/archive.py:61-62
  探针对非空文件要求 magic，但 0 字节文件落到 `sqlite3.connect` 探针并被视为合法空库，自洽 manifest 的 0 字节归档可导入成一个空作品（审查 CLI 实测）。影响：接受畸形空归档，无数据风险；文档「非真实 SQLite 拒绝」的措辞对 0 字节边界过宽。修法：`st_size == 0` 直接拒绝，或要求 sqlite_master 返回行。

### 建议

- 留档为工程待办；后续触碰归档校验时一行修复即可。

### 结论

Ready to merge: With fixes（P3 留档）

### 处理记录

- S2 审查 P2（magic 前缀垃圾库）：随 commit 1908a11 修复，全量回归通过。
- S3 审查 P3（0 字节空库宽容）：无行为风险，按项目先例留档 progress-summary「已留档的待办项」，待后续归档校验改动时清理。
- N25 审查链收敛。
