# 2026-08-19 N7 I2 审查意见修复记录

- 修复来源：20260819-M5N7I2-initial.md 的 P2 ×2 + P3。
- 修复方式：独立 CLI（flash + high），先写失败测试再改。
- 修复范围：src/novel_editorial/cli/memory.py、src/novel_editorial/core/retrieval.py、tests/test_memory.py、tests/test_retrieval.py。

### 改动

- `semantic_search` 新增 `exclude_literal` 参数：命中回读 → 过滤（归档/已删/字面 content 或 setting name）→ 排序 → top_k 截断，失效行与字面命中不再挤占活跃名额。
- `memory search --semantic` 改传 `exclude_literal=keyword`，删除高层去重推导；设定名称命中与关键词层不重复。
- 新增失败测试四条：归档过滤后 top_k 补位、exclude_literal 同时过滤 content 与 setting name、字面命中占满 top_k 时相关命中仍保留、设定名命中不再重复输出语义行。

### 验证

- 子集 `uv run pytest -q tests/test_retrieval.py tests/test_memory.py`：122 passed。
- `uv run ruff check .`：All checks passed；`uv run pyright`：0 errors；宪法校验 OK。
- 全量 `uv run pytest -q`：676 passed；smoke_m3 SMOKE OK；stress_m3 STRESS OK（检索耗时阈值无回归）。

### 结论

审查意见处理完毕，I2 收口（commit b65b42c）。
