修完收口了mashitawa，八个点全部落在指派的五个文件里，别的文件一个没碰，也没做任何 git add/commit desuwa。

R7-A1-01，[web_api.py](E:/code/novel-editorial/novel_editorial/web_api.py:150) 第150~153行：`_get_daily_runs` 接住 `sync_from_n8n` 的返回值，有 error 就透成响应的 `sync_error` 字段，不再静默吞掉desuwa。R7-A1-02，[web_api.py](E:/code/novel-editorial/novel_editorial/web_api.py:1184)：`--db` 默认值从 `"demo.db"` 改成 `str(config.DB_PATH)`，和调度链共用 ROOT 根库，换 CWD 也不会双库分裂mashitawa。R7-A1-03，[misc.py](E:/code/novel-editorial/novel_editorial/services/misc.py:257) 第258~259行加 agent 空值校验，返回 `{"ok": False, "error": "agent required"}`；[web_api.py](E:/code/novel-editorial/novel_editorial/web_api.py:737) 第737~740行看到 ok False 就回 400，IntegrityError 滚成 500 的路断了desuwa。R7-A1-04，[db.py](E:/code/novel-editorial/novel_editorial/db.py:346) 第346~365行：`connect` 对 `:memory:` 和 `file::memory:` 变体走独立分支，在返回的那个连接上直接跑建表迁移，内存库不再是没表的空壳，`file:` 前缀还开了 `uri=True` 让共享内存变体真正可用mashitawa。R7-A1-05，[control.py](E:/code/novel-editorial/novel_editorial/services/control.py:97) 第97~123行新增 `_refresh_hot_topics`，采集挪进后台线程、HTTP 等待最多90秒；第468~505行重写分支，全部源失败时 ok 为 False 且带 error，部分失败保留逐源 error，超时也返回明确错误，不再硬编码 okdesuwa。R7-A1-06，[web_api.py](E:/code/novel-editorial/novel_editorial/web_api.py:660) 第660~671行非法 JSON body 回 400，第756~771行 `novel_id` 和 `session_id`/`meeting_id` 一起做整数清洗，非法输入 400desuwa。R7-A1-07，[misc.py](E:/code/novel-editorial/novel_editorial/services/misc.py:137)：`start_topic_meeting` 默认库改成 `config.DB_PATH`，和项目一致desuwa。R7-A1-08，[config.py](E:/code/novel-editorial/novel_editorial/config.py:82) 第82~100行加 `_strip_inline_comment`，只有 `#` 前带空格或制表符才算行内注释，贴在值里的 `#`（token、URL）原样保留，第106行接入 `load_env`mashitawa。

验证结果：`compileall` 五个文件全过；全量 pytest 485 项全部通过；另外用真实 HTTP 请求复现了 `sync_error` 透出、非法 JSON 400、novel_id 400、agent_states 400，内存库两种形态建表、热点采集的全失败/部分失败/超时三条路径也都实测过desuwa。测试建议（没改测试文件）：给 test_web_api 补 daily_runs 带 sync_error、非法 JSON 400、agent_actions/create 的 novel_id 400、agent_states/update 400 四例；给 test_db 补 `:memory:` 建表；给 test_services_regression 补热点采集聚合与超时、start_topic_meeting 默认库；给 config 相关测试补行内注释边界desuwa。

额外问题只报不改：`do_POST` 对合法但非对象的 JSON（数组/字符串）仍会 500，建议补 `isinstance(payload, dict)` 校验；`/api/agent_states/update` 的 `novel_id` 没做整数清洗，非整数会以文本落进 INTEGER 列；`/api/daily_runs` 的 `sync_error` 前端 ExecutionsPage 还没展示，需要 webapp 跟进；工作区行尾混用是 L-035 已知遗留，我的新增行是 LF，Git 规范化后 diff 干净，没顺手批量格式化desuwa。MEMORY.md 没有动mashitawa。
