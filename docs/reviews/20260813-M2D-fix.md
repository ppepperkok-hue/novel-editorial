# 2026-08-13 M2-D（U23 伙伴状态沉淀）修复复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 2bf7e1c（修复 fec6246 审查意见）
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m2-implementation.md 子阶段 D

### 优点

- `build_agent_prompt` 在窗口化前过滤 `role="system"` 与 `kind="mood_change"` 消息，LLM「最近对话」不再被状态自述挤占，跨伙伴也不串味；log「状态」区展示不受影响。
- `decide` 与 `revise_draft` 将 mood 更新、mood_change 留痕并入同一事务（commit 前），异常时整批回滚、可重试，符合幂等约定；talk 路径保留便捷封装，行为不变。
- 回归测试覆盖提示词过滤、跨伙伴隔离、决策/修订失败回滚与重试、正常路径同批落库。

### 问题

#### Critical

无。

#### Important

无。

#### Minor

无。

### 结论

Ready to merge: Yes

### 处理记录

- fec6246 审查的两个 P2 均已闭合（2bf7e1c）。
- 最终回归：uv run pytest -q 131 passed，ruff check 干净，pyright 0 errors，verify_constitution OK；端到端手动流程（talk → log、状态留痕）符合预期。
