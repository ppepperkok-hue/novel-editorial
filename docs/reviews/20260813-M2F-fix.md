# 2026-08-13 M2-F（U20 叙事追踪）修复复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit ab96418 + 28ff33d（修复 a4b27bb 审查意见）
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m2-implementation.md 子阶段 F

### 优点

- `plot recover <workspace_id> <thread_id>` 按工作区定位，不再跨库扫描；无关库迁移异常不再影响目标工作区，读操作不再触发静默 schema 升级。
- `plant_thread` 拒绝任意行分隔符（含尾随 `\n` / `\r` / `\r\n` / `\u2028`），与 memory note 守卫语义一致；错误信息清晰，exit 2 且不落库。
- 回归测试覆盖错误工作区 recover（不改动目标线索）与尾随分隔符多场景；155 个测试全绿。

### 问题

#### Critical

无。

#### Important

无。

#### Minor

1. **P3（ab96418 复审查）：尾随行分隔符绕过单行校验**
   - 文件：src/novel_editorial/core/plot.py（50 行）
   - 问题：`len(content.splitlines()) > 1` 放行 `"线索A\n"` 等尾随分隔符内容，实测渲染出 `- [伏笔] 线索A\n（第三章）`，章节后缀被挤到下一行。
   - 修复：改为 `"".join(content.splitlines()) != content`（28ff33d），测试补尾随 `\n` / `\r` / `\r\n` / `\u2028` 用例。

### 结论

Ready to merge: Yes

### 处理记录

- a4b27bb 审查的 P2 与 P3 均已闭合（ab96418）；复审查追加的尾随分隔符 P3 已闭合（28ff33d）。
- 最终回归：uv run pytest -q 155 passed，ruff check 干净，pyright 0 errors，verify_constitution OK。
