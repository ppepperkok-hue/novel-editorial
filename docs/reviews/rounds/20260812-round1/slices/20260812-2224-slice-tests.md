基线验证中 python run_tests.py 出现一次真实失败（test_meeting_free_e2e 的并发 seq 冲突，已用 barrier 模拟 10/10 复现 IntegrityError），即现有测试并非稳定全绿，按'现有测试不会 break'的标准不能判定正确；其余范围（webapp 45/45、build 通过、ai_words.json/compliance_words.txt/.env.example 与消费方契约一致、无硬编码 secret、无 fake-green 漏收集）均无问题。修复 flaky 测试后应重跑完整基线确认。

Full review comments:

- 修复 fake_reply 缺失的并发 seq 冲突重试（flaky 测试） — E:\code\novel-editorial\tests\test_meeting_free_e2e.py:21-42
  完整套件首次运行 `python run_tests.py` 时 `test_full_loop_with_approval_and_finish` 失败（`AssertionError: 1 not greater than or equal to 2`，test_meeting_free_e2e.py:106），单文件、按序运行、二次完整运行均通过，属概率性 flaky。根因：kickoff 事件经 `_run_candidates` 并发派发 planner/reviewer 两个 speaker，`fake_reply`（本文件 21-42 行）直接 `SELECT MAX(seq)+1` 后 INSERT，两个连接读到相同 seq 时后者必触发 `idx_meeting_messages_session_seq` UNIQUE 冲突（novel_editorial/db.py:360-361）；用 barrier 强制两线程越过 SELECT 后并发 INSERT 可 10/10 复现 `sqlite3.IntegrityError` 且 count=1，与失败观测一致。生产实现 `reply_to_mention`（tools/meeting_executor.py:250-287）为此专门做了最多 3 次重算重试，而 `fake_reply` 未模拟该行为。建议在 `fake_reply` 中捕获 IntegrityError 重算 seq 重试（与生产一致），否则高负载或 CI 下测试间歇假红，掩盖真实回归。

- 移除 run_tests.py 中恒真的重复 key 检测断言 — E:\code\novel-editorial\run_tests.py:101-105
  `_env_example_entries()` 用 dict 存储，重复 `KEY=` 行在解析时已被后者覆盖，因此 `len(entries) == len(actual)`（dict 与其键集合）恒成立，此断言永远无法检测 .env.example 中的重复 key 行，是无效断言（dead assertion）。若要真正检测重复，应在解析时保留原始行序列并比对 key 出现次数。
