# 2026-08-18 N4 编辑部讨论形态 E1 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit ebbed7b（feat(talk): add N4 structured discussion round (E1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / extension.md / docs/implementation/m5-n4-implementation.md / 06 backlog

### 优点

- E1 按实施文档完整落地：open / contribute / summarize / conclude 四步消息与 payload 结构稳定，事件只落 agent 消息，author 消息不落事件，与既有事件契约一致。
- CLI 校验与退出码正确：topic/outcome 空值、作者/未知别名/重复角色均报用法错误，缺省全员、固定发言顺序。
- 边界探针通过：连续两轮讨论 discussion_id 互不串扰、议题文本含 JSON 片段不会造成 LIKE 误匹配、部分点将只影响本轮。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- `conclude_discussion` 会为不存在的讨论落孤儿拍板消息 — src/novel_editorial/core/discussion.py:174-183
  - 问题：只校验 outcome 非空，未校验 discussion_id 是否真的开过；实测可对未开场的 discussion_id 落一条「作者拍板」消息，且该消息会被 build_agent_prompt 当作对话历史喂给伙伴。
  - 影响：核心 API 路径的数据完整性有缝；CLI 通路不会触达，属非阻塞。
  - 修法：在 outcome 校验后调用既有 `_discussion_exists`，不存在抛 NOT_FOUND，与 summarize_discussion 口径一致。

### 建议

修复 P3 后 E1 可收口；E2 可直接在该底座上接 N2 立场拒绝与 N3 沉淀。

### 结论

Ready to merge: With fixes

### 处理记录

- 2026-08-18：P3 已修复（commit 44dba7f，先写失败测试再改，24 用例绿），修复包归档于 20260818-M5N4E1-fix.md。
- 2026-08-18：E1 收口验证全量 453 测试 + smoke_m3 + stress_m3 全绿。
