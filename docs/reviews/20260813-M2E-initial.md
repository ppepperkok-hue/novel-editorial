# 2026-08-13 M2-E（U19 分层视图与按需检索）初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 49a7345
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m2-implementation.md 子阶段 E / docs/project-plan/05-executable-units.md U19

### 优点

- 三层默认视图符合最小充分原则：写手视图=记忆包（作品档案+风格锚点+章纲+写手私有记忆，不含他人笔记）；主编/责编视图=作品档案+最近对话（不含私有记忆）；作者视图=档案+班子 mood+草稿+最近意见/决策。
- `memory search` 覆盖档案/对话/意见/版本/笔记五类来源，每条带 `[来源类型] 片段（来源: 细节）` 引用；不区分大小写、按 workspace 隔离、空结果明确。
- CLI 契约正确：`--as` 非法值退出码 2，空关键词退出码 2，未知作品退出码 1；142 个测试全绿（含新增 11 个）。

### 问题

#### Critical

无。

#### Important

无。

#### Minor

1. **P3：`memory view --as 总编` 别名缺失**
   - 文件：src/novel_editorial/core/views.py（27-32 行）
   - 问题：VIEW_ROLE_ALIASES 只收「主编」未收「总编」，而 chat.py 的 ROLE_ALIASES 以「总编」为主别名（agents edit / memory note 等命令均接受），导致该命令与其他命令别名不一致。
   - 修复：补 `"总编": EDITOR_VIEW`（88fb6d8），参数化测试同步覆盖。

### 建议

- 检索暂为子串匹配，后续引入向量检索时保持来源引用结构不变。

### 结论

Ready to merge: No（With fixes）

### 处理记录

- 2026-08-13 派第 1 轮修复（88fb6d8，P3 别名）与第 2 轮修复（2e8fffa，help 文案同步）。
- 复审查确认闭合，最终回归 143 passed，ruff / pyright / 宪法全绿。详见 [20260813-M2E-fix.md](20260813-M2E-fix.md)。
