# 2026-08-19 N17 记忆衰减与归档 F3 收口记录

- 范围：docs/usage.md 新增「记忆衰减与归档（N17）」章节（commit 693104e）。
- 审查方式：总监复核（纯文档提交，按预算纪律不派独立审查 CLI；文档示例逐条实跑作为验收证据）。

### 内容

- 衰减模型与三项配置（NOVEL_MEMORY_DECAY_PER_DAY / REHEARSAL_BOOST / ARCHIVE_THRESHOLD 与 config.toml [defaults]）；
- decay / remember / archive / restore / notes --include-archived 命令与退出码语义；
- 归档在检索、笔记清单、写作记忆包的默认不可见与可恢复语义；
- 红线：衰减不删除、归档可逆留痕、不阻塞检索与创作；
- mock 下实跑示例：建作品 → 写两条笔记 → 拨时间 → decay 幂等 → archive --candidates → 默认不可见 → include-archived 可见 → restore → remember → 检索自动保鲜。

### 验证

- 文档示例端到端实跑：每一步输出与文档逐字一致（仅 id / 时间戳随运行变化）。
- `uv run pytest -q`：525 passed。
- `uv run ruff check .`：All checks passed；`uv run pyright`：0 errors, 0 warnings。
- `python scripts/verify_constitution.py`：[OK] constitution verified。
- smoke_m3：SMOKE OK；stress_m3：STRESS OK（检索阈值不回归）。

### 结论

F3 完成，N17 整体收口。
