Baseline passes (python -m compileall on all six tools exits 0; the slice's 36 tests in test_ai_taste_check/test_clean_knowledge/test_distill_lessons/test_knowledge/test_knowledge_bible/test_knowledge_keeper/test_novel_knowledge all pass), but the open P1 remains: every daily sync_latest run re-versions unchanged rows and appends duplicate history entries (reproduced 1→2/0→2; demo.db already contains duplicated history), and the round-2 findings for this slice (distill silent success, dead export tool, ai_taste heuristics, cleanup-tool defects) are mostly still present at HEAD. The knowledge slice is functional but not correct as-is.

Full review comments:

- Re-running chapter knowledge sync bumps versions and duplicates history on every daily run — E:/code/novel-editorial/tools/novel_knowledge.py:417-419
  `sync_from_chapters` always passes a non-empty `change_note` (`f"第{seq}章"` here, and at lines 442/450 for plot/timeline), so the no-churn guard in `upsert_ex` (`row["content"] == content and not change_note`, tools/novel_knowledge.py:196) never fires for chapter sync. Reproduced on a scratch DB: syncing the same summary twice moved `version` 1→2 and `novel_knowledge_history` 0→2, and the second run still reported `updated=2` (fake update signal). Because `sync_latest` is called every daily run (tools/editorial_daily.py:1317) with `LIMIT 3` while the cadence is 2 chapters/day, at least one already-synced summary is re-processed daily; demo.db already contains duplicate history rows (knowledge_id 11, versions 1–6 each ×2). The version/audit trail degrades on every execution and `snapshot()` ordering is skewed. `sync_from_bible` avoids this via `_upsert_if_changed`; chapter sync should skip when stored content is unchanged.

- distill_lessons returns ok=true when the model produces no usable lessons — E:/code/novel-editorial/tools/distill_lessons.py:179-181
  In `distill()` only a completely unparseable reply is treated as failure (`if not lessons and parsed is None`, tools/distill_lessons.py:179); a valid JSON reply with `{"lessons": []}` or a mistyped key (e.g. `{"lesson": [...]}`) returns `{"ok": true, "drafted": 0, "total_lessons": 0}`. Callers treat that as success: web_api.py:923 returns the dict to the client and services/control.py:231 invokes the CLI which always exits 0, so a mis-formatted LLM reply silently skips lesson distillation with no error surfaced and no draft created.

- export_agent_prompts is a silent no-op in proxy mode and always exits 0 — E:/code/novel-editorial/tools/export_agent_prompts.py:47-52
  Every LLM node in n8n/novel_workflow.json carries `{agent:'` in its jsonBody (verified for all 15 agent nodes) and no `{role:'system'...}` prompt, so `proxy` is always True and `main()` returns rc=0 after printing a one-line notice at tools/export_agent_prompts.py:47-52 without exporting anything. The docstring still promises exports, and the dead export path also lacks mappings for `ending_judge`/`knowledge_keeper`. This is a fake-green tool; in proxy mode it should fail loudly or be removed.

- ai_taste_check four-char stacking heuristic flags ordinary prose and misses real stacking — E:/code/novel-editorial/tools/ai_taste_check.py:78-92
  The heuristic at tools/ai_taste_check.py:78-92 counts every ≥8-consecutive-hanzi run as one occurrence (adjacent non-overlapping `[\u4e00-\u9fff]{4}` matches), so plain narration triggers `疑似四字排比堆砌 2 处`: reproduced with '她站在窗前看了很久，心里想着明天该如何开口解释这件事。' and '林深走进大殿的时候，所有人都安静下来不敢出声。' (notes fired, score 12). Meanwhile genuine stacking '天崩地裂日月无光山河变色' yields runs=1 and stays under the threshold (no note, score 6). The heuristic measures run length, not structural parallelism, so its note is noise for the editor/reviewer gates.

- ai_taste_check FILLER misses the common double-ellipsis form of 不是……而是 — E:/code/novel-editorial/tools/ai_taste_check.py:30-30
  `FILLER` lists `"不是…而是"` with a single U+2026 (tools/ai_taste_check.py:30), but the widely used written form `不是……而是` (double U+2026) is not a substring, so both `count_occurrences` and `_non_overlap_count` miss it. Reproduced: '他说他不是……而是有自己的打算。' yields zero filler hits (no note, score 0). The anti-translation-ese rule that editor/reviewer prompts explicitly call out is therefore not enforced for this form.

- clean_novel_knowledge --dry-run flag is dead and can mislead into destructive --apply — E:/code/novel-editorial/tools/clean_novel_knowledge.py:247-247
  `--dry-run` is declared `action="store_true", default=True` (tools/clean_novel_knowledge.py:247) but `args.dry_run` is never read; behavior is driven solely by `not args.apply` (line 255). Passing `--dry-run --apply` still executes the full cleanup plan, so a user who believes `--dry-run` forces a preview gets destructive changes. Either honor the flag (`apply = args.apply and not args.dry_run`) or remove it.

- clean_novel_knowledge backup filename hardcodes the demo- prefix regardless of --db — E:/code/novel-editorial/tools/clean_novel_knowledge.py:261-261
  `backup = backup_dir / f"demo-{stamp}.db"` (tools/clean_novel_knowledge.py:261) always names the backup `demo-...` even when `--db` points at another database, so backups of non-demo DBs are mislabeled and the restore target is ambiguous. Use `Path(path).stem` (or the resolved filename) as the prefix.

- novel_knowledge resolve()/get() interpolate LIKE wildcards unescaped — E:/code/novel-editorial/tools/novel_knowledge.py:348-350
  `resolve()` builds `f"%{topic}%"` directly into LIKE patterns (tools/novel_knowledge.py:348-350; same pattern in `get()` at line 229), so a topic containing `%` or `_` acts as a wildcard. Reproduced: `resolve(conn, 1, "%")` returns every row of the novel. This tool is invoked with LLM-derived topics from the agent loop (tools/agent_meeting.py:242), so arbitrary model output can silently broaden the result set beyond the intended entity.

- clean_novel_knowledge --apply crashes with UNIQUE violation when two rows normalize to the same new entity — E:/code/novel-editorial/tools/clean_novel_knowledge.py:204-209
  `_plan_renames` only merges into a row that already exists under the normalized name; when two rows normalize to the same entity that no row currently holds (e.g. '沈老爷子（已故）' and '沈老爷子(已故)' both → '沈老爷子'), both plan items get `merge_into: None` and `apply_clean` executes two plain `UPDATE ... SET entity=?` (tools/clean_novel_knowledge.py:204-209) against the UNIQUE(novel_id, category, entity) constraint. Reproduced: `apply_clean` raises `sqlite3.IntegrityError: UNIQUE constraint failed`, aborting the whole cleanup. Plan generation should detect same-target renames and chain them (or merge history into the first renamed row).

- novel_knowledge module docstring advertises a --novel-id flag the CLI does not have — E:/code/novel-editorial/tools/novel_knowledge.py:11-11
  The module docstring shows `python tools/novel_knowledge.py --snapshot --novel-id 1 --db demo.db` (tools/novel_knowledge.py:11), but the CLI only accepts `--snapshot N` (positional int), so following the docstring fails with 'unrecognized arguments: --novel-id 1'. Update the example to `--snapshot 1`.
