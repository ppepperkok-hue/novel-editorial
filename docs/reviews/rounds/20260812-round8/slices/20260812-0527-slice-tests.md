基线全绿：python run_tests.py 487/487 OK（exit 0）、webapp npm test 16/16 OK。重点路径核查无 P0-P2：ai_words.json 为合法 UTF-8 JSON、21 词与 quality_gate.py 内置表逐字一致且读取均有容错与告警；compliance_words.txt 25 词无重复、与内置词无重叠、全部可被 check() 命中；.env.example 48 个键全部有代码使用方且无硬编码 secret；tests 全部有断言、无 skip、DB 用临时目录隔离、网络调用均 mock、unittest 与 pytest 收集一致（487）且 run_tests.py 有 fake-green guard。仅 4 个 P3 文档/维护性发现，不阻塞。

Full review comments:

- [P3] README 测试数量过时：写 448 个，实际 487 个 — E:\code\novel-editorial\README.md:19-19
  README.md:19、README.md:191、README.md:222 三处声称后端测试为 448 个，但基线实测 `python run_tests.py` 输出 `Ran 487 tests in 87.145s / OK`（exit 0）。数字停留在旧版本，会让 CI 报告与文档对不上；建议统一改为 487 或改为不写死数量。

- [P3] 测试输出被被测代码的 print 污染，建议 TextTestRunner 开 buffer — E:\code\novel-editorial\run_tests.py:18-18
  运行 `python run_tests.py` 时，测试输出里混入被测代码的 stdout：`{"ok": true, "target": 2, "published": 1, ...}`、`rendered; changed nodes: ['写手A']`、`{"ok": true, "mode": "weekly", "written": 11}`（来自 tools/publish_stock.py 等工具被测试直接调用时的 print）。原因是 run_tests.py:18 的 `unittest.TextTestRunner(verbosity=2)` 未启用 `buffer=True`，这些噪音会淹没失败信息、干扰 CI 日志检索。不影响正确性，属可维护性改进。

- [P3] n8n 工作流硬编码 AI 味词表与 ai_words.json 存在同步漂移风险 — E:\code\novel-editorial\ai_words.json:1-13
  n8n/novel_workflow.json 质量门A/B（约 239/371 行）各硬编码了一份 21 词 `flavorWords` 数组，与 ai_words.json 完全一致，且注释声称二者“同源”。ai_words.json 是仓库根的共享词表（quality_gate.py、tools/editorial_steps.py 都动态读取），但修改 ai_words.json 不会同步这份 n8n 硬编码副本——只有设置了 PIPELINE_ROOT 且 n8n 节点运行时才会被覆盖。当前两份内容一致（已程序化比对），风险是未来编辑词表时平台侧与本地侧行为分叉，建议在 n8n 侧保留运行时读取并删掉硬编码副本，或在 ai_words.json 加注释提醒同步。

- [P3] webapp 测试覆盖偏浅：dashboard 测试只覆盖 helper 未覆盖页面渲染 — E:\code\novel-editorial\webapp\src\__tests__\dashboard.test.jsx:1-12
  webapp/src/__tests__/dashboard.test.jsx 仅 1 个用例，只测 `localToday()` 纯函数，DashboardPage 组件渲染、数据加载、错误态均无覆盖；audit.test.jsx 与 executions.test.jsx 各只有 1 个冒烟用例。16 个前端测试全绿（vitest run 7 文件全过），但作为核心仪表盘页面缺少渲染级回归保护。建议至少补一个 DashboardPage 的渲染/空态用例。
