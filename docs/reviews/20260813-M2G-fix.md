# 2026-08-13 M2-G（U21/U22）修复复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 67c0e28 + ee6c39b（修复 7b481f8 审查意见）
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m2-implementation.md 子阶段 G

### 优点

- `extract_style_keywords` 对纯分隔符/空 token 输入返回空集，generate / revise / quality check 不再崩溃；分隔符正则补齐 …—–·・-，垃圾关键词问题一并消除。
- 风格缺失惩罚按缺失比例归一化，任意长度描述下干净文本不再结构性失败；170 个测试全绿。

### 问题

#### Critical

无。

#### Important

无。

#### Minor

1. **P3（67c0e28 终审）：分隔符正则未收录 …—–·・- 等，纯分隔描述漏出垃圾关键词**
   - 文件：src/novel_editorial/core/style.py（10 行）
   - 问题：`extract_style_keywords("……")` 返回 `{"……"}`、"---" 返回 `{"--", "---"}`，与 docstring「separator-only descriptions yield an empty set」不符，且会在含省略号的正文中误报 style hit。
   - 修复：字符类补充六个分隔符（ee6c39b），测试补 "……" 与 "---" 用例。

### 结论

Ready to merge: Yes

### 处理记录

- 7b481f8 审查的 P1 与 P2 均已闭合（67c0e28）；终审确认后补 P3 分隔符残留已闭合（ee6c39b）。
- 最终回归：uv run pytest -q 170 passed，ruff check 干净，pyright 0 errors，verify_constitution OK。
