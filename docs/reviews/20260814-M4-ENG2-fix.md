# 2026-08-14 M4-ENG-2（瓶颈 2：检索性能）修复复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：ff95a36 → 9cdc194 修复链（覆盖 8b0541c 初始审查全部意见）
- 审查依据：AGENTS.md / rules.md / docs/implementation/m4-eng2-implementation.md

### 修复链摘要

| 轮次 | 提交 | 修复内容 | 复审查结论 |
| --- | --- | --- | --- |
| 1 | ff95a36 | FTS 子查询 JOIN + LIKE 精筛（消除非 ASCII 折叠差异与变量上限）；迁移/搜索 FTS5 探测；表缺失回退 LIKE | 3 条（P2 compile_options 局限 / P2 测试依赖 FTS5 / P3 fake 保真度） |
| 2 | 70ae0a1 | 探测加 compile_options 检查；测试硬编码断言修正 | 2 条（P2 改为运行时探测 / P2 skipif 标记） |
| 3 | 0297529 | 真实运行时探测（temp FTS5 trigram 试建 + DROP）；requires_fts5 skipif；fake session 按语句分发 | 1 条（P3 残留毒化连接池） |
| 4 | bb31ff1 | 前置 DROP IF EXISTS 自愈 + 尾部 DROP 容错 | 2 条（P2 静默吞错 / P3 机制归因不准） |
| 5 | 1288964 | 清理失败改 stderr 警告；docstring 归因修正 | 1 条（P3 归因方向纠正） |
| 6 | 9cdc194 | docstring 归因改为 pysqlite driver autocommit | Ready to merge |

### 优点

- 最终形态：FTS5 运行时探测（真实试建，覆盖可加载扩展与 trigram 缺失）、影子表缺失回退、FTS MATCH 子查询 JOIN + LIKE 精筛、2 字符回退 LIKE、残留自愈、清理失败可观测（stderr 警告）。
- 双路径一致性覆盖非 ASCII 大小写（café/CAFÉ、straße/STRASSE）、特殊字符、攻击性关键词；无 FTS5 环境 12 个真实用例 skip、3 个环境无关用例照常运行（模拟子进程验证零失败）。
- 迁移探测与搜索探测同源逻辑，无 FTS5 构建下所有命令可用。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

无。

### 结论

Ready to merge: Yes

### 处理记录

- 2026-08-14 全链审查闭合；回归验证：259 passed、ruff clean、pyright 0 errors、宪法 OK、smoke OK、stress OK（检索中位 ~0.72s，基线 1.4s 改善 50%）。
- M4-ENG-2 收口完成；验证与基线见 docs/verification/m4-eng2-closeout.md。
