Baseline validation passed (compileall exit 0, all 21 slice modules import, editorial_daily --dry-run completes with 2 published), but the slice contains one P1 functional regression (dispatch envelope mismatch silently disables writer-response and dispatch-note injection in the real daily flow) plus four confirmed P2/P3 defects (workday org-day mislabeled failed, diary/meeting dry-run writes to the DB, outbox prose left as a JSON envelope, unguarded diary JSON loads). These are reproducible and should be fixed before relying on the R1-1/F1 editorial features or dry-run workflows.

Full review comments:

- [P1] Unwrap dispatch envelope before writer-response round and note injection — E:\code\novel-editorial\tools\editorial_daily.py:1607-1609
  `_dispatch()` returns the envelope `{"mode", "dispatch", "degraded"}` (tools/editorial_daily.py:623), but `daily()` stores that envelope on `ctx.dispatch` and passes it straight into `_apply_writer_responses()` (tools/editorial_daily.py:1607-1609). Both `_apply_writer_responses` (line 663) and `_writer_dispatch_notes` (lines 766-771) read `dispatch.get("assignments")` at the top level, so with the envelope they always see an empty list: the R1-1 writer accept/reject/counter round never runs (no audit `writer_response` rows, no reject/counter relation events) and the F1 chief-editor note is never injected into writer prompts in a real run. Unit tests mask this because they assign the raw assignment dict to `ctx.dispatch` directly (tests/test_dispatch.py:91). Reproduction with a mocked successful dispatch: `_apply_writer_responses` returns the envelope unchanged and `_writer_task` output contains no "主编今日分派". Fix by unwrapping `_dispatch(...)["dispatch"]` before storing/calling.

- [P2] Mark produce-skipped workdays as skipped so close() does not report failed — E:\code\novel-editorial\tools\workday.py:276-288
  When `open()` runs with mode org/meeting/free (or any day whose plan skips produce), it only persists `published=0` and leaves the daily_runs row `status='running'` (tools/workday.py:175-177), then `_close_locked` computes `produce_status` from that row and only maps `"completed"/"skipped"` to a non-failed outcome (tools/workday.py:276-288). An intentional non-writing workday therefore closes with `status='failed'` and shows red in the panel/statistics. Reproduced: `workday.open(mode='org', dry_run=False)` -> row status 'running', then `workday.close(...)` returns `failed`. Persist a `skipped` status (or the produce result) on the run row when produce is skipped.

- [P2] Gate diary/meeting dry-run writes so moods and old diaries survive — E:\code\novel-editorial\tools\write_diaries.py:180-196
  `write_diaries.write(..., dry_run=True)` still mutates the DB: for weekly mode it executes `DELETE FROM agent_states` and inserts default moods (tools/write_diaries.py:185-196), it inserts placeholder diaries and activity rows (lines 180-184, 221-236), and `clean_old()` (line 254) deletes diaries older than 56 days with no dry-run gate. `agent_meeting.main --dry-run` likewise inserts `meeting_sessions`/`weekly_meetings`/`agent_actions` and calls `apply_report` (tools/agent_meeting.py:599-612, 648-673, 687-697, 728-734), which can set `novels.status='finishing'` or create a planning novel. Reproduced: a writer mood of satisfaction 0.9 was replaced with the default 0.5, a 60-day-old diary was deleted, and 11 placeholder weekly diaries were inserted. This contradicts the dry-run-must-not-write policy enforced for `editorial_daily` in 38c273c; gate these writes on `not dry_run`.

- [P2] Return unwrapped prose from _handle_outbox instead of the JSON envelope — E:\code\novel-editorial\tools\editorial_daily.py:154-160
  Since 54a5d0e, `_unwrap_text` keeps the whole `{"text": ..., "outbox": [...]}` envelope when collaboration fields are present (tools/agent_tool_loop.py:151-155), and `_handle_outbox` then pops `outbox` but returns `json.dumps(obj)` (tools/editorial_daily.py:154-160), i.e. the remaining `{"text": ...}` JSON rather than the plain prose its docstring promises to return. Any prose agent (writer/editor) that emits an outbox now delivers the JSON envelope to the pipeline: it is embedded in the next agent's prompt, counted by the quality gate, and can be serialized into `content_html` and published if the editor mirrors the envelope. Reproduced: `_handle_outbox(ctx, '写手A', '{"text": "正文", "outbox": [...]}')` returns `'{"text": "正文"}'`. Return `obj["text"]` when it is a string (fall back to `json.dumps(obj)` otherwise).

- [P3] Guard json.loads in latest_weekly/mood_of against dirty diary rows — E:\code\novel-editorial\tools\agent_meeting.py:115-129
  `latest_weekly()` and `mood_of()` (tools/agent_meeting.py:115-129) call `json.loads` on `agent_diaries.content` / `agent_states.mood` without a try/except. The same dirty-JSON class was just fixed in `write_diaries.weekly_payload` (97d33ca P3-5), so production rows can contain non-JSON content; when `round_speech` hits such a row it raises `ValueError` and aborts the whole meeting with no archive. Wrap both loads in a safe helper returning None on failure, matching the `_safe_load` pattern already added in write_diaries.
