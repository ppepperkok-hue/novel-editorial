# 2026-08-13 M2-C（U18 私有记忆）修复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 0801c30（主修复）→ 7678365（P3 第 1 轮）→ faaddda（P3 第 2 轮）
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m2-implementation.md 子阶段 C

### 优点

- 权限语义与验收标准一致：伙伴只能写自己的笔记，作者（老板）只读；`--as` 非法值、越权、作者写入均按 USAGE_ERROR（退出码 2）显式失败。
- content 原样入库，actor 仅做校验不落库；`memory notes` 输出 `<id> [伙伴名] <内容>`，ID 可直接用于 `memory delete`，全链路测试覆盖。
- 修复过程保持最小改动：三次提交均只涉及 cli/app.py、core/memory.py、tests/test_memory.py。

### 问题

#### Critical

无。

#### Important

无。

#### Minor

1. **P3（0801c30）：多行 content 破坏列表单行格式**
   - 文件：src/novel_editorial/cli/app.py（0801c30 约 413-415 行）
   - 问题：content 可含换行，`memory notes` 逐行输出时后续行失去 `<id> [伙伴名]` 前缀，破坏“列出 → 解析 ID → 删除”格式契约。
   - 修复：core 层拒绝含 `\n` / `\r` 的内容（7678365）；复审查后改为 `splitlines()` 语义，统一拒绝 U+2028 / U+2029 / VT / FF 等全部行分隔符（faaddda），并补 CLI 与 core 层回归测试。

2. **P3（0801c30）：`memory note` 未知 target 退出码与兄弟命令不一致**
   - 文件：src/novel_editorial/cli/app.py（0801c30 约 379-384 行）
   - 问题：CLI 把 NOT_FOUND 改写为 USAGE_ERROR（退出码 2），而 `memory notes` / `memory delete` 未知目标仍为 NOT_FOUND（退出码 1），违反 rules.md §5 契约。
   - 修复：移除 CLI 层改写，统一按 NOT_FOUND 退出码 1（7678365），相关测试断言与命名同步修正。

### 建议

- 后续若开放多行笔记，需同步设计结构化输出（如 JSON/分隔符转义），避免破坏单行解析。

### 结论

Ready to merge: Yes

### 处理记录

- 0801c30：主修复（作者只读 + 伙伴只能写自己 + notes 带 ID），独立审查给出 Ready to merge，附 2 个 P3。
- 7678365：修复 P3-1 换行与 P3-2 退出码；复审查确认闭合，提出 splitlines 边界 P3。
- faaddda：换行检查改为 splitlines() 语义并补回归；验收四连全绿（113 passed / ruff / pyright / 宪法）。
- 最终回归：uv run pytest -q 113 passed，ruff check 干净，pyright 0 errors，verify_constitution OK。
