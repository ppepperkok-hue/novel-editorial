# 2026-08-18 N4 E2 审查意见修复记录

- 修复来源：20260818-M5N4E2-initial.md 的两条 Minor（幂等 / 跨通道重申）。
- 修复方式：独立 CLI（flash + high），先写失败测试确认当前行为，再修复并回归。
- 修复范围：src/novel_editorial/core/discussion.py、src/novel_editorial/core/chat.py、tests/test_discussion.py。

### 改动

- summarize_discussion 幂等：新增既有 discussion_summary 查找，已有总结则原样返回，不再写消息、不再跑沉淀循环；新增用例断言二次总结返回同一条消息、消息总数与 viewpoint 行数不变。
- has_same_rule_refusal 跨通道：纳入 kind=discussion_contribution 的拒绝历史（rule 字段只在拒绝表态存在，stated 不误匹配），docstring 同步更新；新增用例断言上一轮讨论拒绝后新一轮命中走 reaffirmation + repeated=true。

### 验证

- `uv run pytest -q tests/test_discussion.py tests/test_refusal.py tests/test_delegation.py`：83 passed（含 2 个新增用例，talk/delegate 既有行为不受影响）。
- `uv run ruff check .`：All checks passed。
- `uv run pyright`：0 errors, 0 warnings, 0 informations。
- 全量 `uv run pytest -q`：463 passed。

### 结论

审查意见全部处理完毕，E2 收口（commit bc71c99）。
