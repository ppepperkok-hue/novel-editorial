# 2026-08-20 N9 质量门语料校准 Q2 修复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 0bd0071（fix(config): make threshold write-back safe for multiline TOML and idempotent (N9 Q2 review)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n09-implementation.md / src/novel_editorial/core/config.py / tests/test_config.py

### 优点

- _scan_chunk 状态机正确跳过多行字符串/未闭合数组内的行，块内假键与假段头不再参与结构匹配；同值调用经 tomllib 短路、文件字节级不变；`8#keep` 注释保留。
- 10 条新回归覆盖审查复现场景；目标 59 条全绿；全量 879 passed；ruff / pyright / 宪法全绿。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

- `src/novel_editorial/core/config.py:287-293`：多行基本字符串内 `\"""` 转义被误判为闭合（合法 TOML `desc = """a\"""b"""`），可静默篡改字符串或写出非法 TOML；上版 6aeac79 对该场景安全，属本次回归。

#### Minor（可后补）

无。

### 建议

修复转义三引号后收口。

### 结论

Ready to merge: With fixes

### 处理记录

- 2026-08-20：派 M5-N09-Q2FIX2，commit 0a86458 修复。
