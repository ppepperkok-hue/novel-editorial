Baseline is fully green: python run_tests.py runs 448 tests (OK, exit 0), webapp npm test passes 7 files/16 tests, and npm run build succeeds (only a chunk-size warning). Findings are non-blocking P2/P3 quality issues: a zero-test fake-green path in run_tests.py, overlapping entries in ai_words.json that double-count in one gate, missing guards/tests for the shipped data files, undocumented env toggles in .env.example, frontend coverage gaps, and midnight-boundary flake risk. No P0/P1, no silent-failure or concurrency/security issues found in scope.

Full review comments:

- run_tests.py exits 0 when unittest discovers zero tests (fake green) — E:/code/novel-editorial/run_tests.py:10-13
  run_tests.py only checks result.wasSuccessful() before sys.exit, so an empty or misnamed tests/ directory yields a green baseline that runs nothing. Reproduced: pointing TestLoader().discover at a directory with no test*.py files prints 'Ran 0 tests in 0.000s OK' and wasSuccessful() is True, giving exit code 0. Scope: if tests/ is ever emptied or the files are renamed, the project's own verification gate silently stops testing. Fix: fail when result.testsRun == 0.

- ai_words.json overlapping entries double-count in quality_gate density — E:/code/novel-editorial/ai_words.json:3-6
  ai_words.json lines 3-6 contain overlapping entries ('微微一' vs '微微一愣', '缓缓' vs '缓缓说道'). quality_gate.ai_flavor_density (novel_editorial/quality_gate.py:45-52) sums per-word re.findall counts, so one occurrence of '微微一愣' or '缓缓说道' is counted as 2 hits (verified: text '她微微一愣' yields hits=2 for one phrase), while the publish-path gate tools/editorial_steps.py:359 and the n8n 质量门 JS use a single | alternation regex that counts non-overlapping matches. The two gates documented as 同源 (docs/evolution.md:427) therefore disagree on the same text. Impact is limited to inflated ai_flavor_density/style score in short chapters or repeated phrases.

- No test guards the shipped ai_words.json / compliance_words.txt; corruption is silently swallowed — E:/code/novel-editorial/tests/test_compliance.py:28-35
  No test reads the real data files: the only references are tests/test_editorial_steps.py:211 (hardcoded words) and tests/test_compliance.py:28-35 (patches WORDS_FILE with a temp file). quality_gate.py:18-24 and tools/editorial_steps.py:321-329 catch OSError/ValueError when loading ai_words.json and silently fall back to lists that differ between modules (15 words in quality_gate.py vs 21 in editorial_steps.py:332-336), and compliance._load_words() silently returns built-ins if the file is unreadable. A corrupted ai_words.json (bad encoding, trailing comma) or an accidentally emptied compliance_words.txt ships green and changes gate behavior without any test failing.

- .env.example omits behavior-critical env vars read at import time — E:/code/novel-editorial/.env.example:1-5
  config.py reads AGENCY_ENABLED, TASK_RESPONSE_MODE, DISPATCH_MODE, MEETING_MODE, CLAIM_INJECT, RELATION_WEIGHT, TOPIC_REQUEST_ACTIONS, REWORK_MAX, REVIEW_RETRY_MAX and AGENT_CTX_* (config.py:33-49), N8N_BASE / N8N_WORKFLOW_DAILY / N8N_WORKFLOW_WEEKLY / N8N_WORKFLOW_KEEPER (config.py:30-32) and tools/n8n_api.py:8 reads N8N_EMAIL — none of these appear in .env.example, and only TASK_RESPONSE_MODE is mentioned anywhere in README.md:55. Operators following the example file cannot discover these agent-behavior toggles, which silently change orchestration semantics (agency, task response, dispatch, meeting mode).

- webapp tests cover 7 of 20+ components; landing page has only a tautological helper test — E:/code/novel-editorial/webapp/src/__tests__/dashboard.test.jsx:3-9
  The 16 frontend tests cover App/audit/chapters/dashboard/editorial/executions/works, while CostPage, FlowPage, MeetingsPage, ReaderPage, SettingsPage, AgentsPage, Shell, CommandPalette and MeetingLive have zero tests. dashboard.test.jsx asserts only the localToday helper by re-implementing it in the expectation (it can only fail on formatting), so the app's landing page (cost/quality/health rendering) has no component-level coverage.

- Time-dependent assertions can flake when the run crosses midnight — E:/code/novel-editorial/tests/test_agent_actions.py:108-117
  tests/test_agent_actions.py:108-117 and :142 compare _due_date output to a freshly computed (datetime.now() + timedelta(days=3)).strftime('%Y-%m-%d'); if the production call and the assertion straddle 00:00 the date strings differ and the test fails spuriously. tests/test_daily_runs.py:29 uses the same now-relative pattern (now - 1 minute). Freezing time (e.g., patching datetime) would make these deterministic.
