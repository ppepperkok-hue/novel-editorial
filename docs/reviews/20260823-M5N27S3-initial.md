# 2026-08-23 M5-N27-S3 审查报告（初审查）

- 审查方式：codex exec review（独立 CLI，只读，flash/high）
- 审查范围：commit 1afba94（feat(choice): add free-will behavior selector pipeline）
- 审查依据：AGENTS.md / rules.md / docs/implementation/m5-n27-implementation.md / 09-free-will-design.md / 10-free-will-checklist.md

### 优点

- 三层管线纯本地计算、不调 LLM；粗筛 / 权重 / 加权随机 / 沉默入口各司其职，seed 复现与换 seed 变体、被拒单调性均有断言。
- 权重输出 [0,1] 可比较值、feedback 常量注释有依据；34 条测试 + smoke，ruff / pyright 全绿。

### 问题

#### P2（应该修）

- load_feedback_counts 双倍计数 — src/novel_editorial/core/choice.py:448-465
  每个真实裁决在 behavior_timeline 写两行（decision 的 relationship+impression、delegation 的 relationship+viewpoint/impression），按 source 计数导致一次拒绝计 2，反馈衰减比文档契约快约一倍。修法：每个裁决只计一次（如按 kind 限定）。
- pick_candidate 全零权重且 dial=0 返回 index 0 — src/novel_editorial/core/choice.py:385-388
  文档承诺空 / 全零返回 None，但守卫在 dial==0 分支之后，`pick_candidate([0.0,0.0], dial=0.0)` 返回 0。修法：全零检查移到 dial 分支前。
- 无 trigger 时 _motive_factor 被动机强度缩放 — src/novel_editorial/core/choice.py:289-296
  文档称无 trigger 为中性，实现把 topics is None 当作「全部动机相关」，80 强度动机把中性 0.5 拉到 0.4。修法：topics is None 时返回 1.0。

### 结论

Ready to merge: With fixes（3 条，1 重要 + 2 契约一致）

### 处理记录

- 2026-08-23 总监派 M5-N27-S3-FIX，修复提交 c1a9ee3：反馈计数按 kind="relationship" 每裁决计一次（注释写明依据）、全零守卫前置、无 trigger 动机因子中性；4 条失败测试先行，全量 1266 测试绿。
- 2026-08-23 修复 c1a9ee3 由总监复核收口（diff 抽查 + 受影响测试 + 员工全量 1266），审查链收敛，S3 收口。
