# 2026-08-19 N6 H1 审查 P3 修复记录

- 修复来源：20260819-M5N6H1-initial.md 的 Minor（多行设定内容跨行错乱）。
- 修复方式：独立 CLI（flash + high），先写失败测试再改。
- 修复范围：src/novel_editorial/core/setting.py、tests/test_setting.py。

### 改动

- settings_section 渲染 content 时折叠空白（" ".join(content.split())，与 _snippet 一致），来源后缀保持在同一行；add_setting 的换行合法性不变。
- 新增参数化失败测试（\n 与 \r\n）：settings_section / build_memory_pack / build_editor_view 三处输出均保持单行、无裸续行。

### 验证

- 新用例先红后绿（2 passed）。
- `uv run pytest -q tests/test_setting.py tests/test_draft.py tests/test_views.py`：102 passed。
- `uv run ruff check .`：All checks passed；`uv run pyright`：0 errors。
- 全量 `uv run pytest -q`：598 passed。

### 结论

审查意见处理完毕，H1 收口（commit 6f8c76c）。
