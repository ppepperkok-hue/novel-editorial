# 2026-08-21 N24 API 服务层 S1 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 75e06ac（feat(api): add FastAPI skeleton with workspace routes and config (N24 S1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n24-implementation.md / src/novel_editorial/api/app.py / src/novel_editorial/cli/api.py / src/novel_editorial/core/config.py / tests/test_api.py / tests/test_config.py / tests/test_deps.py

### 优点

- FastAPI 骨架忠实实现 S1 规格：NovelError 映射（NOT_FOUND→404、USAGE_ERROR→422、其余→500）、统一 `{"detail": ...}`、配置驱动 host/port（1–65535 校验）、CLI `api serve` 薄壳、依赖守卫补 api 层。
- 全部路由复用既有 core/store 函数，不复制业务逻辑；GET 路由不落事件。
- 85 条相关测试、ruff、pyright、uv lock 检查全绿；真实起服冒烟（health / POST 201 / show band 4 人 / 404）通过。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- [P3] GET /works 缺少次级排序键 — src/novel_editorial/api/app.py:84
  仅按 `Workspace.created_at` 排序，同一时钟刻度内创建的多条记录顺序未定义，测试与客户端依赖创建顺序时会抖动；show_workspace 的 band 同理。修法：`order_by(created_at, id)`。

### 建议

- API 契约与测试一律要求确定性排序，时间戳相同用 id 决胜。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：同 created_at 场景顺序未定义。
- 修复：随 S2 提交 7893b83 落地——works 与 band 均按 `created_at, id` 升序，补同时间戳双作品排序断言；回归全绿。
