# 2026-08-21 N22 好句识别与保留 S2 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit c9e159e（feat(quality): surface good sentences in quality explain (N22 S2)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n22-implementation.md / src/novel_editorial/quality/gems.py / src/novel_editorial/cli/quality.py / tests/test_quality_gems.py / tests/test_quality_explain.py

### 优点

- 引语片段合并规则落实：`「别开灯。」他说。` 与 `她说：“别开灯。”然后走了。` 均合并为一条 gem，index 与 quality explain 分句口径一致；信号固定顺序去重。
- `quality explain` 好句区块只追加在 style 摘要之后，既有问题行 / CLEAN_MESSAGE / style 摘要文案与行序零改动；46 条相关测试、ruff、pyright 全绿；CLI 冒烟实测输出符合文档。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

- [P2] 未合并的闭引号片段被当作好句输出 — src/novel_editorial/quality/gems.py:100-103
  无法合并且以闭引号开头的片段仍会追加为独立 GoodSentence：`“灯亮着。水开了。”` 会产生单独一个 `”` 的「好句」，`“别开灯。”他喊“快走。”她又说。` 会产生 `”他喊“快走`、`”她又说` 两个残句，与实施文档「输出不出现以闭引号开头的残句」矛盾；新增测试还把错误输出固化为断言。修法：无法合并的闭引号开头片段直接跳过（不是句子），并同步修正测试与文档。

#### Minor（可后补）

无。

### 建议

- 「输出永不出现以闭引号开头的条目」作为硬保证，补一条遍历多种引语文本的回归断言。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：`”`、`”他又说。` 实测会作为 gem 输出。
- 修复：随 S3 提交 79e7b76 落地——闭引号残句直接跳过，新增 `test_single_close_quote_fragment_is_skipped` 与 `test_output_never_starts_with_close_quote` 硬保证；全量 1019 passed、smoke_m3 / stress_m3 全绿，S3 审查 Ready to merge。
