切片范围：novel_editorial/ 全部模块 + services/*；基线：python -m compileall -q novel_editorial 通过（exit 0），tools 侧 20 个依赖接口签名全部与调用匹配。发现 3 个 P2（daily 手动运行假启动、周会子进程静默失败、畸形 LLM 输出可击溃整个会议，后者已用复现脚本证实）与 2 个 P3（静态文件路径包含检查可逃逸、行动项认领竞态），均属本切片内可修复的真实缺陷，故判定不正确。

Full review comments:

- [P2] run_workflow_now("daily") 未启动任何任务却返回 started=True — E:\code\novel-editorial\novel_editorial\services\control.py:216-223
  control.py:216-223 的 daily 分支只返回 {"ok":true,"started":true,"note":"日更已在后台启动，可在执行记录页查看进度"}，并没有像 weekly 分支那样启动任何线程/进程。它实际只做了 control.py:335 的 set_many("manual_run_requested","1")，该标志仅由每天定时运行一次的计划任务消费（scripts/install_daily_task.ps1 用 New-ScheduledTaskTrigger -Daily -At，tools/preflight.py:219 / tools/editorial_daily.py:482 读取标志）。因此点击面板"立即运行"后最长要等 24 小时才会真正执行，而响应与审计都声称"已启动"，属于 fake green。

- [P2] _run_cli 吞掉非零退出码，周会链路步骤静默失败 — E:\code\novel-editorial\novel_editorial\services\control.py:85-97
  control.py:85-97 的 _run_cli 用 capture_output 执行子进程后从不检查 returncode，只有抛异常（如超时）才会 _alert；非零退出码、stderr 内容全部被丢弃。_weekly_worker（control.py:206-211）依次调用 architect_weekly.py、agent_meeting.py、distill_lessons.py，任一脚本失败（例如 LLM 密钥失效、JSON 解析失败返回 exit 1）时面板上没有任何告警、审计或日志，周会看起来"已跑完"。修复应检查 out.returncode 并在非零时 _alert 并附带输出片段。

- [P2] LLM 输出的畸形 agency/outbox 字段会让整个会议崩溃 — E:\code\novel-editorial\novel_editorial\services\meeting_session.py:294-295
  agency.py:60 的 int(item.get("action_id") or 0) 与 meeting_session.py:294-295 的 int(item.get("chapter_id") or 0)、int(item.get("reply_to") or 0) 对 LLM 生成的字符串直接 int()。已复现：agency.apply(conn,"writer",0,[{"action":"claim_task","action_id":"abc"}]) 抛出 ValueError，且该异常经 _handle_meeting_actions（meeting_session.py:454 无 try/except 包裹）传播到 _run_locked 外层，把整个会话标记为 failed，跳过剩余发言人、总结报告与会后任务。这违背了 agency 白名单"畸形项应被拒绝并审计"以及"单个参会者失败不拖垮会议"的设计意图。

- [P3] _serve_static 的 startswith 前缀检查可逃逸到 dist 前缀兄弟目录 — E:\code\novel-editorial\novel_editorial\web_api.py:1021-1028
  web_api.py:1021-1028 的路径包含检查是 str(target).startswith(str(root))，没有分隔符/父级校验。请求如 /../dist-other/secret 经 (root / rel).resolve() 后落在 webapp/dist 之外，但字符串前缀仍以 root 开头从而放行（例如 E:\...\webapp\dist2\x 以 E:\...\webapp\dist 开头）。当前 webapp/ 下只有 dist、node_modules、src，尚无可利用的兄弟目录，属于潜在缺陷；建议改为 target == root or root in target.parents 的标准包含判断。

- [P3] claim_action 的 check-then-act 竞态允许同一行动项被重复认领 — E:\code\novel-editorial\novel_editorial\services\activity.py:132-144
  activity.py:126-146 先 SELECT 校验 status/claimed_by，再无条件 UPDATE ... WHERE id=?。ThreadingHTTPServer 并发处理请求时，两个并发认领都能通过校验并以 ok 返回，后写的 UPDATE 静默覆盖先写的 claimed_by，造成双重认领且无任何痕迹。建议改为 UPDATE agent_actions SET claimed_by=?, status='claimed' WHERE id=? AND claimed_by='' AND status='pending'，按 rowcount 判断。
