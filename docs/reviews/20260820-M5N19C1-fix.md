# 2026-08-20 N19 一致性自动核查 C1 修复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit a1da381（fix(consistency): report conflicts only when text value absent from setting (N19 C1 review)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n19-implementation.md / src/novel_editorial/core/consistency.py / tests/test_consistency.py

### 优点

- 数字冲突改为「正文值不在设定同单位值集合中才报」，多值设定不再误报；「两」归一为 2；单字伏笔回落为归一内容本身匹配；detail 措辞统一为「正文「X」不在设定值中（设定含：…）」。
- TDD 先红后绿；目标 26 条全绿；全量 918 passed；ruff / pyright 干净。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- `src/novel_editorial/core/consistency.py:177-191`：同一句同一逻辑值不同写法（十二点 / 12点）会产生两条冲突，issue 数依赖写法而非归一值；
- `docs/implementation/m5-n19-implementation.md` 的 C1/C2 规格仍写旧匹配规则与旧 detail 格式，与实现不同步。

### 建议

两条一并修复后收口。

### 结论

Ready to merge: With fixes

### 处理记录

- 2026-08-20：派 M5-N19-C1FIX2（053aa81）。
