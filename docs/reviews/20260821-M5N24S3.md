# 2026-08-21 N24 API 服务层 S3 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 0e1b9d5（fix(api): make style route read-only and document HTTP API (N24 S3)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n24-implementation.md / src/novel_editorial/api/app.py / tests/test_api.py / docs/usage.md / config.example.toml

### 优点

- S2 审查 P1 完整闭合：GET /works/{id}/style 只读查询，锚缺失返回 `{"description": "", "forbidden_words": ""}` 且不新建行，新测试用全表行数快照验证。
- usage.md「HTTP API（N24）」小节与实现逐项一致（启动、配置、路由表、错误映射、只读红线、curl 示例）；rules.md 目录约定与依赖方向补 api 层；config.example.toml 补默认值。
- 全量 1047 测试、smoke_m3 SMOKE OK、stress_m3 STRESS OK、ruff / pyright 全绿；真实起服冒烟通过。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- [P3] api 层内联锚查询，建议抽到 core — src/novel_editorial/api/app.py:114-127
  `_style_anchor_dict` 直接查询 StyleAnchor，绕过了 core.style 服务层，与「只复用 core / store 既有能力」及新写的 `api → core` 依赖方向有轻微出入；`core.style.get_style_anchor` 仍是 CLI 的访问器，未来读语义变更要改两处。修法：在 core.style 增只读访问器（如 `get_style_anchor_or_none`）供 API 使用，后续可顺带统一 style_drift 的同类只读读取。

### 建议

- 留档为工程待办，随下次触碰 style 读取语义时一并清理。

### 结论

Ready to merge: With fixes（P3 留档）

### 处理记录

- S2 审查 P1（style 路由落库）：随 commit 0e1b9d5 修复，全量回归通过。
- S3 审查 P3（core 访问器抽取）：无行为影响，按项目先例留档 progress-summary「已留档的待办项」，待后续统一 style 读取语义时清理。
- N24 审查链收敛。
