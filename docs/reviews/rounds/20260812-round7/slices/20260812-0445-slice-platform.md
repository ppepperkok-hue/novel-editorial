Targeted validation passed (40/40 slice tests, all slice files compile/parse) and no P0/P1 blocking defect was found; the confirmed issues are one P2 in a dev-tooling path and several P3 robustness/diagnostics gaps that do not break the main runtime.

Full review comments:

- Fix worker always passes -m "" and fails when no model is specified — E:\code\novel-editorial\scripts\_run_fix_worker.ps1:40-44
  scripts/_run_fix_worker.ps1:40-44 unconditionally appends `-m ""` to the codex command line, but run_fix_batch.ps1 only forwards `-Model` when it is non-empty (`[string]$Model = ""`), so the documented optional-parameter path dispatches workers that fail instantly. Reproduced with the exact Start-Process invocation: `node codex.js exec --ephemeral -m "" "reply with exactly: OK"` exits 1 with `ERROR: {"error":{"message":"The supported API model names are deepseek-v4-pro or deepseek-v4-flash, but you passed .","type":"invalid_request_error",...}}` and writes nothing useful to the .out log. `run_fix_batch.ps1 -TaskFiles ...` without `-Model` therefore produces zero completed fixes; the worker should omit the `-m` argument when `$Model` is empty.

- Guard int() conversions in record_work against dirty LLM numbers — E:\code\novel-editorial\tools\record_work.py:414-415
  tools/record_work.py:414-415 (`int(c.get("prompt_tokens") or 0)`, `int(c.get("completion_tokens") or 0)`) and line 306 (`words = int(ch.get("words") or 0)`) raise an uncaught ValueError when the payload carries non-integer strings such as "1,234" or "1234.5" (reproduced: `int("1,234")` -> ValueError). Because record_payload() has no per-field guard, one dirty value aborts the whole daily-result recording; in the n8n/record path the chapters were already published on Fanqie but stay unmarked locally, so the next preflight's `check_already_ran` sees no published chapter and the day can be published again (duplicate-chapter risk). Other numeric fields (`expected_recover`, settings) already use try/except, so these three sites are inconsistent with the file's own defensive style.

- Handle non-dict protagonist entries in create_book instead of crashing — E:\code\novel-editorial\tools\create_book.py:270-273
  tools/create_book.py:270-273 calls `p.get("name")` for `protagonists[:MAX_PROTAGONISTS]`; if `row["protagonists"]` parses to a list of strings instead of dicts (dirty panel/seed data; reproduced: `"主角".get("name")` -> AttributeError), the AttributeError is not covered by the surrounding `except (URLError, HTTPError, RuntimeError, ValueError)` and escapes as a full traceback rather than the structured `{"ok": false, "error": ...}` that the rest of create_book_on_fanqie returns on every other failure. Add a per-entry isinstance check or include AttributeError in the except clause.

- Handle missing Chrome CDP endpoint in inject_fanqie_cookie gracefully — E:\code\novel-editorial\scripts\inject_fanqie_cookie.py:35-36
  scripts/inject_fanqie_cookie.py:35-36 calls `urllib.request.urlopen(CDP + "/json/list", timeout=10)` with no try/except; when the debug Chrome on 127.0.0.1:9222 is not running (the common case after a reboot), the script dies with a URLError traceback instead of the "no fanqie tab found" style message it prints for the no-tab case. A short except around the probe (or around main's calls) would make the failure explicit and actionable.

- Report lock release failures distinctly in release_lock.py — E:\code\novel-editorial\tools\release_lock.py:19-22
  tools/release_lock.py:19-22 maps every OSError to "no lock to release", including Windows PermissionError raised when the lock file is still open by the running scheduler; an operator who sees "lock released" (or its absence) gets no signal that the lock is still held, and `sys.exit(main())` always returns 0 so n8n cannot branch on failure either. Distinguish FileNotFoundError (truly absent) from other OSErrors and propagate a non-zero exit for the latter.

- Check codex exit code in run_review.ps1 to avoid fake green reports — E:\code\novel-editorial\scripts\run_review.ps1:222-223
  scripts/run_review.ps1:222-223 runs `& node $codexJs @a *> $Out` and immediately prints "Report written to: $Out" without checking `$LASTEXITCODE` (the variable is never referenced in the file); a review that crashes (e.g., the reproduced `-m ""` invalid-model error) still reports success. In slices mode, line 204 treats any non-empty .md as finished (size > 0), so an error transcript written into the report file is counted as a completed slice in the index. Add an exit-code check and mark failed slices (e.g., size==0 or .err content) in the index.

- Treat unqueryable PIDs on Windows as alive in preflight lock handling — E:\code\novel-editorial\tools\preflight.py:182-184
  tools/preflight.py:182-184 returns False when `OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION, ...)` fails, i.e. access denied (scheduled task running as a different user/elevated context vs a manual run) or any other query failure is interpreted as "PID dead", so acquire_lock() unlinks the lock and lets a second run start while the first is still publishing (double-publish window). This contradicts the conservative fallback used for exceptions (returns True) and the POSIX PermissionError branch. Query failures should return True (assume alive) so only confirmed-dead PIDs reclaim the lock.

- Persist item_id so failed publish chains can be cleaned up — E:\code\novel-editorial\tools\publish_stock.py:155-159
  tools/publish_stock.py:117-159 creates the Fanqie draft via new_article and only stores `item_id` locally (chapter.fanqie_item_id) after the full chain succeeds; if cover_article or publish_article fails (line 159 returns without the id), the draft already exists on Fanqie but is unreferenced locally, so the next run's retry creates a second orphaned draft and there is no way to identify or delete the first one. Consider recording the item_id of the created draft (e.g., a draft-status row or the failure log) so partial-chain failures are visible and retries reuse the same draft.

- Quote n8n bin path in start_n8n.ps1 Start-Process arguments — E:\code\novel-editorial\scripts\start_n8n.ps1:29-30
  scripts/start_n8n.ps1:29-30 passes `-ArgumentList $n8nBin` as a bare string; if `$env:APPDATA` (or the username) contains a space, node receives `...\npm\node_modules\n8n\bin\n8n` split across arguments and the start fails. Wrap the argument in quotes (or pass an argument array) so paths with spaces survive; the same quoting pattern is already used correctly in install_autostart.ps1.
