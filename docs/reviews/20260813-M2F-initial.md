# 2026-08-13 M2-F（U20 叙事追踪）初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit a4b27bb
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m2-implementation.md 子阶段 F / docs/project-plan/05-executable-units.md U20

### 优点

- PlotThread 模型与迁移（98fd820d7e77）线性挂在最新 head 后，旧库升级可建表；`plot plant / list / recover` 状态流转（planted → recovered）与幂等语义清晰，退出码契约正确。
- 写手记忆包与审稿提示词（build_agent_prompt 对 reviewer）均注入「悬置线索：」节，只列 planted/pending，已回收线索不出现；跨作品隔离。
- 测试覆盖埋设/列表/回收/错误路径/注入/旧库升级，153 个测试全绿。

### 问题

#### Critical

无。

#### Important

1. **P2：plot recover 跨工作区全量扫描，无关库异常时命令整体失败**
   - 文件：src/novel_editorial/core/plot.py（78-87 行）
   - 问题：recover_thread 遍历 list_workspace_ids 并逐个调用 workspace_session（首次访问会对每个库执行 Alembic upgrade head）；只要存在一个迁移版本不兼容的无关库，命令就以 exit 3 中止，目标线索所在工作区轮不到；且读操作附带写副作用（静默升级无关库 schema）。
   - 修复：recover 改为接收 workspace_id，只查指定工作区（ab96418）。

#### Minor

1. **P3：线索 content 未拒绝多行，破坏提示词与列表逐行格式**
   - 文件：src/novel_editorial/core/plot.py（48-49 行）
   - 问题：plant_thread 只校验非空，多行 content 可入库，open_thread_lines 与 plot list 的逐行渲染被破坏。
   - 修复：增加 splitlines 守卫拒绝多行（ab96418），复审查后又补拒尾随分隔符（28ff33d），与 memory note 守卫一致。

### 结论

Ready to merge: No（With fixes）

### 处理记录

- 2026-08-13 派第 1 轮修复（ab96418：recover 按工作区 + 多行拒绝）与第 2 轮修复（28ff33d：尾随分隔符）。
- 终审确认全部闭合，最终回归 155 passed，ruff / pyright / 宪法全绿。详见 [20260813-M2F-fix.md](20260813-M2F-fix.md)。
