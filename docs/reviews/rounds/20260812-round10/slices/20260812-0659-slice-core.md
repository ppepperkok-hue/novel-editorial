slice 整体质量高：compileall、64 个目标单测与 HTTP 冒烟全部通过，未发现 P0/P1；但 hot_topics.refresh 的固定临时文件并发写盘在 Windows 上已复现 PermissionError（P2 竞态），按严格标准不属于完全无缺陷，另有 5 个 P3 边缘问题建议顺手修复。

Full review comments:

- [P2] hot_topics.refresh 固定临时文件名并发写盘竞态 — E:\code\novel-editorial\novel_editorial\hot_topics.py:287-291
  `novel_editorial/hot_topics.py:287-291` 使用固定 `target.with_suffix(".tmp")` 作为临时文件再 `os.replace` 落盘。两个生产调用方可并发触发：`services/control.py:110`（用户手动刷新，独立后台线程）与 `services/control.py:251`（周会 worker，weekly.lock 不覆盖手动刷新路径）。并发复现实验（三线程各 200 次模拟同一 tmp 写+replace）在 Windows 上稳定抛出 `PermissionError: [WinError 32] 另一个程序正在使用此文件`，导致 refresh 失败并写入 alerts.log，且两个线程相互截断同一 tmp 文件存在产出损坏 JSON 的窗口（`misc.load_hot_topics` 读取损坏文件会静默降级并告警）。建议改为进程唯一临时名（pid/时间戳/uuid）再 os.replace。

- [P3] load_meetings 的 topics 字段 JSON 解析缺少容错 — E:\code\novel-editorial\novel_editorial\services\misc.py:136-136
  `novel_editorial/services/misc.py:136` 直接 `json.loads(r["topics"] or "[]")`，而同一函数中 `report` 与 `attendees` 都包了 try/except 并回退默认值。若 weekly_meetings 表存在损坏的 topics 数据（手动编辑、旧版本写入或部分写入），GET /api/meetings 会整体 500；该分支属于面板只读接口，损坏一行即可让整页不可用。建议与 attendees 采用相同的容错模式。

- [P3] write_knowledge 仅校验 title/source/updated_at，type 可注入换行破坏 frontmatter — E:\code\novel-editorial\novel_editorial\services\knowledge.py:82-87
  `novel_editorial/services/knowledge.py:82` 的换行校验只覆盖 title/source/updated_at，而 `type`（来自调用方 meta，如 `POST /api/knowledge` save 的 `payload["meta"]`）未经校验直接以 `f"type: {meta['type']}\n"` 写入 frontmatter。传入含换行的 type 会注入额外键值行，破坏 frontmatter 结构，使 `_parse` 后续解析错乱并污染所有使用该知识包的系统提示。建议把 type 纳入换行校验白名单。

- [P3] POST /api/knowledge save 中 write_knowledge 的 ValueError 未捕获，返回 500 — E:\code\novel-editorial\novel_editorial\web_api.py:911-911
  `novel_editorial/web_api.py:911` 调用 `write_knowledge(file, dict(meta), body)`，而该函数在路径穿越（`_resolve_knowledge` 返回 None）或字段含换行时会抛 ValueError。当前未就地捕获，异常冒泡到 do_POST 外层 except，前端收到 500 "internal error" 且只有 alerts.log 可见详情；这类输入错误应返回 4xx 明确提示（同分支的 `knowledge_drafts accept` 在 `web_api.py:960` 有同样问题）。

- [P3] bind_book 的 book_id 未清洗换行，可向 ~/.n8n/.env 注入额外配置行 — E:\code\novel-editorial\novel_editorial\services\ending.py:51-57
  `novel_editorial/services/ending.py:52,57` 将用户输入的 book_id 直接以 `f"FANQIE_BOOK_ID={book_id}"` 写入 `~/.n8n/.env`，只做了首尾 strip。book_id 若包含换行（面板输入或 API 直接调用），可写入额外键值行污染 n8n 环境配置，且写入成功后数据库已更新、无回滚。建议校验 book_id 不含 \r\n 后再写盘。

- [P3] 会议 outbox 单条消息 chapter_id/reply_to 非整数会丢弃该 agent 全部外发邮件 — E:\code\novel-editorial\novel_editorial\services\meeting_session.py:311-312
  `novel_editorial/services/meeting_session.py:311` 中 `chapter_id=int(item.get("chapter_id") or 0)` 与 `reply_to=int(...)` 对 LLM 输出的字段直接 int()；若模型输出非数字字符串（如 "chapter3"），抛 ValueError 使整个 `_handle_meeting_actions` 中断，同一 speech 的其余 outbox 消息全部静默丢弃（仅有 audit 记录），且 agency 部分已先行提交造成部分生效。建议对单条消息做 try/except 或使用容错转换，逐条跳过坏数据。
