# 2026-08-14 M4-ENG-1（瓶颈 1：CLI 拆包）修复复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 6d02f2e（修复 73b7ea5 审查意见；链上前序修复 121b52c、73b7ea5）
- 审查依据：AGENTS.md / rules.md / extension.md / docs/architecture/skeleton.md

### 优点

- 121b52c 清除 talk/draft 延迟 import 的隐藏循环依赖，改为模块顶部直连 `build_client`，extension.md 指针同步修正。
- 73b7ea5 补齐 5 个混合用例的 `cli.talk.build_client` patch，测试不再依赖 conftest 删 key 兜底。
- 6d02f2e 将 inspect 用例中 talk 的 mock 回复与 draft 解耦（改为默认「（模拟回复）」），使 `assert "钩子埋在最暗处" in result.output` 只能由 [版本] 层满足，恢复断言强度。
- 每次修复后全量验证全绿：237 passed、ruff clean、pyright 0 errors、宪法 OK、smoke_m3 SMOKE OK。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

无。

### 建议

无。

### 结论

Ready to merge: Yes

### 处理记录

- 2026-08-14 复审查通过；回归验证：237 passed、ruff clean、pyright 0 errors、宪法 OK、smoke_m3 SMOKE OK。
- M4-ENG-1（瓶颈 1 CLI 拆包）收口完成；实施与验证记录见 docs/verification/m4-eng1-closeout.md。
