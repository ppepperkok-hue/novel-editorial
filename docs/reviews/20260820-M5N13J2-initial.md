# 2026-08-20 N13 作品结构 J2 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 49b6840（feat(structure): add structure/outline CLI, outline versions, and progress visibility (N13 J2)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / extension.md / docs/implementation/m5-n13-implementation.md / 06 backlog

### 优点

- structure / outline 两个命令组、works status / show 接入、build_memory_pack 大纲注入全部按 J2 规格落地；零结构输出除新增状态行外与既有断言一致（审查员核对 works show 输出与既有测试）。
- 端到端与失败路径测试覆盖齐全（建树、挂草稿、环移动、互斥 flag、三态、大纲创建/修订/历史、事件留痕、记忆包两态）；全量 745 测试 + ruff + pyright + 宪法校验全绿。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

- outlines 表缺 (workspace_id, version) 唯一约束 — src/novel_editorial/store/models.py Outline + migrations/versions/d3c2b1a09f8e_add_outlines_table.py
  - 问题：create/revise_outline 是「读最新版本→插入 version+1」的独立事务，无 DB 层守卫；两个并发修订可插入相同版本号，get_outline / list_outline_versions 出现歧义与重复行。项目其他版本表（draft_versions / setting_versions）都有唯一约束。
  - 修法：模型加 UniqueConstraint("workspace_id", "version")，迁移镜像同一约束（含中断首跑场景的单独确保）。
- structure move --order 不带 --parent 时静默移到根 — src/novel_editorial/cli/structure.py structure_move
  - 问题：parent_id = None if root or parent is None else parent，而 move_node 把 parent_id=None 当根级；仅传 --order（本意同级重排）会把节点静默挪到根。审查员已实测复现。
  - 修法：仅传 --order 时先查节点当前 parent_id 并保持同级重排；--root / --parent / 互斥规则与输出不变。

#### Minor（可后补）

无。

### 建议

两条修复合并进一个修复包；先写失败测试再改，全量回归。

### 结论

Ready to merge: With fixes

### 处理记录

- 2026-08-20：P2 ×2 均已修复（commit 6faf3c2，先写失败测试再改，全量 750 测试绿），修复包归档于 20260820-M5N13J2-fix.md。
