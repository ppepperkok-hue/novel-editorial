# 2026-08-14 M3-B2（U26 穿透查询）修复复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit ceae3a9（修复 0e6ed4e 审查意见）
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m3-implementation.md 子阶段 B/B2

### 优点

- `search_all_layers` 空值检查后统一 `keyword = keyword.strip()`，`needle` 与 `_snippet` 使用同一去空白关键词，带首尾空格查询不再漏匹配。
- 命中辅助字段（消息/意见 actor、笔记 owner、草稿标题、线索 kind/status、决策 action/actor）时输出行带对应字段前缀，查询词可见；`_snippet` 对内容无命中已有开头截取兜底，无崩溃或输出错乱路径。
- 新增测试覆盖去空白关键词与各层辅助字段命中；改动收敛在 views.py 与 test_inspect.py，未动契约、表结构、迁移。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

无。

### 建议

无。

### 结论

Ready to merge: Yes

### 处理记录

- 2026-08-14 终审通过；回归验证：190 passed、ruff 干净、pyright 0 errors、宪法 OK。
- M3-B2（U26 穿透查询）归档完成，B2 标记完成。
