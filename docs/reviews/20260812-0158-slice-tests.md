审查范围：tests/（62 文件）、run_tests.py、ai_words.json、compliance_words.txt、.env.example，及其契约方 quality_gate/compliance/config/pipeline/editorial_steps 等。基线：python run_tests.py 472 用例全绿（exit 0，62 文件全部被 discover 覆盖，import 错误会 fail 而非假绿）；webapp npm test 7 文件/16 用例通过；npm run build 成功（仅 chunk 体积警告）。无 P0/P1：未发现安全、并发、静默失败或硬编码凭据问题。P2 一项（ai_words.json 重叠词在 quality_gate 双倍计数，docs 声称已修复但实际未修，影响回退/演示路径的质量门评分）；P3 四项（测试 stdout 污染、.env.example 重复键、MONTHLY_BUDGET 死配置、数据文件无真实文件测试守卫）。均为非阻塞问题，基线全绿，patch 判定为正确。

Full review comments:

- ai_words.json 重叠词在 quality_gate 中双倍计数，声称已修复但未修复 — E:\code\novel-editorial\ai_words.json:3-6
  ai_words.json:3-6 含重叠条目（'微微一' vs '微微一愣'、'缓缓' vs '缓缓说道'），而 novel_editorial/quality_gate.py:50 的 ai_flavor_density 逐词 `re.findall` 求和：实测 '她微微一愣。'（5 个中文字）→ density=400.00（2 hits），'她微微一愣，缓缓说道。' → 444.44（4 hits/9 字）。提交 2aae15c 与 docs/planning/20260812-slice-p1-fixes.md（P3-15）声称修复了 'ai-word overlap'，但该提交只改了 tools/ai_taste_check.py 的 FLOWERY/FILLER 词表（另一套词），quality_gate.py 未动。现役发布路径 tools/editorial_steps.py:359 用 `|` 交替正则（非重叠、正确），两处文档标称同源（docs/evolution.md:427）的质量门在同一文本上计数不一致。影响：pipeline.py:119/192（generate_one_chapter/run_demo，经 novel_flow.py/autopilot.py 回退路径可达）的 style_score 被低估、passed 判定偏移，短章或重复短语时更明显；tests/test_quality_gate.py 用例未覆盖重叠词场景。

- 测试直接调用 main() 泄漏脚本 stdout，污染测试输出 — E:\code\novel-editorial\tests\test_publish_stock.py:43-48
  tests/test_publish_stock.py:48 与 tests/test_render_workflow.py:63 直接调用 publish_stock.main()/render_workflow.main() 且未 patch builtins.print（对比 tests/test_services_regression.py:44 的正确做法），导致 tools/publish_stock.py:369 与 tools/render_workflow.py:184 的 print 泄漏。基线输出实测可见 `{"ok": true, "target": 2, "published": 1, ...}` 和 `rendered; changed nodes: ['写手A']` 混入 472 个用例的 OK 流中，掩盖 CI 警告与失败信息。

- .env.example 中 COST_PRO_PER_1K/COST_FLASH_PER_1K 重复定义 — E:\code\novel-editorial\.env.example:53-54
  .env.example:34-35 与 :53-54 两处重复定义相同的成本单价键，第二处（:53-54）还缺少注释。消费方 novel_editorial/llm_client.py:40-41 与 tools/record_work.py:360-364 只读环境变量值，重复定义无功能影响，但用户按示例复制时容易混淆哪一份生效（后设置的进程环境覆盖 .env 值）。应删除其中一处。

- .env.example 的 MONTHLY_BUDGET 无任何消费方（死配置） — E:\code\novel-editorial\.env.example:24-24
  .env.example:24 注释声称 '超限触发监控告警'，但全仓库（.py/.mjs/.ps1/.json）无任何代码读取环境变量 MONTHLY_BUDGET：novel_editorial/monitor.py:73 使用 CLI 参数 `--budget`（默认 100.0），tools/editorial_daily.py:480 从 settings 表读 monthly_budget（tools/app_settings.py:19 默认 '100'）。用户在 .env 里改 MONTHLY_BUDGET 不会生效，属于误导性死配置，应删除或接入 monitor.py 的默认值。

- 真实 ai_words.json/compliance_words.txt 无测试守卫，损坏时静默回退且行为不一致 — E:\code\novel-editorial\tests\test_compliance.py:28-35
  tests/test_compliance.py:28-35 只用 mock.patch 临时文件测试合并逻辑，没有任何测试读取仓库根的真实数据文件。若 ai_words.json 损坏：novel_editorial/quality_gate.py:18-24 捕获 (OSError, ValueError) 静默回退 15 词内置表，而 tools/editorial_steps.py:321-329 回退 21 词表——两处回退表不同且无告警，质量门行为在无任何测试失败的情况下改变；且 quality_gate 对 `ai_flavor` 非 list 值（如字符串）无 isinstance 校验，会按字符遍历计数。compliance.py:_load_words 对损坏的 compliance_words.txt 无捕获，UnicodeDecodeError 会直接穿透 compliance.check() 使发布前合规扫描崩溃。建议增加读取真实数据文件的测试（有效解析 + 词表非空 + 损坏回退）。
