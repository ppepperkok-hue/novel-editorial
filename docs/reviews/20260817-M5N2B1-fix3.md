# 2026-08-17 M5-N2-B1-FIX3 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit e07f6c1
- 审查依据：AGENTS.md / rules.md / docs/implementation/m5-n2-implementation.md

### 优点

- kind/rule 改为 SQLite json_extract 等值比较加 `.first()` 早停，与 JSON 序列化格式无关，紧凑 JSON 已补测试。333 测试全绿。

### 问题

#### Critical（必须修）

- [P1] 称 `json_extract` 对字符串路径返回带引号文本、等值比较在标准构建上不命中——经总监查证为误报：SQLite 官方文档明确 json_extract 返回去引号文本，且本地 3.45.1 实测 `= 'refusal'` 命中；SQLite 不存在 `json_unquote`。不改代码，记录在案。

#### Important（应该修）

- [P2] `json_valid` 防护依赖 WHERE 谓词从左到右短路；SQL 不保证求值顺序，json_extract 遇 malformed JSON 抛 OperationalError，planner 重排时会让 talk send 抛系统异常。修法：用 CASE WHEN json_valid(...) THEN json_extract(...) END 使条件求值与顺序无关。

### 结论

Ready to merge: With fixes

### 处理记录

- P1 误报核实依据：SQLite json1 文档 + 本地实测（3.45.1，`json_extract(..., '$.kind') = 'refusal'` 返回 1）；未改动。
- P2 已核实为真，修复 commit f70c46b，复审见 20260817-M5N2B1-fix4.md。
