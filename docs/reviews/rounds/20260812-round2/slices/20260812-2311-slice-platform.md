审查范围：scripts/*、tools/publish_stock.py、create_book.py、check_stock.py、get_meta.py、record_work.py、delete_book.py、collect_reader_stats.py、current_book.py、preflight.py、release_lock.py、n8n_api.py、pyproject.toml、launch_desktop.vbs，依赖接口（config/db/services）仅作契约核对。基线：切片文件 compileall 全部通过；定向测试 42 passed（publish_stock/create_book/delete_book/record_work/preflight_guard/book_isolation/feedback）；demo.db 冒烟正常，publish_batch 完结路径、delete_book 清理、create_book 超时路径均已复现验证。存在 1 个 P1（get_meta.py 硬编码 ROOT 路径在桌面部署下静默丢失读者反馈/热点）、2 个 P2（create_book 超时漏捕获破坏幂等、delete_book 回复链孤儿）与 2 个 P3，P1 为确定性的静默数据缺失，故判定为不正确。

Full review comments:

- [P1] get_meta.py 硬编码 ROOT 运行时路径，NOVEL_DATA_DIR 下与写入方分裂导致静默丢数据 — E:\code\novel-editorial\tools\get_meta.py:82-84
  tools/get_meta.py 读取 `ROOT / "hot_topics.json"`（L82）、`ROOT / "demo_data" / "reader_stats.csv"`（L211）并 6 处向 `ROOT / "alerts.log"` 追加（L31/44/89/176/230），而写入方全部走 config 常量：collect_reader_stats.py:23/35 用 `config.READER_CSV`/`config.ALERTS_LOG`，services/control.py:111/252 写 `config.HOT_TOPICS_JSON`，web_api/services/misc.py:24-37 读 `config.ALERTS_LOG`/`config.READER_CSV`/`config.HOT_TOPICS_JSON`。config.py:9 `RUNTIME_ROOT = NOVEL_DATA_DIR or ROOT`，desktop/main.js:111 在打包桌面版总是注入 NOVEL_DATA_DIR；已实测：设置 NOVEL_DATA_DIR 后 config 解析到数据目录，get_meta 仍读 E:\code\novel-editorial\hot_topics.json（该文件无人再写）。后果：桌面部署下 LLM 上下文静默丢失读者反馈与热点主题、告警日志分裂到两个文件，且不报错。create_book.py:116 的 `_warn` 同样写 ROOT/alerts.log。应改用 config.HOT_TOPICS_JSON / config.READER_CSV / config.ALERTS_LOG。

- [P2] create_book.py 异常元组漏掉 OSError/socket.timeout，建书超时崩溃且不写 pending 痕迹 — E:\code\novel-editorial\tools\create_book.py:495-503
  create_book_on_fanqie 对建书 POST 的兜底 `except (urllib.error.URLError, urllib.error.HTTPError, RuntimeError, ValueError)`（L497，另 L378/L441/L492 同款）不包含 OSError；`urllib.request.urlopen(timeout=30)` 超时抛 socket.timeout（TimeoutError/OSError 子类，非 URLError）。已复现：mock http_json 抛 socket.timeout 时异常直接穿透 create_book_on_fanqie（main() 无 try/except，traceback 退出），且 `_write_pending` 从未执行，`n8n_tmp/create_book_pending_*.json` 未生成。这恰好破坏 L435-437 注释所设计的幂等机制：create POST 已成功但响应超时后，重跑工具找不到 pending 痕迹，会再次 POST 建书，造成番茄重复建书或触发“每天最多 1 本”限制。建议把 `OSError`（覆盖 socket.timeout）加入这些 except 元组。

- [P2] delete_book._purge_novel 回复链清理被 ref_chapter_id 删除提前破坏，留下孤儿消息 — E:\code\novel-editorial\tools\delete_book.py:81-86
  _purge_novel 中按 ref_chapter_id 删除的循环（L65-72）先于回复链清理（L77-99）执行，而链式清理的种子只从 `WHERE ref_novel_id=?` 取（L81-86）。当父消息同时带 ref_novel_id 和 ref_chapter_id（章节相关消息的典型形态）时，父消息先被 L67-72 删掉，种子集合变空，回复链遍历失效。已复现：父消息 ref_novel_id=1+ref_chapter_id=1、回复消息 ref_novel_id=0/ref_chapter_id=0/reply_to=父，purge 后 agent_messages 仍残留 1 行孤儿（仅带 ref_novel_id 的父消息场景才能正确清理）。与 L73-76 注释声称的行为不符，孤儿行会永久留在 outbox 查询里。修复方向：把回复链遍历移到 ref_chapter_id 删除之前，或种子查询同时包含 ref_chapter_id 命中本作品章节的消息。

- [P3] collect_reader_stats.load_env 未剥离行内注释，与 config/preflight 的 env 解析不一致 — E:\code\novel-editorial\tools\collect_reader_stats.py:48-51
  collect_reader_stats.py:48-51 的 load_env 用 `v.strip()` 保留整行值，而 config.load_env（config.py:100-113，`_strip_inline_comment`）和 preflight.load_env（preflight.py:44-57）都会剥离空白前置的 `#` 注释。若 ~/.n8n/.env 中出现 `FANQIE_COOKIE=xxx # 备注` 这类行（.env.example 风格允许），本工具会把 `# 备注` 拼进 Cookie 导致番茄鉴权失败，而同一文件被其它工具读取时行为不同。建议复用 config._strip_inline_comment 保持解析一致。

- [P3] 运行锁目录在 NOVEL_DATA_DIR 下与调度器分裂，并发互斥与 release_lock 失效 — E:\code\novel-editorial\tools\publish_stock.py:378-378
  本切片内 publish_stock.py:378、preflight.py:276、release_lock.py:60 统一使用 `config.TMP_DIR`（= RUNTIME_ROOT/n8n_tmp，随 NOVEL_DATA_DIR 迁移），但实际持有锁的调度器 tools/editorial_daily.py:562 与 tools/workday.py:194/293/340 仍硬编码 `ROOT / "n8n_tmp"`。当操作者环境设置 NOVEL_DATA_DIR（打包桌面版由 desktop/main.js:111 注入，control.py:157 在 web_api 进程内跑调度器）时，手动 publish_stock CLI 与调度器各持一把锁，防重复发布互斥失效，且 release_lock 无法清理调度器遗留的 ROOT 锁。该问题在 R12 已记录（editorial_daily/control 在切片外，未随本切片修复），此处仅确认切片侧契约仍不一致。
