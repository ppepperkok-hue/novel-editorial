# 2026-08-21 N25 导入导出与备份 S2 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 0413e45（feat(archive): add works export-import CLI with crafted-archive hardening (N25 S2)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n25-implementation.md / src/novel_editorial/core/archive.py / src/novel_editorial/cli/works.py / tests/test_archive.py / tests/test_works_archive_cli.py

### 优点

- S1 审查两个 P3 均已修复（PRAGMA 标识符转义、SQLite 真实性预检），CLI `works export` / `works import` 端到端全链可用，退出码映射正确。
- 38 条相关测试、smoke_m3 SMOKE OK、stress_m3 STRESS OK、ruff / pyright 全绿；真实 CLI 搬家冒烟通过。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

- [P2] magic 前缀垃圾 data.db 仍漏出原始 DatabaseError — src/novel_editorial/core/archive.py:52-53
  预检在文件头 magic 命中后提前返回 True，`PRAGMA schema_version` 对 magic 前缀垃圾也成功；随后 run_migrations 抛原始 sqlite3.DatabaseError，CLI 以 Unexpected error（退出码 3）呈现，违背「不冒原始 DatabaseError、校验失败退出码 2」的契约（实测复现）。修法：命中 magic 后必须真实读 schema（如 `SELECT count(*) FROM sqlite_master`）成功才算有效。

#### Minor（可后补）

无。

### 建议

- SQLite 真实性判定以「能真实读 schema」为唯一标准，magic 只作非空文件的必要前置提示。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：`SQLite format 3\0` + 垃圾的 data.db 经 CLI 导入退出码 3。
- 修复：随 S3 提交 1908a11 落地——探针改为真实读 sqlite_master；补 magic 前缀垃圾测试（core 干净拒绝、CLI 退出码 2、三处无残留）；全量 1074 passed、smoke_m3 / stress_m3 全绿。
