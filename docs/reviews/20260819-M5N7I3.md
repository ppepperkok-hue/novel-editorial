# 2026-08-19 N7 语义记忆检索 I3 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit bd7e2e1（docs: add N7 semantic memory retrieval usage section）
- 审查依据：AGENTS.md / docs/architecture/rules.md / extension.md / docs/implementation/m5-n7-implementation.md / docs/usage.md 全文与既有章节风格

### 优点

- usage.md 新增「语义记忆检索（N7）」节：两档后端能力边界、四项配置（NOVEL_EMBEDDING_* 与 config.toml 同名键、优先级、校验范围）、--semantic 行为与行格式、reindex 幂等、降级语义与红线三句，全部与实现逐字一致。
- 示例不是抄的：用临时数据目录真实跑通「works create → 两条无共同双字词的笔记 → 字面 no matches → --semantic 命中 0.51/0.44 → reindex 幂等 → 重建后结果不变」，并把真实输出写进文档。
- 交叉验证通过：config 默认值/校验、cli 的 --semantic 与 reindex 接线、semantic_search 的回读过滤与 exclude_literal、search_memory 关键词层输出，审查员逐一对照源码并用真实 CLI 复跑确认。
- 额外实测了降级分支：空索引静默 no matches、api 后端不可用时 stdout no matches 且退出码 0、stderr 有 warning；文档按实测把「索引空（无告警）」与「嵌入失败（告警）」分开描述，如实。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

无。

### 建议

无需修复，I3 可直接收口。

### 结论

Ready to merge

### 处理记录

- 2026-08-20：无修复意见；实施文档状态与 N7 收口由总监更新（commit 见 git log）。
