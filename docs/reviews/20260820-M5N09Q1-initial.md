# 2026-08-20 N9 质量门语料校准 Q1 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit e59d02a（feat(calibration): add corpus scan and score distribution service (N9 Q1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n09-implementation.md / src/novel_editorial/core/calibration.py / tests/test_calibration.py

### 优点

- scan_corpus 与 Q1 计划一致：目录递归/单文件、隐藏与空文件跳过、每样本 check_quality 各维度、按路径排序、确定性 nearest-rank 分布、suggested_threshold = max(1, ceil(p90))。
- 错误码复用既有枚举（NOT_FOUND / USAGE_ERROR）并写明理由；单文件读取失败降级为 skipped + errors 暴露，不整体失败。
- 目标 19 条测试全绿；全量 849 passed；ruff / pyright / 宪法校验全绿。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- `src/novel_editorial/core/calibration.py:33`：UTF-8 BOM（Windows Notepad 常见）未剥离，char_count 多算 1、纯 BOM 文件被当有效样本。修法：`encoding="utf-8-sig"`。

### 建议

修复 BOM 后收口。

### 结论

Ready to merge: With fixes

### 处理记录

- 2026-08-20：派 M5-N09-Q1FIX，commit 3c2060e 修复；全量 852 passed（独立审查员复核）。
