# 2026-08-19 N5 作品设定库 G3 收口记录

- 范围：docs/usage.md 新增「作品设定库（N5）」章节（commit ee2cbfd）。
- 审查方式：总监复核（纯文档提交，按预算纪律不派独立审查 CLI；文档示例逐条实跑作为验收证据）。

### 内容

- 五条命令（setting add / list / show / revise / history）的参数、输出与退出码语义；
- kind 四类标签与版本语义（v1 起、修订递增、原因与操作者必填、来源可溯）；
- 检索 [设定] 层（memory search / inspect 一致，带来源与版本号）；
- 红线：沉淀不是前置、只记录不改写正文；
- mock 下实跑示例：建作品 → setting add×2 → list → show → revise → history → memory search → inspect。

### 验证

- 总监独立实跑示例：每步输出与文档逐字一致（仅 id 随运行变化）。
- 员工全量回归：`uv run pytest -q` 586 passed；ruff All checks passed；pyright 0 errors；宪法 [OK]；smoke_m3 SMOKE OK；stress_m3 STRESS OK。

### 结论

G3 完成，N5 整体收口。
