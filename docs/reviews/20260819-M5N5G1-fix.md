# 2026-08-19 N5 G1 审查意见修复记录

- 修复来源：20260819-M5N5G1-initial.md 的 Minor（revise_setting 未校验 actor）。
- 修复方式：独立 CLI（flash + high），先写失败测试再改。
- 修复范围：src/novel_editorial/core/setting.py、tests/test_setting.py。

### 改动

- revise_setting 在 content/reason 校验后新增 actor 非空校验（USAGE_ERROR，"setting actor must not be empty"）。
- 新增参数化失败测试（actor="" / "   "）：断言抛错、内容与版本号不变、历史只有 v1。

### 验证

- 修复前：blank_actor 用例 2 failed（漏洞复现）。
- `uv run pytest -q tests/test_setting.py`：36 passed。
- `uv run ruff check .`：All checks passed；`uv run pyright`：0 errors。
- 全量 `uv run pytest -q`：561 passed。

### 结论

审查意见处理完毕，G1 收口（commit 2f0974b）。add_setting source 校验并入 G2。
