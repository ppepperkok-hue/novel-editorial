验证基线全绿：`python run_tests.py` 497 个用例全部通过（exit 0，系统 Python 3.11、无需第三方依赖，与文件头「仅标准库」声明一致），`webapp npm test`（vitest run）8 个文件 20 个用例全部通过（exit 0）。范围内未发现 P0-P2 缺陷：ai_flavor 词表四处副本（ai_words.json / quality_gate.py / editorial_steps.py / n8n workflow）当前完全一致，.env.example 与全部活跃代码读取的 env 键一一对应且无硬编码密钥或路径，测试无 fake-green（无 assertTrue(True)、无空断言、全部用临时库/本地 HTTP 服务/ mock）。仅 2 项 P3 级缺测试/测试与设计矛盾类问题，不阻塞合并。

Full review comments:

- 为 ai_flavor 词表四处副本补同步守卫测试 — E:/code/novel-editorial/tests/test_quality_gate.py:55-60
  ai_words.json 的 `_note` 声明它是唯一权威来源，且 novel_editorial/quality_gate.py:15-21、tools/editorial_steps.py:332-337、n8n/novel_workflow.json:239/371 各带一份注明「禁止单独扩充，避免两处词表漂移」的回退副本。我程序化核对了当前状态：四处均为 21 词且完全一致，无现存缺陷；但 tests/test_quality_gate.py:55-60 的 `test_real_ai_words_file_is_valid` 只校验 JSON 形状（`data["ai_flavor"]` 是 list 且非空），没有任何断言比较 ai_words.json 与三份回退副本。按文件注释「修改词表请只改本文件」改词时若漏改任一回退副本（或反向只改回退副本），漂移不会在任何测试中暴露，导致 n8n 流程与本地质量门的 AI 味判定静默不一致。建议在 test_quality_gate.py 增加一个断言 `AI_FLAVOR_WORDS == _DEFAULT_AI_FLAVOR_WORDS == json.loads(ai_words.json)["ai_flavor"]`（并可选校验 workflow 内硬编码列表），把文档化不变式变成可执行检查。

- 移除 compliance 词表非空断言以兼容裁剪后的空词表 — E:/code/novel-editorial/tests/test_quality_gate.py:63-66
  tests/test_quality_gate.py:63-66 的 `test_real_compliance_words_file_has_at_least_one_word` 断言仓库根 compliance_words.txt 至少含一个词，但 compliance.py:16、114-115 专门设计了空/全注释词表的降级路径（EMPTY_WORDS_WARNING，且 tests/test_compliance.py:42-57 已用临时文件覆盖该行为），文件头也写明「正式启用前请按目标平台的审核规则补充与裁剪」「可按平台规则增删」。当运营按文件自身说明把示例词裁到只剩注释时，`compliance.check` 会带 RuntimeWarning 正常降级，而测试套件会失败——测试与设计行为直接矛盾。建议把该断言放宽为仅校验 `_read_custom_words()` 返回 list（允许为空），与已有的 EMPTY_WORDS_WARNING 单元测试保持一致。
