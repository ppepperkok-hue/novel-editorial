# 2026-08-21 N21 风格漂移检测 S1 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 1f34921（feat(style): add cross-chapter style drift detection (N21 S1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n21-implementation.md / src/novel_editorial/core/style_drift.py / tests/test_style_drift.py

### 优点

- 核心漂移分逻辑与实施文档 S1 完全一致：五维偏差公式、阈值 50、首章基线、基线章恒不漂移、无关键词时维度剔除重归一、禁忌词只报告不算分。
- 只读语义扎实：缺风格锚点直接按空处理、不新建行；不落事件；确定性可复现。
- 测试 13 条覆盖排序、边界 49/50、空态、确定性、只读、NOT_FOUND；ruff / pyright 全绿。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- [P3] 结构挂章的悬空 draft_id 会中断整份报告 — src/novel_editorial/core/style_drift.py:171-181
  `_ordered_chapters` 对每个挂章直接 `get_draft`，草稿不存在时 NOT_FOUND 冒泡，单个坏节点导致整份漂移报告失败。`WorkspaceStructureNode.draft_id` 无外键约束，`structure add --draft` 也不校验归属，实际可达。影响：一个悬空挂章让全书趋势不可见。修法：跳过无法解析的挂章并计入 skipped（或收集错误进报告）。

### 建议

- 悬空挂章按「未分析章节」处理，与空正文统一进 `skipped`，避免两种跳过语义分叉。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：悬空 draft_id 复现 NOT_FOUND 中断（审查 CLI 实测）。
- 修复：并入 S2 提交 7d8e4db——`_ordered_chapters` 捕获 NOT_FOUND 跳过挂章并计入 skipped，新增核心与 CLI 用例；回归全绿。
