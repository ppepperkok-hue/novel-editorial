基线全绿：python run_tests.py 481/481 通过（exit 0，102s），webapp vitest 7 文件/16 用例通过；fake-green 守卫实证有效（坏模块→error 用例 exit 1，空目录→countTestCases()==0 exit 1）；ai_words.json 与 quality_gate/editorial_steps/n8n 内联四处词表一致（21 词）。6 项发现均为 P2/P3 配置文档卫生与防御性缺口，不影响现有测试与功能正确性。

Full review comments:

- [P2] .env.example 行内注释导致配置静默回退失效 — E:/code/novel-editorial/.env.example:71-72
  `.env.example` 的 `COST_PRO_PER_1K=0.01             # pro 模型单价` 等 11 个键带行内注释，而 README.md:152 指导用户把该文件整体复制为 `~/.n8n/.env`；`config.load_env()`（novel_editorial/config.py:87-93）只做 `v.strip()`，不剥离 `#` 注释。实证：`float('0.01             # pro 模型单价（元/千 token）')` 抛 ValueError，于是 llm_client.estimate_cost（llm_client.py:40-44）、tools/record_work.py:369-379、meeting_session.py:87-92 全部静默回退默认单价/60 分钟，用户自定义数值被悄悄忽略且无任何告警。DISPATCH_MODE=editorial（.env.example:24）若经加载该 env 文件的进程传播，`tools/editorial_daily.py:574` 的 `config.DISPATCH_MODE != "editorial"` 精确比较会把默认 editorial 判成 fixed。建议把说明移到独立 `#` 注释行。

- [P3] compliance_words.txt 全注释，发布扫描每次触发 EMPTY 警告 — E:/code/novel-editorial/compliance_words.txt:1-3
  compliance_words.txt 仅含 3 行注释、0 个自定义词；实测 `compliance.check()`（novel_editorial/compliance.py:86-95）每次返回非空 `warnings` 且发出 EMPTY_WORDS_WARNING RuntimeWarning。该路径是活路径：pipeline.py:126/198 与 tools/editorial_daily.py:1188 每次发布前合规扫描都会走到。发布不会因此失败，但警告噪音持续存在，且真实用户自定义词从未被启用；应填词或明确标注该文件处于占位状态。

- [P3] quality_gate 对 ai_words.json 缺失/损坏静默回退无告警 — E:/code/novel-editorial/novel_editorial/quality_gate.py:33-34
  novel_editorial/quality_gate.py:33-34 `except (OSError, ValueError): pass` 在 ai_words.json 缺失或 JSON 损坏时静默回退内置 21 词表，无 warnings.warn；与 compliance.py 的 MISSING/READ 告警（compliance.py:42-45/79-81）容错不对称。若文件被误删或坏编码，质量门行为变化时不会有任何提示，且 tests/test_quality_gate.py 没有真实文件守卫用例，测试仍全绿。

- [P3] test_quality_gate 未钉住重叠词非重叠计数语义 — E:/code/novel-editorial/tests/test_quality_gate.py:14-16
  tests/test_quality_gate.py:14-16 只断言 `density > 0`，没有覆盖 ai_words.json 中重叠条目（'微微一' vs '微微一愣'、'缓缓' vs '缓缓说道'）的按位置计一次语义。当前实现（合并正则）实测正确：`ai_flavor_density('她微微一愣。')=200.0` 单次命中、`'她微微一愣，缓缓说道。'=222.2` 两次命中，但该语义无测试防回归，未来若改回逐词 findall 会静默双倍计数（round4 曾出现该缺陷）。

- [P3] .env.example 与 config.py 硬编码真实 n8n 工作流 ID — E:/code/novel-editorial/.env.example:54-56
  .env.example:54-56 与 novel_editorial/config.py:31-33 将 `N8N_WORKFLOW_DAILY=SkLUnm3uRyBSY84F` 等真实实例 ID 作为默认值，而非占位符（如 YOUR_WORKFLOW_ID）。模板语义应为示例，新用户照抄后会在本地 n8n 中调用不存在的工作流（404），且注释声称「换成你导入后的实际 ID」与示例值自相矛盾。

- [P3] run_tests.py 不收集 *_test.py 命名测试 — E:/code/novel-editorial/run_tests.py:8-11
  run_tests.py:8 的 `unittest.TestLoader().discover()` 使用默认 pattern `test*.py`，与 pytest 默认收集规则（test_*.py 与 *_test.py）不一致；未来若按 pytest 惯例新增 `foo_test.py` 会被静默漏掉（countTestCases 不为 0，假绿守卫不触发）。当前 62 文件/481 用例已核对全部被收集，无现役影响，但守卫存在盲区。
