# 2026-08-19 N6 知识管家 H3 收口记录

- 范围：docs/usage.md 新增「知识管家（N6）」章节（commit 8166264）。
- 审查方式：总监复核（纯文档提交，按预算纪律不派独立审查 CLI；文档示例逐条实跑作为验收证据）。

### 内容

- 分发语义：写手记忆包与编辑视图自动带「设定：」段（永远当前版本、kind 固定序、无设定无段）；检索 [设定] 层与分发同源。
- 修订事件留痕：setting revise 后 events list 可见 [system] setting_revised（payload 结构完整、超长截断说明）。
- setting check：统计行、陈旧（已修订）列表、同名冲突候选、空态单行、退出码语义。
- 红线：沉淀不是前置、只报告不改写、分发用当前版本。
- mock 实跑示例：建作品 → 同名两条设定 → revise → memory pack / editor view / events / setting check / search，另含空态与退出码验证。

### 验证

- 总监独立实跑示例：每步输出与文档逐字一致（仅 id / 时间戳随运行变化）。
- 员工全量回归：`uv run pytest -q` 608 passed；ruff All checks passed；pyright 0 errors；宪法 [OK]；smoke_m3 SMOKE OK；stress_m3 STRESS OK。

### 结论

H3 完成，N6 整体收口。
