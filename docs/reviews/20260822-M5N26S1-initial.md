# 2026-08-22 N26 预设编辑部模板 S1 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit fd77ed8（feat(templates): add preset band templates and template-aware workspace creation (N26 S1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n26-implementation.md / src/novel_editorial/core/templates.py / src/novel_editorial/core/workspace.py / src/novel_editorial/store/db.py / tests/test_templates.py

### 优点

- 三个模板（网文 / 同人 / 正统）班子四角色齐全、十二项字段完整、风格描述非空；`seed_band` 泛化保持默认路径行为零变化（委托实现）。
- `create_workspace(template=...)` 班子与锚点落库正确，不带 template 与现状一致；24 条相关测试、ruff / pyright 全绿；全量 1142 测试通过。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- [P3] `list_templates` 独立 `_ORDER` 与 TEMPLATES 有漂移风险 — src/novel_editorial/core/templates.py:216-222
  新增模板漏更 `_ORDER` 会 KeyError 或列出不全。修法：顺序直接从 TEMPLATES（字典插入序）推导。
- [P3] 冻结 dataclass 仍暴露共享可变 band — src/novel_editorial/core/templates.py:11-18
  `get_template` 返回共享模块级对象，调用方改 band 内 dict 会污染后续创建，确定性保证失效。修法：`get_template` 返回 deepcopy。

### 建议

- 模板常量的确定性契约要真实生效：读取即副本、顺序单一来源。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：两个 P3 均可通过调用方改动复现。
- 修复：随 S2 提交 7c21f9b 落地——`list_templates` 改从 TEMPLATES 推导、`get_template` 返回 deepcopy，补污染隔离测试；回归全绿。
