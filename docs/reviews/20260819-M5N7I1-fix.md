# 2026-08-19 N7 I1 审查意见修复记录

- 修复来源：20260819-M5N7I1-initial.md 的 P2 + P3。
- 修复方式：独立 CLI（flash + high），先写失败测试再改。
- 修复范围：src/novel_editorial/llm/embeddings.py、src/novel_editorial/core/memory.py、tests/test_embeddings.py、tests/test_memory.py、docs/implementation/m5-n7-implementation.md。

### 改动

- build_embedding_client：backend=api 且 embedding_model 为空抛 NovelError(CONFIG_ERROR)（不再回退 llm_model）；OpenAICompatEmbedder 构造失败（含缺凭据）统一包 NovelError(LLM_ERROR)。
- delete_memory_note：无条件先执行 delete_embedding_safe（笔记在不在都清索引），最后按是否找到决定抛 NOT_FOUND；重试可清掉孤儿索引行。
- 新增失败测试三条：api 缺模型 CONFIG_ERROR 且不构造客户端、构造失败包装 LLM_ERROR、孤儿索引重试清理。

### 验证

- 子集 `uv run pytest -q tests/test_embeddings.py tests/test_memory.py tests/test_retrieval.py`：113 passed。
- `uv run ruff check .`：All checks passed；`uv run pyright`：0 errors。
- 全量 `uv run pytest -q`：653 passed。

### 结论

审查意见处理完毕，I1 收口（commit 85cfb01）。
