六项都修完了mashitawa。逐个说：hot_topics.py 第 288 到 307 行，临时文件改成 mkstemp 唯一名再 os.replace，Windows 并发 replace 的瞬态 PermissionError 加了五次退避重试，finally 里清理残留，四线程各 100 次并发写同一个目标，零报错、JSON 完好、无 .tmp 残留desuwa。misc.py 第 114 到 150 行，topics 解析失败回退空列表，report 解析失败也补了 alerts.log 留痕，坏数据行不再让 /api/meetings 整页 500，手工复现通过desuno。knowledge.py 第 82 到 93 行，frontmatter 校验从三个字段扩到 type 和所有写入字段，换行与控制字符一律拒绝，type 注入、agents 元素带控制字符都被 ValueError 拦下，正常写入不受影响mashitawa。web_api.py 第 916 到 919 行，save 的 ValueError 就地捕获，配合第 680 行和第 1036 行的 http_status 机制返回 400 带错误信息，路径穿越现在正确落到 400 而不是 500desuwa。ending.py 第 47 行，book_id 含换行或控制字符直接拒绝，env 文件一个字都不写，复现里注入行没进 .env、DB 状态也没动desuno。meeting_session.py 第 303 到 320 行，outbox 单条容错，chapter_id/reply_to 非整数只跳过那一条并写 audit outbox_skip，其余消息照常外发，复现里坏消息被跳、好消息完整入库mashitawa。

验证方面，六个文件 compileall 全过；相关 pytest 跑了十一个文件，127 过 1 败。那条失败是 tests/test_web_api.py 的 test_knowledge_save_rejects_path_traversal，还在断言旧的 500，现在正确返回 400——测试文件不在授权清单，我没改，建议把断言改成 400 并顺带校验响应体的错误文案desuwa。

额外问题只报不改：web_api.py 第 960 行附近的 knowledge_drafts accept 分支，write_knowledge 的 ValueError 同样没捕获，仍是 500，和这次修的是同一类病；另外 /api/knowledge、/api/knowledge_drafts、/api/novel_knowledge 三个分支打开的 conn 一直没关，每个请求漏一条 SQLite 连接，建议后续统一收口；还有 test_publish_draft_rejection_is_visible 组合跑时偶发「运行锁占用」失败，单独跑全过，属于测试环境污染，和 L-057 同类desuwa。报告实际文件名是带时间戳的 20260812-0659-slices-summary.md，任务书里的路径又漂了；legacy-tracker 的 L-048 状态更新不在允许修改清单里，我没动。仓库没有 MEMORY.md，本次未更新mashitawa。
