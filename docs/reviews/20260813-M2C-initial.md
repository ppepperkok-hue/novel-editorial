# 2026-08-13 M2-C（U18 私有记忆）初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 23bdcb5
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m2-implementation.md 子阶段 C

### 优点

- `agent_memories` 表、`memory note / notes / delete` 三个命令骨架齐全，测试覆盖了持久化、空态与未知目标。

### 问题

#### Critical

无。

#### Important

1. **P1：权限分层未实现**
   - 文件：src/novel_editorial/core/memory.py、src/novel_editorial/cli/app.py（commit 23bdcb5）
   - 问题：验收标准为“写手笔记仅写手可写、老板可看”，但 `memory note` 无 actor 限制，任何人可写任意伙伴的笔记。
   - 要求：增加 `--as <actor>` 参数（角色别名或“作者”）；角色只能写给 target=自己；作者（老板）只读，不可写入。
   - 影响：私有记忆的隔离承诺不成立，伙伴“有自己的记忆”落空。
   - 修法：core 层校验 actor 与 target 的归属关系，CLI 暴露 `--as`，拒绝时以 USAGE_ERROR（退出码 2）显式失败。

2. **P2：`memory notes` 不显示笔记 ID，delete 无法从 CLI 使用**
   - 文件：src/novel_editorial/cli/app.py（commit 23bdcb5）
   - 问题：`memory notes` 只输出归属与内容，无 ID；`memory delete` 需要 ID，用户无法在 CLI 内完成“列出 → 解析 ID → 删除”闭环。
   - 要求：输出格式 `<id> [伙伴名] <内容>`，并补全链路测试：从 notes 输出解析 ID → delete → notes 不再显示。

#### Minor

无。

### 建议

- 权限校验放 core 层，CLI 只做参数透传，避免依赖方向被破坏。

### 结论

Ready to merge: No（With fixes）

### 处理记录

- 2026-08-13 派修复任务（第 1~3 次独立 CLI）。
- 第 3 次修复后主验收人复核：验证四连全绿、改动范围合规，但语义仍不符——实现允许“作者可写任意伙伴”，与“作者只读”相反；且 actor 以 `[actor]` 前缀写入 content，污染存储与输出格式。判定验收不通过，派第 4 次修复。
- 第 4 次修复（commit 0801c30）通过验收：作者只读、伙伴只能写自己、content 原样入库、notes 带可删 ID；转独立审查。
- 修复审查结论 Ready to merge，2 个 P3（多行 content、退出码不一致）；第 5 次修复（7678365）闭合后复审查，又提出 splitlines 边界 P3；第 6 次修复（faaddda）最终闭合。
- 最终回归：113 passed，ruff / pyright / 宪法全绿。详见 [20260813-M2C-fix.md](20260813-M2C-fix.md)。
