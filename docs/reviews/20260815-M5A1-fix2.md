# 2026-08-15 M5-A1-FIX2 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit ebe32dd
- 审查依据：AGENTS.md / rules.md / docs/implementation/m5-implementation.md

### 优点

- 过滤回到 SQL 侧，select(func.count()) 直接出 COUNT，正文列不参与查询，消除全量物化。
- 五类 kind 白名单或_条件完整保留父提交引入的语义，对当前所有 Message payload 写入方（均经 json.dumps 默认分隔符）行为等价。
- 新增 SQL 形态守卫测试锁定不再物化的回归线；276 测试、ruff、pyright 全绿。

### 问题

#### Minor（可后补）

1. src/novel_editorial/core/proactive.py:115 — SQL 中 initiator 标记硬编码
   问题：SQL 模式硬编码 "agent"，与 build_proactive_payload 写入的 INITIATOR_AGENT 常量解耦；常量一旦变更，计数会静默归零。
   影响：当前值相同无行为差异，属维护隐患。
   修法：改为 f'%"initiator": "{INITIATOR_AGENT}"%'。

### 建议

- 无其他阻塞项。

### 结论

Ready to merge: With fixes

### 处理记录

- 修复包 M5-A1-FIX3 → commit d9c0cc1（refactor: share initiator constant in proactive count SQL）。
- 回归：276 passed（-n auto 13.77s）、ruff、pyright 0 errors、宪法 OK；rg 确认 proactive.py 内无解耦硬编码。
- 全 src 仍有两处历史遗留硬编码（chat.py:18 PROACTIVE_PAYLOAD、draft.py:240 rebuttal payload），属 A1 范围外，留待后续另行处理。
