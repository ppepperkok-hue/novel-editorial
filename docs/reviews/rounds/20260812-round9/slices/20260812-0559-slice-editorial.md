审查范围：tools 下 21 个目标文件（editorial_daily/workday/editorial_steps/editorial_state/flow_graph/export_flow_html/daily_runs/auto_fill_actions/agent_context/agent_meeting/agent_tool_loop/mailroom/relations/promises/meeting_actions/meeting_kinds/meeting_materials/write_diaries/architect_weekly/apply_architect/app_settings），仅按需读取 novel_editorial/config.py、db.py、llm_client.py 及 services/* 验证契约，未扫描全仓。基线：21 个文件 python -m compileall 全部通过；切片相关 pytest 263 项全部通过（test_agent_meeting/test_editorial_daily/test_workday/test_review_retry/test_meeting_session 等）；运行时冒烟：editorial_daily.daily(dry_run=True) 全链跑通且零 DB 写入，workday open/close（dry-run 与非 dry-run）状态机正常，无编码问题（UTF-8 干净）、无硬编码密钥。未发现 P0/P1：发布链路、质量门、锁与幂等设计均有显式失败处理，未发现会阻断发布或破坏数据的问题。确认两处 P2 功能性缺陷（重做失败仍标记行动项 done；dry-run 会议仍写 actions/activity）与一处 P3 诊断噪音，均不影响生产发布路径，故整体判定为正确，但建议按上述 P2 修复后再依赖任务板/承诺证据做决策。

Full review comments:

- 重做链路失败时 _settle_rework 仍把行动项标为已完成 — E:\code\novel-editorial\tools\editorial_daily.py:1265-1271
  tools/editorial_daily.py:1265-1271 在调用 _review_retry 后无条件执行 _settle_rework(conn, track_req)，而 _review_retry 在写手/润色/审稿任一 LLM 失败时（1124-1137 的 break）或重试后质量门仍未通过时，都会返回失败 gate。_settle_rework（1287-1309）随后仍把 message resolve 成 rework，并调用 activity.update_action(..., status="done", result="即时重写已完成")。已用模拟复现：让重试链全部失败后，action 状态为 done、result 为“即时重写已完成”，而 gate 仍为 passed=False。后果：任务板与承诺结算（settle_promises/auto_fill_actions 会把 done 行动项当作完成证据）记录假成功，重做永远不会被再次拾起。建议仅在 gate.get("passed") 为 True 时结算，失败时保留 pending。

- agent_meeting --dry-run 仍会落库 agent_actions 与 activity — E:\code\novel-editorial\tools\agent_meeting.py:736-745
  tools/agent_meeting.py:736-745 把 dry_run=args.dry_run 传给 activity.generate_post_meeting_actions，但该函数（novel_editorial/services/activity.py:323-399）的 dry_run 只跳过 LLM 调用：tasks 为 None 时走规则兜底并无条件 create_action，因此 dry-run 会议会给每个参会者写入“复盘本次会议…”等 6 条 agent_actions，同时 main() 里每条发言的 activity.log_activity（659/670 行）也不受 dry_run 门控。已用全新临时库复现：一次 --dry-run --rounds 1 会议写入 meeting_sessions=1、weekly_meetings=1、agent_actions=6、agent_activity=13，而本仓其他工具（editorial_daily/workday/write_diaries，均有 test_dry_run_has_no_db_side_effects 等测试）的 dry-run 是零写入。这些幻影行动项会进入 agent 简报、上下文快照与 promise 结算证据。注意测试 test_meeting_dry_run_full_chain 明确断言 session/weekly_meetings 落库（属有意），但未覆盖 actions/activity；建议把生成动作也纳入 dry_run 门控。

- settings 缺键时 _safe_int(None) 向 stderr 误报非整数配置 — E:\code\novel-editorial\tools\architect_weekly.py:332-333
  tools/architect_weekly.py:332-333 直接对 settings.get("daily_chapters") 等调用 _safe_int，而 _safe_int（67-76 行）在值为 None 时先 int(None) 抛 TypeError 再打印 "note: 非整数配置，使用默认值"。agent_meeting/meeting_session 流程不调用 app_settings.ensure_defaults，新库/未初始化库每次开会都会向 stderr 刷 3 行误导性告警（已在新临时库复现，一次会议出现 3 条 note）。建议把 _safe_int 对 None/空串直接走默认值、不告警（与 publish_stock._safe_int_setting 一致），或先 ensure_defaults。
