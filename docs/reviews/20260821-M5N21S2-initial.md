# 2026-08-21 N21 风格漂移检测 S2 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 7d8e4db（feat(style): add style drift CLI with dangling-chapter tolerance (N21 S2)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n21-implementation.md / src/novel_editorial/cli/style.py / src/novel_editorial/core/style_drift.py / tests/test_style_drift.py / tests/test_style_drift_cli.py / tests/test_cli_registry.py

### 优点

- CLI 输出与实施文档 S2 示例逐项对齐：逐章行、trend、drifted chapters、forbidden hits、verdict 单复数；空态两分支退出码 0。
- S1 审查 P3（悬空挂章）已修复并有核心 + CLI 双层用例覆盖。
- 全量 991 测试、ruff、pyright 全绿；端到端、确定性、只读断言齐备。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- [P3] n/a 分支吞掉 skipped 提示 — src/novel_editorial/cli/style.py:116-119
  可分析章不足 2 章时提前返回，只输出 `chapters: N` 与 `drift: n/a ...`；若存在悬空/空正文章节（如 1 个有效章 + 1 个悬空挂章），用户看不到被跳过的章，误以为全书只有 1 章。实施文档 S2 契约未限定 skipped 行只在完整报告输出。修法：n/a 早退前也按 `skipped > 0` 输出 `skipped chapters: N`。

### 建议

- skipped 行的输出条件统一为「report.skipped > 0」，与分支无关，保证「未分析章节」永远可见。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：1 有效章 + 1 悬空挂章场景下 n/a 分支无 skipped 提示。
- 修复：列入实施文档 S3 修复包（n/a 分支输出 skipped chapters 行），随 S3 收口回归并回填本行。
