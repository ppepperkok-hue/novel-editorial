审查范围：tools/ 下 21 个文件（workday/editorial_daily/editorial_steps/editorial_state/flow_graph/export_flow_html/daily_runs/auto_fill_actions/agent_context/agent_meeting/agent_tool_loop/mailroom/relations/promises/meeting_* /write_diaries/architect_weekly/apply_architect/app_settings）。Baseline：21 个文件 `py_compile` 全部通过（`python -m compileall` 等价验证，按要求未跑完整测试套件）；对 demo.db 副本做了 40+ 项离线冒烟测试，覆盖 relations/editorial_state/mailroom/promises/app_settings/flow_graph/agent_context/architect_weekly/meeting_materials/meeting_actions/editorial_steps/auto_fill_actions/export_flow_html/daily_runs，其中 relations 冒烟失败并确认根因。结论：存在 1 个 P1 必现 bug——`relations.ensure` 在默认生产库 demo.db（旧 schema 迁移，`other_agent TEXT NOT NULL`）上每次插入都抛 IntegrityError，直接导致日更链路 `_rel()` 崩溃、承诺结算与关系系统全面失效；另有 2 个 P2（LLM outbox 非数字字段拖垮日更、skipped 分支误删 workday 行）和 3 个 P3。核心流水线在默认库上不可用，故判定补丁不正确，建议先修复 P1 再合入。

Full review comments:

- [P1] relations.ensure 在迁移库上必现 NOT NULL IntegrityError，导致日更整体失败 — E:\code\novel-editorial\tools\relations.py:44-49
  `tools/relations.py` 的 `ensure()` 只 INSERT `(agent, other, novel_id, ...)`，但默认生产库 demo.db 的 `agent_relations` 表是旧 schema 迁移而来：`other_agent TEXT NOT NULL`（无默认值，见 `PRAGMA table_info(agent_relations)`），`db.py:_migrate` 只添加了 `other` 列，未处理 `other_agent` 的 NOT NULL 约束。实测 `relations.ensure(conn, "writer", "eic", 1)` 抛 `sqlite3.IntegrityError: NOT NULL constraint failed: agent_relations.other_agent`（表内 0 行，任何新关系创建必现）。影响链：`editorial_daily._run_track` 中 `_rel(...)`（润色→写手 collaboration、审稿→写手 collaboration、feedback_rejected 等）非 dry-run 必调用且无 try，异常沿 `_generate → daily()` 传播，整次日更被标记 failed（demo.db 中 3 条 `status='failed'` 的 workday 记录与此吻合）；`promises.settle_promises` 的 promise_kept/promise_broken 事件全部失败，且 kept UPDATE 未提交即被后续 `log_activity` 的 commit 意外提交，状态半更新。修复需在 `_migrate` 中迁移 `other_agent` 数据并放开 NOT NULL，或让 `ensure` 同时写 `other_agent`。

- [P2] _handle_outbox 对 LLM 输出做无防护 int() 转换，非数字 reply_to 会拖垮整次日更 — E:\code\novel-editorial\tools\editorial_daily.py:97-98
  `tools/editorial_daily.py:_handle_outbox` 中 `reply_to = int(item.get("reply_to") or 0)`（第 97-98 行）与 `chapter_id=int(item.get("chapter_id") or 0)`（第 107 行）直接转换 LLM 生成的字段，无任何容错。实测构造 `{"outbox": [{"to": "eic", "body": "hi", "reply_to": "msg-42"}]}` 抛 `ValueError: invalid literal for int() with base 10: 'msg-42'`。该异常在 `_agent()` 中未被捕获，沿 `_run_track → _generate → daily()` 传播，导致整次日更 run 标记 failed、published=0。LLM 输出不可控，只要某个 agent 的 outbox 出现 `"reply_to": "msg-42"` 或 `"第3条"` 这类非数字值即触发；建议对非数字值降级为 0 并记录 warning（与 `decision` 的 `.strip().lower()` 处理一致）。

- [P2] daily() skipped 分支无条件删除 daily_runs 行，会删掉 workday 创建的工作日记录 — E:\code\novel-editorial\tools\editorial_daily.py:1656-1660
  `tools/editorial_daily.py` 第 1656-1660 行在 preflight 返回 skipped 时执行 `DELETE FROM daily_runs WHERE run_id=?`，没有区分 `workday_run_id`。当 `workday.open(..., trigger="scheduled")` 且设置 `daily_enabled=false` 时，`_preflight` 返回 skipped，该 DELETE 会删掉 workday 刚刚 INSERT 的 daily_runs 行；随后 `workday.open` 仍执行 `_update(..., phase="awaiting_close")`（更新 0 行）并返回 `{"ok": True, "status": "awaiting_close"}`，之后 `workday.close` 报 `run not found`，面板上的工作日凭空消失。当前 web 端调用 trigger 恒为 manual（`novel_editorial/services/control.py`），但 CLI `python tools/workday.py --action open --trigger scheduled` 是文档化入口，可复现；修复应仅在 `workday_run_id is None` 时删除行。

- [P3] daily_runs.sync_from_n8n 无异常防护，n8n 本地库不可读时 executions 端点 500 — E:\code\novel-editorial\tools\daily_runs.py:56-70
  `tools/daily_runs.py:_n8n_executions`（第 56-70 行）只处理 `N8N_DB` 不存在的情况，`sqlite3.connect` / `execute` 抛出的异常（数据库被占用、表结构变化、损坏）会直接穿透 `sync_from_n8n`；调用方 `novel_editorial/web_api.py:_get_daily_runs`（第 150 行）也没有 try/except，导致整个 runs 端点 500 且 `list_runs` 完全不返回。该模块 docstring 声明 `sync_from_n8n` 是 watch-only 的耐久记录，失败应降级为返回空列表而不是中断面板查询。

- [P3] agent_meeting.main 中 apply_report 只捕获 ImportError/AttributeError，outline 损坏时会议 CLI 崩溃 — E:\code\novel-editorial\tools\agent_meeting.py:748-753
  `tools/agent_meeting.py` 第 748-753 行 `apply_report(conn, novel_id, report)` 的 except 只覆盖 `(ImportError, AttributeError)`，而 `apply_architect.apply_report` 内 `json.loads(row["outline"] or "{}")`（`apply_architect.py` 第 223 行）无容错，outline JSON 损坏或 SQL 异常会直接终止整个会议 main（此时会议数据已提交，重跑会产生重复归档）。对比 `novel_editorial/services/meeting_session.py` 第 658-672 行对同一调用的完整 try/except + audit 处理，此处应保持一致。

- [P3] editorial_state._scoped_ids 的 (0,) 分支不可达（死代码） — E:\code\novel-editorial\tools\editorial_state.py:11-13
  `tools/editorial_state.py` 第 11-13 行 `_scoped_ids` 返回 `(0,) if novel_id == 0 else (0, novel_id)`，但三个调用点（`list_relations`/`list_memories`/`list_promises`）都在 `if novel_id:` 为真时才调用该函数，novel_id==0 时根本不会进入 IN 子句，因此 `(0,)` 分支永不执行。无害但属于误导性死代码，与 `agent_context._scoped_ids` 的等价实现一起可清理。
