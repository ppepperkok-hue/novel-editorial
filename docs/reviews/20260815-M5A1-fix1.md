# 2026-08-15 M5-A1-FIX1 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 3769382
- 审查依据：AGENTS.md / rules.md / docs/implementation/m5-implementation.md

### 优点

- 单次评估按 agent 扣减剩余预算，一次评估对任一 agent 不再超额。
- count 改为解析 payload 并只认 PROACTIVE_KINDS，rebuttal 不再占用预算。
- 负数 proactive_max_per_agent 在配置加载时即报 CONFIG_ERROR。
- 275 个测试全绿，ruff/pyright 通过，修复均有红转绿用例。

### 问题

#### Important（应该修）

1. src/novel_editorial/core/proactive.py:101-116 — count 改全量物化造成性能回归
   问题：count_proactive_messages 把 SQL COUNT 改成 .all()，将伙伴在工作区的全部 Message 行（含 content 正文）物化到内存再逐条 json.loads。
   影响：M5 文档已计划把评估挂到 talk/draft/review/decision/style/memory 等每个业务事件之后，A2 还要求 stress 不拖慢；对话积累后明显回归。
   修法：保留 kind 白名单语义，把过滤放回 SQL（五类 kind 精确 LIKE + initiator 标记组合 COUNT），或至少投影仅 payload 列。

### 建议

- 当前没有生产调用方，不构成阻塞，但 A2 接线前应修复。

### 结论

Ready to merge: With fixes

### 处理记录

- 修复包 M5-A1-FIX2 → commit ebe32dd（perf: count proactive messages in SQL without materializing rows）。
- 回归：276 passed（-n auto 13.75s）、ruff、pyright 0 errors、宪法 OK；SQL 形态守卫断言 count(*) + payload LIKE、不含 messages.content；502 行冒烟 1.59ms。
- 修复后遗留 P3 另案归档于 20260815-M5A1-fix2.md。
