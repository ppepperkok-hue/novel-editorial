# 2026-08-15 M5-A2-A 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit b644df6
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m5-implementation.md

### 优点

- record_proactive_messages 接线辅助复用 _record_message_in_session，消息与 agent.message 事件同事务落库，独立于业务事务，主动层失败不回滚业务。
- 三组 draft 情境（写手 report / 写手 question / 责编 gate review）固定文案、mock 下可精确断言；CLI 回显不破坏既有输出。
- 测试覆盖生成、修订、频次上限、开关关闭、失败不回滚；事件断言同步纳入 agent.message。

### 问题

#### Critical（必须修）

1. src/novel_editorial/core/proactive.py:230-236 — 写手 draft 追问吞掉 talk 首轮提问（红线 3 回归）
   问题：写手 draft_revised 追问复用 kind=proactive_question（带 trigger=draft_revised），而 chat.py 的 has_proactive_message 匹配任何含 "kind": "proactive_question" 的消息；此后 talk send 的责编首轮 PROACTIVE_QUESTION 被静默抑制。
   影响：违反 M5 红线 3「现有 PROACTIVE_QUESTION 行为保留」，破坏 talk 首轮对齐提问。
   修法：has_proactive_message 收窄为只认无 trigger 的 talk 首轮 payload（kind=proactive_question 且 not_like trigger），或给 draft 追问独立 kind。

#### Important（应该修）

2. src/novel_editorial/core/proactive.py:223-229 — 重生成误报初稿并重复消耗预算
   问题：draft generate 兼作重生成路径（同标题 v2 起 reason=revision），但 report 条件恒真；同一标题第二次 generate 仍发"初稿写完了"report 与重复 gate review。
   影响：事实错误且快速耗尽写手/责编频次预算。
   修法：context 传入 current_version/reason，report 与 gate review 只认 initial version。

#### Minor（可后补）

3. src/novel_editorial/cli/draft.py:99-104 — 质量门失败的修订仍发写手追问
   问题：draft_revise 的 context 只有 rebutted，质量门失败（quality_failed）时写手仍问"钩子要不要收"。
   影响：与 draft_gate_passed 只在 passed=True 触发的口径不一致。
   修法：context 传入 passed，question 条件加 passed is True。

### 建议

- 三处一并修复后重新全量验证；P1 需补 talk 回归用例。

### 结论

Ready to merge: With fixes

### 处理记录

- 修复包 M5-A2-A-FIX2 → commit 66ecb2c（fix: gate draft proactive follow-ups by version and quality），TDD 红转绿。
- 修复口径：has_proactive_message 加 not_like trigger；report/gate review 只认 current_version==1；question 条件 passed is True 且非 rebuttal。
- 回归：287 passed（-n auto 14.56s）、ruff、pyright 0 errors、宪法 OK；CLI 冒烟三场景全过。
- 复审归档于 20260815-M5A2A-fix2.md。
