Baseline: `python run_tests.py` discovered 578 tests and printed OK in 3 of 5 runs but FAILED once with `test_meeting_free_e2e.test_full_loop_with_approval_and_finish` (AssertionError, count=1 < 2), so the baseline is flaky, not reliably green; `webapp npm test` passed 10 files/45 tests and `npm run build` succeeded (only chunk-size/empty-react-chunk warnings). In-scope data files are healthy: ai_words.json is valid UTF-8 JSON with 21 words identical across quality_gate.py, tools/editorial_steps.py and the n8n fallback, overlap handling is now non-overlapping in all consumers with a dedicated test; compliance_words.txt and .env.example are BOM-free UTF-8 with no duplicate keys and every key consumed per the contract test. Two defects remain: a real-time flaky E2E test (P1) and a dead fake-green guard in run_tests.py (P2), so the patch is not fully correct.

Full review comments:

- Fix flaky wall-clock deadline in free-meeting E2E test — E:\code\novel-editorial\tests\test_meeting_free_e2e.py:86-106
  tests/test_meeting_free_e2e.py:86-106 polls for at most 8 real-time seconds for the async free-meeting loop to post >=2 speech rows, then hard-asserts `count >= 2`. This is load/time sensitive: a standalone `python run_tests.py` run in this review failed with `AssertionError: 1 not greater than or equal to 2` at line 106 (traceback captured in run output; 2 of 5 full-suite runs exited non-zero while the rest were green), so the verification baseline is intermittently red. The same real-time polling pattern with 5-6s deadlines exists throughout tests/test_meeting_free_loop.py (lines 45, 69, 159, 228, 267, 295, 424, 504) and is at the same risk. Consider waiting on the loop's processed-event counter or a completion signal instead of a wall-clock budget, and retry/raise the deadline to de-flake.

- Move zero-test guard before adding env-contract tests so it can fire — E:\code\novel-editorial\run_tests.py:138-141
  run_tests.py:138-141 appends EnvConfigContractTests to the suite and only then checks `if suite.countTestCases() == 0` for the fake-green guard, so the count is always >= 2 and the guard is dead code. Verified by replication: with an empty tests/ directory, `discover` returns 0 tests but the suite still counts 2 after `addTests`, so the run exits 0 having executed only the two env-contract tests — exactly the silent fake-green the guard was added to prevent (documented in the commit for P3-15). Move the `countTestCases() == 0` check before `suite.addTests(...)` (or count only the discovered suites) so an emptied or broken tests/ directory fails the run.
