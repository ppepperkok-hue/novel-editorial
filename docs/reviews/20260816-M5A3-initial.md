# 2026-08-16 M5-A3 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit a152152
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m5-implementation.md

### 优点

- talk list 用 payload 判定（initiator=agent 且 kind 属于五种主动类型）把主动消息标记为 `[agent·主动·<kind>]`，普通对话、拒稿、心情变化保持原格式；talk 首轮无 trigger 的 proactive_question 也能正确识别。
- events list 的 agent.message payload 已带 initiator/kind，来源可辨，测试锁定。
- usage 文档补全主动行为五类型与触发点、开关与上限配置、辨认方法，配置示例实测生效。
- 311 测试、ruff、pyright、宪法、smoke_m3、stress_m3 全绿。

### 问题

#### Minor（可后补）

1. src/novel_editorial/cli/talk.py:67 — 非哈希 kind 值导致崩溃
   问题：kind 可以是任意 JSON 类型，`kind in PROACTIVE_KINDS` 对 list/dict 做 frozenset 成员测试抛 TypeError，一条畸形 payload 会让整个 talk list 命令崩溃，与 docstring 承诺不符。
   影响：正常操作不受影响；畸形/损坏数据时可见性命令不可用。
   修法：加 isinstance(kind, str) 守卫。

### 建议

- 修复后补畸形 kind 回归用例。

### 结论

Ready to merge: With fixes

### 处理记录

- 修复包 M5-A3-FIX → commit 2bab016（fix: guard non-string kind in proactive message classification）。
- 修复口径：isinstance(kind, str) and kind in PROACTIVE_KINDS；分类测试补 list/dict kind，新增 talk list 畸形 payload 端到端用例。
- 回归：314 passed（-n auto 16.80s）、ruff、pyright 0 errors、宪法 OK。
- 复审归档于 20260816-M5A3-fix.md。
