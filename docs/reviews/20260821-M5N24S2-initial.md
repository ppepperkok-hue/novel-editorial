# 2026-08-21 N24 API 服务层 S2 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 7893b83（feat(api): add visibility routes with deterministic ordering (N24 S2)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n24-implementation.md / src/novel_editorial/api/app.py / tests/test_api.py

### 优点

- 四条可见性路由（/overview、/events、/style、/structure）全部复用既有 core/store 函数，字段口径与 CLI 对齐；S1 审查 P3 排序问题已修复并补断言。
- 93 条相关测试、smoke_m3 SMOKE OK、stress_m3 STRESS OK、ruff / pyright 全绿；只读断言覆盖 events 内容不变与全部表行数不变。

### 问题

#### Critical（必须修）

- [P1] GET /works/{id}/style 在锚缺失时落库 — src/novel_editorial/api/app.py:183-188
  `get_workspace_style` 调用 `style.get_style_anchor`，该函数在锚缺失时会新建并提交一行 style_anchors（实测新作品 GET 前 0 行、GET 后 1 行），违反「本阶段只有 POST /works 可写」红线与实施文档只读要求；只读测试因预建锚而漏检。修法：只读查询 StyleAnchor（参照 style_drift），锚缺失返回空值且不新建行，并补无锚断言。

#### Important（应该修）

无。

#### Minor（可后补）

无。

### 建议

- 只读路由统一以「不新增任何行」为硬保证，测试用全表行数快照。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：无锚作品 GET /style 后 style_anchors 行数 0→1。
- 修复：随 S3 提交 0e1b9d5 落地——只读查询缺锚返回空串，补 `test_style_route_missing_anchor_is_read_only`；全量 1047 passed、smoke_m3 / stress_m3 全绿。
