# 2026-08-22 N23 试读反馈回流 S2 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit ac41a73（feat(quality): add feedback CLI with agreement-maximizing suggestion (N23 S2)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n23-implementation.md / src/novel_editorial/core/feedback.py / src/novel_editorial/cli/quality.py / tests/test_feedback.py

### 优点

- S1 审查 P2 完整闭合：建议阈值改为一致率最大化（并列取更高、更保守），canonical 一致率从 5/6 提升到 6/6 不再倒挂。
- `quality feedback` CLI 输出与规格逐字一致；`--apply` 复用 set_quality_threshold 且幂等；退出码路径完整。123 条相关测试、smoke_m3 SMOKE OK、stress_m3 STRESS OK、ruff / pyright 全绿。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

- [P2] `int(best)` 截断小数候选阈值 — src/novel_editorial/core/feedback.py:179-185
  候选含小数时（check_quality 的 style_penalty 可产生 0.5 倍数分数），`int(best)` 把一致率最高的候选截断，报告一致率与实际阈值不一致（bad=9.5 / good=9.2 时最佳候选 9.2 一致率 1.0，截断后 9 的一致率仅 0.5 且 9 不在候选集）；并列取更高的保守 tie-break 也被抵消。当前默认打分路径分数恒为整数暂不可触发。修法：候选集改为分数跨度内整数网格，报告一致率在该整数阈值上重算。

#### Minor（可后补）

无。

### 建议

- 建议阈值恒为整数（config 只接受整数），报告一致率必须与落库阈值严格对应。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：小数分数场景 int(9.2)=9 导致一致率 1.0 → 0.5。
- 修复：随 S3 提交 a24e9db 落地——候选集改为 `range(floor(min_score), ceil(max_score)+1) ∪ 当前阈值`，补两条小数用例；全量 1135 passed、smoke_m3 / stress_m3 全绿。
