The commit fixes the intended round-3 issues (workday dry-run persistence, rework idempotency, cover/mailroom response checks, retry helper) and all 251 targeted slice tests pass, but it introduces a silent-failure regression in meeting_actions (marker committed before side effects, making failures permanently unretryable) and leaves two closely related unguarded JSON/int parses in the same slice. The blocking regression should be fixed before merge.

Full review comments:

- Commit the post-actions marker only after side effects succeed — E:\code\novel-editorial\tools\meeting_actions.py:34-56
  In `tools/meeting_actions.py:34-56` the idempotency marker is now inserted with `INSERT ... SELECT ... WHERE NOT EXISTS` and `conn.commit()` runs **before** the lesson/proposal drafts are written. If any later step throws (DB write failure, constraint, disk error), the marker is already committed, so every retry of `run_post_actions` returns `{"skipped": true}` and the incident/retro/learning knowledge drafts are silently lost (only `post_actions_routing_failed` is audited by `meeting_session.close`). The previous code inserted the marker last, so a failed run could be retried. Reproduced with a temp DB: patch `_insert_draft` to raise, first call raises, second call after restore returns `skipped` with zero drafts written.

- Pass messages through to the first ask() chat call — E:\code\novel-editorial\tools\agent_meeting.py:210-219
  The retry refactor in `tools/agent_meeting.py:218` calls `_chat_with_retry(agent, model, system, user, temperature, max_tokens, tools=tools)` and silently drops the `messages` argument of `ask()`, even though `_chat_with_retry` accepts it and the pre-refactor code passed `messages=messages` to `chat_deepseek`. No in-repo caller currently passes `messages`, so there is no active failure, but the parameter is now dead and any caller (including the tool-loop path) relying on multi-turn context would get a fresh conversation. Pass `messages=messages` in the first `_chat_with_retry` call.

- Guard reply_to parsing against non-integer LLM output — E:\code\novel-editorial\tools\editorial_daily.py:97-99
  In `tools/editorial_daily.py:98`, `reply_to = int(item.get("reply_to") or 0)` runs inside the `_handle_outbox` loop with no local `except`. When an agent emits a non-integer `reply_to` (e.g. `"abc"` or `12.5`, which LLM JSON output regularly produces), `ValueError` propagates out of `_agent` and the whole daily run is marked `failed`, discarding the rest of the run. This is pre-existing (introduced in 769b9434, before this commit), but it directly gates the new rework flow since `rework_requests` depend on `reply_to`. Wrap the cast in try/except and skip invalid items.

- Guard novels.outline JSON parse in apply_architect too — E:\code\novel-editorial\tools\apply_architect.py:188-188
  This commit hardened the same failure in `tools/architect_weekly.py:181-188` (`json.loads(row["outline"])` with fallback), but `tools/apply_architect.py:188` (`apply_report`) and `:343` (CLI `main`) still call `json.loads(row["outline"] or "{}")` unguarded. With a corrupted-but-real `outline` value (the exact scenario the round-3 fix targets), every meeting tail that calls `apply_report` raises `ValueError`; in `agent_meeting.main` only `ImportError/AttributeError` are caught, so blueprint/reader_persona/finish/next_book decisions are not persisted and the CLI exits abnormally. Pre-existing, but the same defect the commit set out to fix and missed.

- Skip session/archive writes when agent_meeting runs dry — E:\code\novel-editorial\tools\agent_meeting.py:619-632
  `tools/agent_meeting.py:619-632` and `:668-680` unconditionally `INSERT INTO meeting_sessions` and `weekly_meetings` in `main()` even with `--dry-run`, while the commit just fixed `workday` so dry-runs no longer persist state. A `--dry-run` meeting therefore still creates a `running` session and a `completed` archived meeting row, and topic-meeting mode additionally writes `agent_diaries` (lines ~700). Wrap these writes in `if not args.dry_run:` for consistency with the dry-run contract. Pre-existing.

- Map completed_with_pending and skipped statuses in flow report — E:\code\novel-editorial\tools\export_flow_html.py:74-81
  The new `status_class` map in `tools/export_flow_html.py:74-81` covers `completed/success/partial/failed/error/running/idle` but not `completed_with_pending` or `skipped`, both of which `workday.close`/`open` write into `daily_runs.status`. A workday that ends with pending actions (the common case when `pending_actions`/legacy is non-empty) renders the header badge grey "待命（暂无运行）" and the report summary misleads. Add `"completed_with_pending": "warn"` and `"skipped": "idle"` to the map (and the adjacent `status_text` map).
