审查范围：novel_editorial 包全部模块，并仅对 tools/* 依赖接口做契约核验；基线为 `python -m compileall novel_editorial`（exit 0，语法通过），按要求未跑全量测试，另做了 9 组针对性运行探针。结论：无 P0/P1，但存在 3 个可复现的 P2 缺陷（action_items JSON 字符串被拆碎、bind_book 半提交状态、apply_schedule 跨盘 ValueError），均属边界触发且有真实后果，另有两个 P3（注释与实现不符、死代码），因此该切片不能判定为无缺陷。

Full review comments:

- [P2] _normalize_action_items 把 JSON 数组字符串拆成碎片任务 — E:/code/novel-editorial/novel_editorial/services/activity.py:263-273
  round4 新增的 `_normalize_action_items`（novel_editorial/services/activity.py:263-273）对字符串只做 `re.split(r"[\n,;；、]+")`，没有先尝试 `json.loads`。当会议 report 的 `action_items` 是 JSON 数组字符串（LLM 常见输出，如 `'[{"task":"A","owner":"writer"},...]'`）时会被拆成 `'"owner": "writer"}'` 这类碎片；实测在 LLM 兜底路径失败走规则回退时，`generate_post_meeting_actions` 把 `'"owner": "writer"}'` 和 `'"owner": "editor"}]'` 原样写入 `agent_actions`，这些垃圾任务还会被注入日更简报。现有测试只覆盖 list 与纯文本字符串，未覆盖 JSON 字符串。建议字符串分支先 `json.loads`，失败再按分隔符拆分。

- [P2] bind_book 先提交 DB 再写 ~/.n8n/.env，目录缺失时状态半更新 — E:/code/novel-editorial/novel_editorial/services/ending.py:51-68
  novel_editorial/services/ending.py:51-68：`UPDATE novels SET status='publishing', book_id=...` 先 `commit()`，随后 `env_file.write_text(...)` 不保证父目录存在。实测当 `~/.n8n/` 不存在（首次使用或目录被删）时抛 FileNotFoundError → API 500，但 DB 已提交；此时 status 不再是 'ready'，再次绑定时被 `bind_book` 拒绝（'新书未确认'），而 env 中 FANQIE_BOOK_ID 仍是旧值，日更发布链会继续把新章节发到旧书。测试 test_audit_evolution.py:167 只覆盖父目录已存在的场景。建议写入前 `mkdir(parents=True, exist_ok=True)`，并将 DB 更新移到 env 写入成功之后。

- [P2] apply_schedule 跨盘符 os.path.relpath 抛 ValueError 导致 500 — E:/code/novel-editorial/novel_editorial/services/control.py:289-294
  novel_editorial/services/control.py:290 在构建 powershell 命令时调用 `os.path.relpath(_db_path(), ROOT)`，该调用位于 `try`（294 行）之外。当 web_api 以 `--db` 指定与代码根 ROOT 不同盘符的数据库（如 `D:\...\demo.db`，ROOT 在 E:）时抛 `ValueError: path is on mount 'D:', start on mount 'E:'`，异常直接冒泡到 do_POST 变成 500，定时任务注册失败且无明确错误信息。这与 round4 在 desktop 侧修复的 'non-C drive paths' 属同类问题，已实测复现。建议直接传绝对路径（并让 install_daily_task.ps1 接受绝对路径），或对跨盘场景单独处理。

- [P3] run_session 注释声称用会话库，实际忽略行的 db_path 回退 demo.db — E:/code/novel-editorial/novel_editorial/services/meeting_session.py:324-330
  novel_editorial/services/meeting_session.py:324-330 的 docstring 写着 'Use the database the session was created on; never fall back to a hardcoded demo.db lookup'，但 `run_session` 从不读取 `meeting_sessions.db_path` 行字段，而是 `db.connect(db_path or config.DB_PATH)`；`start_session_async` 默认 `db_path=None` 时同样落到 `config.DB_PATH`。当前唯一调用方 web_api 显式传 db_path 所以未触发，但任何未来调用方省略该参数都会静默连错库、会话找不到。建议按行内 db_path 解析或修正注释。

- [P3] services/n8n.py 全仓库无调用方，属死代码 — E:/code/novel-editorial/novel_editorial/services/n8n.py:1-6
  对 novel_editorial/tools/desktop/webapp/scripts 全量 rg 未发现任何 `from novel_editorial.services.n8n` 或 `services.n8n` 导入，模块内 `n8n_api`/`workflow_status`/`executions` 均无消费者；其职责（读取 n8n 执行记录）与工具实际路径 `tools/daily_runs.py` 重叠。round4 仍在维护该模块（N8N key 刷新改动），建议删除或标注 DEPRECATED，避免双份 n8n 调用逻辑漂移。
