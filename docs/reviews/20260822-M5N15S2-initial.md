# 2026-08-22 N15 灵感素材库 S2 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit f0f1e79（feat(inspiration): add CLI group with event payload convention fix (N15 S2)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n15-implementation.md / src/novel_editorial/core/inspiration.py / src/novel_editorial/cli/inspiration.py / tests/test_inspiration.py / tests/test_inspiration_cli.py

### 优点

- S1 审查 P2 完整闭合：事件 payload 遵循「payload.kind 即事件子类型」惯例，created / removed 可区分。
- `inspiration` CLI 组四条命令按规格输出与退出码实现；40 条相关测试、smoke_m3 SMOKE OK、stress_m3 STRESS OK、ruff / pyright 全绿；全量 1099 测试通过。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- [P3] `inspiration add` 位置参数无法接收 `-` 开头内容 — src/novel_editorial/cli/inspiration.py:22
  内容作为位置参数会被 Click 误判为选项（`- 破折号` 实测报 No such option、退出码 3）；随手记场景中列表式文本很常见。修法：content 改为 `--content` 必填选项（与 `setting add` 惯例一致）。
- [P3] 测试辅助 `_create_workspace` 解析脆弱 — tests/test_inspiration_cli.py:23-26
  `split()[2]` 在 logging capture 关闭时会取到 stderr 的 "INFO"；改用正则 `re.search(r"created workspace (\w+):", ...)`（与 test_cli_registry 一致）。

### 建议

- 自由文本类参数统一走 `--content` 选项，位置参数只留 id 类短值。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：`- 破折号` 内容 CLI 退出码 3；无 logging capture 时测试解析失败。
- 修复：随 S3 提交 60d4f58 落地——content 改 `--content` 选项、测试改正则解析、补 `-` 开头内容用例；全量 1100 passed、smoke_m3 / stress_m3 全绿。
