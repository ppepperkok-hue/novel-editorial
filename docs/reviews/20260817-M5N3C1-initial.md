# 2026-08-17 M5-N3-C1 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit abc09b5
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m5-n3-implementation.md

### 优点

- BehaviorTimeline 模型十列与迁移一致，fresh-DB autogenerate 探测无漂移；服务层 kind 白名单、空值校验、rowid 插入序与事件溯源取最新语义完整，10 条测试覆盖迁移升级与边界。
- 迁移头单链（f80e112950a2）、upgrade→downgrade→upgrade 往返通过；364 测试、ruff、pyright、宪法校验全绿。

### 问题

#### Important（应该修）

- 迁移幂等守卫只检查表存在：若表在而两个索引缺失（中断的首次运行等），revision 被标记已应用、索引永不创建，与模型 index=True 元数据静默分叉。修法：索引创建与表守卫解耦，用 CREATE INDEX IF NOT EXISTS。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实为真，修复 commit b6973b9，复审见 20260817-M5N3C1-fix.md。
