# 2026-08-20 N14 K2 审查意见修复记录

- 修复来源：20260820-M5N14K2-initial.md 的 P3。
- 修复方式：独立 CLI（flash + high），先写失败测试（Émilie / émilie 场景）再改。
- 修复范围：src/novel_editorial/core/agents.py、tests/test_agents.py。

### 改动

- create_agent 名字唯一检查：改为拉取作品内全部 agent 名字后在 Python 中 casefold 比较，不再依赖 SQLite lower()。
- resolve_agent 名字分支：先精确 ID → 再对全部 agent 名字做 Python casefold 匹配 → 最后角色别名；与 create_agent 同一策略，func.lower 依赖移除。
- 新增失败测试两条：Émilie 后再建 émilie 被拒（USAGE_ERROR）、resolve_agent("émilie") 命中 Émilie；既有 ASCII/中文名字测试保持绿。

### 验证

- 子集 `uv run pytest -q tests/test_agents.py`：22 passed。
- `uv run ruff check .`：All checks passed；`uv run pyright`：0 errors；宪法校验 OK。
- 全量 `uv run pytest -q`：779 passed。

### 结论

审查意见处理完毕，K2 收口（commit f9c0731）。
