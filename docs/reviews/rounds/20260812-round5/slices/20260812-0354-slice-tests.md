范围：tests/（62 个文件）、run_tests.py、ai_words.json、compliance_words.txt、.env.example，及其契约方 config.py / compliance.py / quality_gate.py / services/meeting_session.py 等。基线：python run_tests.py 476 用例全绿（exit 0，62 文件全部 discover、无 0 用例文件、无 skip 装饰器）；webapp npm test 7 文件/16 用例通过；npm run build 成功（仅 chunk 体积警告）。未发现 P0/P1：无安全、并发、SQL、编码或假绿问题；前几轮记录的 ai_words 重叠双倍计数已由 c00198d 修复（实测「微微一愣」仅计 1 次）。全部 5 项发现均为 P3 文档/防御性缺口（合规词库告警无测试守护、.env.example 漏键与死键、quality_gate 类型校验缺失、合规词库读取无异常捕获），不影响现有测试与功能正确性，故判定 patch 正确。

Full review comments:

- [P3] compliance 空词库告警逻辑与真实数据文件均无测试守护 — E:\code\novel-editorial\tests\test_compliance.py:23-29
  tests/test_compliance.py:23-29 只用 mock.patch 把 WORDS_FILE 换成临时文件，没有任何用例读取仓库根的真实 compliance_words.txt / ai_words.json，也没有用例断言 round3 新增的 warnings 行为（novel_editorial/compliance.py:86-95）。当前 compliance_words.txt:1-3 只有注释，属「空词库」活路径：pipeline.py:126/198、tools/editorial_daily.py:1179 每次发布前都会走到 EMPTY_WORDS_WARNING 分支——实测 compliance.check() 返回 warnings 非空且抛 RuntimeWarning，但 476 个测试全绿。docs/reviews/rounds/20260812-round4/slices/20260812-0330-slice-tests.md:8-9 已明确建议补「缺失/空/有自定义词」三组 warnings 断言，round4 修复提交 d92d929 未落实；若文件被清空或误存坏编码，发布前合规扫描行为改变时不会有任何测试失败。

- [P3] .env.example 遗漏被实际消费的 REVIEW_RETRY_MAX 与 MEETING_HEARTBEAT_TIMEOUT_MINUTES — E:\code\novel-editorial\.env.example:1-81
  通过 AST 扫描全仓 .py/.js/.mjs 的 env 读取调用并与 .env.example:1-81 比对：REVIEW_RETRY_MAX 被 novel_editorial/config.py:59 与 tools/editorial_daily.py:994 消费，MEETING_HEARTBEAT_TIMEOUT_MINUTES 被 novel_editorial/services/meeting_session.py:87 消费，两者均未出现在 .env.example 中。commit 2aae15c 的「env docs」修复补充了 DISPATCH_MODE/REWORK_MAX/COST_* 等一批键，但漏了这两个，新用户无法从示例配置发现这些可调开关。

- [P3] .env.example 四个死配置键误导用户（MONTHLY_BUDGET/N8N_HOST/N8N_LISTEN_ADDRESS/N8N_PASSWORD） — E:\code\novel-editorial\.env.example:24-24
  全仓（排除 docs/legacy/n8n JSON/desktop/release 打包副本）检索确认这四个键没有任何代码消费方：MONTHLY_BUDGET（.env.example:24，注释声称「超限触发监控告警」，实际无读取方）、N8N_HOST 与 N8N_LISTEN_ADDRESS（.env.example:77-78，无消费方）、N8N_PASSWORD（.env.example:46，tools/n8n_api.py:21 实际使用 N8N_TMP_PW，键本身只在注释里自认「当前代码未读取」）。用户按示例配置 MONTHLY_BUDGET 期望的预算告警不会生效，属静默失效的文档配置。

- [P3] quality_gate 加载 ai_flavor 缺 isinstance 校验，字符串值会按单字符计 AI 味密度 — E:\code\novel-editorial\novel_editorial\quality_gate.py:20-25
  novel_editorial/quality_gate.py:22-23 直接 `AI_FLAVOR_WORDS = _AI_DATA.get("ai_flavor", AI_FLAVOR_WORDS)`，无类型校验；对照 tools/editorial_steps.py:314-321 的 `_load_flavor_words` 有 `isinstance(words, list)` 守卫。若 ai_words.json 是合法 JSON 但 ai_flavor 为字符串（手误/生成器错误），quality_gate 会把字符串按字符迭代拼成正则：复现 `_AI_DATA={'ai_flavor':'突然'}` 后，普通句子「他已然习惯，这里并无不妥。」的 ai_flavor_density 从 0.0 变为 90.9/千字（命中单个「然」字），style 评分静默失真且无任何测试失败；两个消费方对同一数据文件的容错行为不一致。

- [P3] compliance._read_custom_words 无异常捕获，坏编码词库文件直接穿透发布前扫描 — E:\code\novel-editorial\novel_editorial\compliance.py:62-71
  novel_editorial/compliance.py:67 用 `WORDS_FILE.read_text(encoding="utf-8")` 读取自定义词库且无 try/except（_load_words 亦无），而 quality_gate/editorial_steps 对 ai_words.json 均捕获 (OSError, ValueError)。一旦 compliance_words.txt 被误存为 GBK/带 BOM 或截断损坏，UnicodeDecodeError 会从 compliance.check() 直接抛出，使 pipeline.py:126/198 与 tools/editorial_daily.py:1179 的发布前合规扫描崩溃，且当前无测试覆盖该路径。
