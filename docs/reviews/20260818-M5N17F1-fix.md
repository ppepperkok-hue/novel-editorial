# 2026-08-18 N17 F1 审查 P3 修复记录

- 修复来源：20260818-M5N17F1-initial.md 的两条 Minor（时区归一化 / 配置重复读取）。
- 修复方式：独立 CLI（flash + high），先写失败测试再改。
- 修复范围：src/novel_editorial/core/memory.py、tests/test_memory.py。

### 改动

- _now_utc 归一化：None → datetime.now(UTC)，naive → 补 UTC，aware → astimezone(UTC)；写库时间永远归一化。
- effective_strength 增加可选 decay_per_day 参数，缺省才读一次配置；apply_memory_decay / list_archive_candidates / archive_memory_notes(candidates=True) 循环外一次 load_settings，批量路径 O(1) 次配置重读。

### 验证

- 新增 5 条测试（含写库归一化与三个批量函数的配置计数断言，先红后绿）。
- `uv run pytest -q tests/test_memory.py tests/test_config.py`：97 passed。
- `uv run ruff check .`：All checks passed；`uv run pyright`：0 errors, 0 warnings。
- 全量 `uv run pytest -q`：506 passed。

### 偏离记录

- 审查原文举例「effective_strength 按 UTC 墙钟算 1 天」实测不成立（_as_utc 已归一 now），真正的缺陷在写库路径；保留原例为行为锁定，另补写库归一化用例覆盖。

### 结论

审查意见全部处理完毕，F1 收口（commit 43ed147）。
