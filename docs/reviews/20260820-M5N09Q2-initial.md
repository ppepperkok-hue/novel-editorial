# 2026-08-20 N9 质量门语料校准 Q2 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 6aeac79（feat(quality): add calibrate CLI with idempotent threshold write-back (N9 Q2)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n09-implementation.md / src/novel_editorial/core/calibration.py / src/novel_editorial/core/config.py / src/novel_editorial/cli/quality.py / tests/test_quality_calibrate.py / tests/test_config.py

### 优点

- `quality calibrate` 输出（样本/每样本行/分布/建议/skipped/warning）与 Q2 规格逐条一致；--apply 打印将写入值、幂等写回、无 --apply 不写文件；退出码由 app 层映射（NOT_FOUND→1、USAGE_ERROR→2）。
- set_quality_threshold 文本级写回保留注释与其他键/段，非法 TOML 先校验后写。
- 目标 143 条全绿；全量 869 passed；smoke_m3 SMOKE OK；stress_m3 STRESS OK。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

- `src/novel_editorial/core/config.py:270-286`：逐行正则不感知多行基本字符串（""" / '''），块内假键会被误改、块内 `[foo]` 行会提前截断段扫描并写出重复键的非法 TOML。

#### Minor（可后补）

- `src/novel_editorial/core/config.py:276-286`：现值等于传入值时仍重写该行，破坏字节级幂等并可能丢弃 `#` 前无空格的注释。

### 建议

两条意见一并修复后收口。

### 结论

Ready to merge: With fixes

### 处理记录

- 2026-08-20：派 M5-N09-Q2FIX，commit 0bd0071 修复（多行字符串/数组状态机 + tomllib 同值短路 + 注释保留）；复审发现转义三引号回归，派 Q2FIX2，commit 0a86458 修复。
