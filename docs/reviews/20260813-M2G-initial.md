# 2026-08-13 M2-G（U21 质量门完善 + U22 AI 味定位）初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 7b481f8
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m2-implementation.md 子阶段 G / docs/project-plan/05-executable-units.md U21/U22

### 优点

- check_quality 扩展句式重复率（3 字句尾重复计数）与风格一致性（style_keywords 命中率），保持纯函数，默认参数下与旧行为兼容；新增维度进入 details。
- extract_style_keywords 支持分隔符分词与无分隔 ngram 两种提取；generate/revise 与 quality check 均从风格锚点提取关键词传入。
- quality explain 逐句定位 AI 词 / 修饰词密度 / 句式重复并给规则改写建议，干净文本明确提示；170 个测试全绿。

### 问题

#### Critical

1. **P1：风格缺失惩罚无界，长描述下干净草稿结构性失败**
   - 文件：src/novel_editorial/quality/gate.py（108-114 行）
   - 问题：missing_style * 0.5 无上限；无分隔 8+ 字符描述产生 18 个 ngram 关键词后，干净文本 score 9.0 > threshold 8 而 quality_failed，decide 拒绝 accept，阈值在描述越具体时越失效。
   - 修复：惩罚归一化为 STYLE_MISS_WEIGHT * missing_style / len(style_keywords)（67c0e28），纯缺失最多贡献 0.5 分，干净文本不再结构性失败；风格一致性退化为软信号（可接受取舍）。

#### Important

1. **P2：纯分隔符描述导致 extract_style_keywords IndexError**
   - 文件：src/novel_editorial/core/style.py（23-26 行）
   - 问题：描述仅为分隔符（如 "，"）时 split 后 tokens 为空，tokens[0] 抛 IndexError，且函数已接入 generate/revise/quality check，导致相关命令崩溃。
   - 修复：split 过滤后增加空列表守卫返回空集（67c0e28）；复审查补充未收录分隔符（…—–·・-）后垃圾关键词问题（ee6c39b）。

#### Minor

无。

### 结论

Ready to merge: No（With fixes）

### 处理记录

- 2026-08-13 派第 1 轮修复（67c0e28：空 token 守卫 + 惩罚归一化）与第 2 轮修复（ee6c39b：分隔符正则补齐）。
- 终审确认全部闭合，最终回归 170 passed，ruff / pyright / 宪法全绿。详见 [20260813-M2G-fix.md](20260813-M2G-fix.md)。
