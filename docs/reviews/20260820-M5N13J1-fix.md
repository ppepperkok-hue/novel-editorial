# 2026-08-20 N13 J1 审查意见修复记录

- 修复来源：20260820-M5N13J1-initial.md 的 P2 + P3。
- 修复方式：独立 CLI（flash + high），先写失败测试（临时 repro 确认两条问题）再改。
- 修复范围：src/novel_editorial/core/structure.py、tests/test_structure.py。

### 改动

- move_node 早退：`parent_id == node.parent_id and sort_order is None` 时直接返回节点，不写库、不留 structure_moved 事件；已在根传 None 同理（no-op）。
- 新增 `_validate_sort_order`：create_node / move_node 在 sort_order 非 None 时校验 >= 0，负数抛 USAGE_ERROR（message 含 "invalid sort order"），失败不落库。
- 新增失败测试四条：同父无序移动 no-op（顺序/sort_order/事件数不变）、根到根 no-op、create 负序拒绝、move 负序拒绝且节点状态不变；既有事件留痕测试改为「先挂卷再移回根」保持移动事件真实发生。

### 验证

- 子集 `uv run pytest -q tests/test_structure.py`：34 passed。
- `uv run ruff check .`：All checks passed；`uv run pyright`：0 errors；宪法校验 OK。
- 全量 `uv run pytest -q`：745 passed。

### 结论

审查意见处理完毕，J1 收口（commit 210d429）。
