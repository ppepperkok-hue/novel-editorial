# 2026-08-20 N13 J2 审查意见修复记录

- 修复来源：20260820-M5N13J2-initial.md 的 P2 ×2。
- 修复方式：独立 CLI（flash + high），先写失败测试（4 条复现两条问题）再改。
- 修复范围：src/novel_editorial/store/models.py、migrations/versions/d3c2b1a09f8e_add_outlines_table.py、src/novel_editorial/cli/structure.py、tests/test_outline.py、tests/test_structure_cli.py。

### 改动

- Outline 模型加 `UniqueConstraint("workspace_id", "version", name="uq_outlines_workspace_version")`；迁移建表镜像同一约束，并沿用「index ensured separately」模式补 `CREATE UNIQUE INDEX IF NOT EXISTS`，中断首跑/旧表场景下 DB 层守卫同样存在；并发重复版本插入由 IntegrityError 兜底。
- structure_move 目标父级解析重写：--root → None；--parent → 指定值；仅 --order → 查节点当前 parent_id 保持同级重排；三者互斥规则与输出不变。
- 新增测试五条：模型约束声明、迁移镜像约束、重复 (workspace_id, version) 被 DB 拒绝、order-only move 保持当前父级、--root/--parent/--order 组合语义回归。

### 验证

- 子集 `uv run pytest -q tests/test_outline.py tests/test_structure_cli.py`：24 passed。
- `uv run ruff check .`：All checks passed；`uv run pyright`：0 errors；宪法校验 OK。
- 全量 `uv run pytest -q`：750 passed。

### 结论

审查意见处理完毕，J2 收口（commit 6faf3c2）。
