# 2026-08-19 N5 作品设定库 G1 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 5bec3b6（feat(setting): add versioned setting entries and core services (N5 G1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / extension.md / docs/implementation/m5-n5-implementation.md / 06 backlog

### 优点

- 两表（setting_entries / setting_versions）与迁移符合规格：线性链到 head 92a0cb3a3bb1、幂等建表、旧库升级安全；五个核心函数（add / list / get / revise / history）与版本化语义（版本递增、reason/actor 留痕、唯一约束）完整。
- 全量 559 测试 + ruff + pyright + 宪法校验全绿；34 个新用例覆盖校验、隔离、迁移回填。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- revise_setting 未校验 actor 非空 — src/novel_editorial/core/setting.py:115-118
  - 问题：content/reason 有校验，actor 没有；actor="" 时会把空操作者写进 setting_versions，违反 N5 红线「每条修订必须有原因与操作者」。
  - 影响：版本历史操作者不可溯；属低严重度但违反本阶段强制红线。
  - 修法：content/reason 校验后加 `if not actor.strip(): raise NovelError(USAGE_ERROR, ...)`。

### 建议

修复后 G1 可收口；另建议 add_setting 的 source 同样补非空校验（与本次同性质），并入 G2 处理。

### 结论

Ready to merge: With fixes

### 处理记录

- 2026-08-19：revise actor 校验已修复（commit 2f0974b，先写失败测试再改，全量 561 测试绿），修复包归档于 20260819-M5N5G1-fix.md。
- 2026-08-19：add_setting source 非空校验并入 G2 任务包处理。
