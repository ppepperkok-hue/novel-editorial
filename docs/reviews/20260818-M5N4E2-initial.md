# 2026-08-18 N4 编辑部讨论形态 E2 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 6962088（feat(discussion): add N2 stance refusal and N3 sediment to discussions (E2)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / extension.md / docs/implementation/m5-n4-implementation.md / 06 backlog

### 优点

- E2 按实施文档完整落地：N2 拒绝口径与 talk send / delegation 一致，拒绝带 rule/stance、重复命中带 reaffirmation + repeated，作者推翻后恢复正常表态。
- N3 沉淀正确：每位发言伙伴一条 viewpoint，正常/拒绝的 summary 与 after_value 分开，source 稳定；mood 更新为 MOOD_TALK；沉淀或 mood 失败只告警、不回滚总结。
- 端到端实测通过：正常态、含拒绝态、override、跨通道重申、malformed payload 兜底均符合规格；全量 461 测试 + ruff + pyright 全绿。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- summarize_discussion 非幂等 — src/novel_editorial/core/discussion.py:261-269
  - 问题：同一 discussion_id 重复调用会再写一条 discussion_summary 消息，并给每位发言伙伴重复追加 viewpoint 沉淀（实测第二次调用后每人 2 行），违反 AGENTS.md 幂等规则与 E2「每人每轮一条 viewpoint」不变量。
  - 影响：公共 API 路径重复调用会污染 behavior show / timeline；CLI 每次只调一次，不会触达。
  - 修法：已有 discussion_summary 时原样返回既有消息，不再写消息、不再跑沉淀。
- 讨论渠道的重复拒绝不计入重申历史 — src/novel_editorial/core/discussion.py:101-107
  - 问题：has_same_rule_refusal 只匹配 kind=refusal / delegation_response，不含 discussion_contribution(position=refused)，同一伙伴上一轮讨论拒绝过、新一轮再命中时用首次拒绝文案而非重申口径。
  - 影响：与 N2「重复命中 → reaffirmation」跨通道口径不一致（E2 验收只要求 talk/互委 → 讨论，属非阻塞）。
  - 修法：has_same_rule_refusal 增加 discussion_contribution 分支（rule 字段只在拒绝表态存在，stated 不误匹配）。

### 建议

两条非阻塞意见修复后 E2 可收口；E3 在该底座上补可见性与文档。

### 结论

Ready to merge: With fixes

### 处理记录

- 2026-08-18：两条意见均已修复（commit bc71c99，先写失败测试再改，83 相关用例 + 全量 463 绿），修复包归档于 20260818-M5N4E2-fix.md。
- 2026-08-18：E2 收口验证全量 463 测试 + smoke_m3 + stress_m3 全绿。
