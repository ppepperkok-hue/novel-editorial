范围：novel_editorial 包（db/config 仅作契约核验，tools/webapp/desktop 未扫描）。基线：python -m compileall 退出码 0，切片相关 153 个测试全过（43+110）。三条 P3 均不阻塞现有代码与测试：监控/seed 的 CLI 相对路径（复现）、score_chapter 空 prev_tail 的潜在硬失败（复现）、agent_save 失败路径的 workflow 文件不一致（代码证据），无 P0-P2，结论为可合入。

Full review comments:

- Resolve monitor/seed_demo CLI --db paths against project ROOT — E:\code\novel-editorial\novel_editorial\monitor.py:70-74
  `novel_editorial/monitor.py:70-74` 和 `seed_demo.py:65-73` 仍把 `--db` 默认值 `demo.db` 直接交给 `db.connect(args.db)`，相对路径按进程 CWD 解析；`scheduler.py:108-114` 上一轮已修复为 `ROOT / db_path`，这两处漏掉了。复现：在临时目录执行 `python -m novel_editorial.monitor --db demo.db`（PYTHONPATH 指向仓库）会在该目录新建 `demo.db` 并输出「健康检查通过：无异常」，即对空库假绿。凡是从非仓库目录（如 Task Scheduler 默认 CWD）运行监控或 seed，都会静默读写另一个数据库。

- Skip coherence check when prev_tail is empty in score_chapter — E:\code\novel-editorial\novel_editorial\quality_gate.py:126-132
  `novel_editorial/quality_gate.py:126-132` 只对 `prev_tail is None` 跳过衔接校验，`prev_tail=""` 会走 else 分支算出 `tail=""`，因 `tail and tail in text` 为假而给 coherence 打 4.0（低于单维及格线 5.0），整章直接判 failed。复现：1020 字、其余四维全 10.0 的正文，`score_chapter(text, kw, min_chars=800, max_chars=2000, prev_tail="")` 返回 coherence 4.0 / passed False，而默认 `prev_tail=None` 返回 10.0 / passed True。空字符串是「首章无前情摘要」的自然表示，任何这样传参的调用方都会让每章静默失败；当前仓库内尚无调用方传空串，属潜在缺陷。

- Restore n8n workflow JSON when agent_save validation fails — E:\code\novel-editorial\novel_editorial\services\agents.py:149-151
  `novel_editorial/services/agents.py:128-151` 在 `agent_save` 里先运行 `tools/render_workflow.py`（其 `tools/render_workflow.py:183` 已把新内容写入 `n8n/novel_workflow.json`），再运行 validate；校验失败时只回滚 agent 的 .md（`_restore_agent_file`），workflow JSON 保留未经验证的新 prompt/model。结果：`agents_list()`（agents.py:58-79）据此比对 `synced` 会持续显示 False 直到下一次成功保存，且若有人部署该遗留 workflow 文件会带上未通过校验的内容。影响限于 de-n8n 后的遗留产物（`deploy_workflow` 已是 no-op），属失败路径状态不一致。
