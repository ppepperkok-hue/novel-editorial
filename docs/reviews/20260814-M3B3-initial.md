# 2026-08-14 M3-B3（U26 拍板提醒）初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 770aa9d
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m3-implementation.md 子阶段 B/B3 / docs/project-plan/05-executable-units.md U26

### 优点

- `decision pending <workspace_id>` 按 `status == draft` 过滤、按更新时间倒序，空态输出 `no pending decisions`；`draft generate` / `draft revise` 仅在质量门通过时输出 `awaiting decision: <draft_id>`，quality_failed 不提示，与 B1 的 `decision.requested` 事件条件一致。
- 195 测试全绿，ruff / pyright 干净；accept 后从待拍板清单消失、修订后仍列出等路径均有覆盖。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

1. **P2：reject 路径缺端到端覆盖**
   - 文件：tests/test_decision_pending.py（79-92 行）
   - 问题：B3 验收标准要求 accept 与 reject 后草稿都从 `decision pending` 消失，但测试只覆盖 accept；reject 分支（`decide(reject)` 状态流转 + `list_pending_drafts` 过滤）从未通过新命令端到端验证。
   - 影响：reject 或过滤逻辑回归时不会被发现。
   - 修法：新增镜像用例 `test_reject_removes_from_pending`：generate 过质量门 → `decision reject` → `decision pending` 输出 `no pending decisions` 且草稿 id 不在输出。

#### Minor（可后补）

无。

### 建议

无。

### 结论

Ready to merge: No（With fixes）

### 处理记录

- 2026-08-14 派修复任务包 M3-B3-FIX；修复提交 4a15c63（新增 reject 端到端用例）。
- 终审结论见 [20260814-M3B3-fix.md](20260814-M3B3-fix.md)。
