# 2026-08-22 N12 图形面板 S1 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 2dca618（feat(api): add panel endpoints for events, pending, decisions, inspect, drafts, reviews, log (N12 S1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n12-implementation.md / src/novel_editorial/api/app.py / tests/test_api.py

### 优点

- 八个面板端点全部复用既有 core / store 函数，依赖方向正确；错误映射与只读断言齐备；27 条 API 测试、全量 1156 测试通过。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

- [P2] inspect / log 被 FastAPI 包成转义 JSON 字符串 — api/app.py
  声明 `-> str` 导致响应为 JSON 字符串（引号与换行被转义），面板需再解析。修法：`PlainTextResponse(media_type="text/plain")`。
- [P2] GET /events 用磁盘扫描且无失败隔离 — api/app.py
  `list_workspace_ids` 扫磁盘且单作品失败拖垮整条流，与 /overview 的注册表 + skipped 语义不一致。修法：以全局注册为源、逐作品失败跳过并计数，返回 `{"events": [...], "skipped": N}`。

#### Minor（可后补）

无。

### 结论

Ready to merge: With fixes

### 处理记录

- 修复：随 S2 提交 08fe21f 落地（PlainTextResponse + 注册表源 + skipped 计数），回归全绿。
