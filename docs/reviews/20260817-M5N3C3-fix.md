# 2026-08-17 M5-N3-C3-FIX 审查报告（收口）

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 6b621c3
- 审查依据：AGENTS.md / rules.md / docs/implementation/m5-n3-implementation.md

### 优点

- 多 kind 过滤合并为单条按 rowid 升序、统一 limit 的查询，修复分 kind 查询合并的排序与 limit 语义；冻结时钟回归测试覆盖同刻插入序与反转参数顺序。
- 394 测试、ruff、pyright 全绿。

### 问题

#### Minor（可后补）

- core 传空 kind 序列时 `in_([])` 渲染为恒假谓词、静默返回空结果，与 CLI 把空列表映射为 None（不过滤）语义不一致；当前 CLI 已挡掉空列表、无实际影响。建议后续 core API 清理时加 `if kinds:` 守卫，令空序列等价于不过滤。

### 结论

Ready to merge: Yes

### 处理记录

- P3 接受并留档：无当前影响，列入后续 core/behavior.py API 清理项，不改代码。
- N3 审查链全部收敛：C1（abc09b5 → b6973b9）、C2（6b760d8）、C3（3a47db4 → 6b621c3）。
