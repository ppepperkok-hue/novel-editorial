# 2026-08-19 N7 语义记忆检索 I1 审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit 92ded53（feat(retrieval): add embedding abstraction, index table, and incremental sync (N7 I1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / extension.md / docs/implementation/m5-n7-implementation.md / 06 backlog

### 优点

- 嵌入抽象、MemoryEmbedding 索引表（幂等迁移）、四项配置、笔记/设定增删改同步埋点全部按 I1 规格落地；safe 封装失败只告警不回滚。
- LocalNGramEmbedder 确定性、归一化、空文本零向量；OpenAI 兼容后端错误转 NovelError(LLM_ERROR)。
- 全量 651 测试 + ruff + pyright + 宪法校验全绿；迁移链 head 8030d420636d。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

- api 后端 embedding_model 为空时回退用 llm_model — src/novel_editorial/llm/embeddings.py:131-136
  - 问题：空模型会把聊天模型名（默认 deepseek-chat）传给 /embeddings，请求必 400；api key 缺失时 OpenAI 构造抛裸 OpenAIError 而非 NovelError(LLM_ERROR)。
  - 影响：api 后端用户配置即坏且只见告警；I2 的 semantic_search 直连客户端时会暴露裸异常。
  - 修法：api 后端 embedding_model 为空抛 CONFIG_ERROR（须显式配置）；构造失败统一包 NovelError(LLM_ERROR)。

#### Minor（可后补）

- 删除失败留下索引孤儿后无法重试清理 — src/novel_editorial/core/memory.py:303-307
  - 问题：delete_memory_note 的 NOT_FOUND 分支提前 raise，跳过 delete_embedding_safe；索引删除失败留下的孤儿行在重试时清不掉，违反「增量一致」红线。
  - 修法：无条件执行 delete_embedding_safe，最后再决定抛 NOT_FOUND（或 reindex 时清理孤儿）。

### 建议

两条修复后 I1 可收口；I2 按实施文档拆包。

### 结论

Ready to merge: With fixes

### 处理记录

- 2026-08-19：P2/P3 均已修复（commit 85cfb01，先写失败测试再改，全量 653 测试绿），修复包归档于 20260819-M5N7I1-fix.md；实施文档同步修正 api 后端必须显式配置 embedding_model。
