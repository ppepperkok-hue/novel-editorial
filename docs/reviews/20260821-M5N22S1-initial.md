# 2026-08-21 N22 好句识别与保留 S1 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit d05b5c1（feat(quality): add deterministic good-sentence detection (N22 S1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n22-implementation.md / src/novel_editorial/quality/gems.py / tests/test_quality_gems.py

### 优点

- 三条具体细节信号（对话引语 / 数字细节 / 感官细节）全部确定性实现，信号常量集中定义并文档化；GoodSentence 为 frozen dataclass，index 1 起、按句序返回。
- 不依赖质量门分数：AI 味句带信号同样可标为好句，符合「去 AI 味不误伤」的核心语义。
- 13 条单测覆盖三类信号、多信号合并、无信号 / 空文本、确定性、边界；ruff / pyright 全绿，未改任何既有文件。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- [P3] 引语内含句末标点时被分句器切成两段 — src/novel_editorial/quality/gems.py:69-74
  `find_good_sentences` 复用 `split_sentences`，引语内 `。！？` 会切句，`「别开灯。」他说。` 返回 `「别开灯` 与 `」他说` 两条 gem，第二段以闭引号开头、不是句子，且 S2 验收示例无法复现。影响：好句区块出现残句，句号与原文不可对应。修法：相邻引语片段合并为一条（index 取前段），或以闭引号开头的片段跳过。

### 建议

- 合并路径以「前段含开引号、后段以闭引号开头且后段不含开引号」为准，兼容 `她说：“别开灯。”然后走了。` 的常见写法。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：`「别开灯。」他说。` 实测返回两段残句。
- 修复：并入 S2 提交 c9e159e——相邻引语片段合并（index 取前段、signals 对拼接整段重新判定），新增 6 条合并用例；回归全绿。
