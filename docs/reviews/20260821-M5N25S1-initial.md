# 2026-08-21 N25 导入导出与备份 S1 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit c350876（feat(archive): add workspace export-import with manifest verification (N25 S1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n25-implementation.md / src/novel_editorial/core/archive.py / tests/test_archive.py

### 优点

- 导出用 sqlite3 backup API 取一致快照、manifest 带 sha256、同目录临时文件原子落盘；导入生成新 id、迁移到 head、全表 workspace_id 重写、失败清理半成品，语义完整。
- 17 条单测覆盖往返一致性、导出只读、目标语义、校验失败、清理路径；ruff / pyright 全绿。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

- [P3] `PRAGMA table_info` 表名未转义 — src/novel_editorial/core/archive.py:224-227
  恶意表名（如 `weird"name`）会让 PRAGMA 抛原始 sqlite3.OperationalError，违背「无效归档 → USAGE_ERROR」的错误契约。修法：与 UPDATE 一致做 `"` → `""` 转义。
- [P3] 导入前未预检 data.db 是否为真实 SQLite — src/novel_editorial/core/archive.py:279-281
  自洽 manifest + 垃圾 data.db 会让 run_migrations 冒原始 DatabaseError。修法：复制/迁移前加 SQLite 真实性预检（文件头 magic 或 schema 探针），失败 → USAGE_ERROR。

### 建议

- 归档输入一律按不可信输入处理：路径逃逸、标识符转义、DB 真实性三件事都要有干净的错误契约。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：两个 P3 均通过构造归档复现原始异常。
- 修复：随 S2 提交 0413e45 落地——PRAGMA 标识符转义 + SQLite 真实性预检（magic + PRAGMA 探针），补两条回归用例；回归全绿。
