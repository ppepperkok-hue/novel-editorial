审查范围 novel_editorial 全部 33 个源文件：语法基线 compileall 通过，密钥均从环境读取、SQL 全部参数化（f-string 仅用于 db.py migrate 的硬编码列名）、路径穿越均有防护、前端 POST 均带 application/json 不受 text/plain 拒绝影响、Electron 同源加载无 CORS 问题。未发现 P0/P1 阻断缺陷；2 个 P2（LLM 输出结构不稳导致会后任务丢失、心跳阈值与 LLM 调用时长竞态）和 4 个 P3 均为边缘条件触发，建议按序修复但不阻塞合入。

Full review comments:

- [P2] 字符串 action_items 使会后任务生成整体失败 — E:\code\novel-editorial\novel_editorial\services\activity.py:324-326
  `generate_post_meeting_actions` 的 fallback 里 `mine = [item for item in action_items if str(agent) in str(item).lower() or str(agent) in str(item.get("owner", ""))]`：当 `report["action_items"]` 含字符串元素（LLM 输出不稳定，常返回字符串数组而非 dict 数组）时，`item.get` 抛 AttributeError，已用 `["下周完成主线推进", {"task": "修复伏笔", "owner": "writer"}]` 实测复现。异常在 meeting_session.py 调用处被 `except Exception` 吞掉仅记 audit，导致**所有参会者的会后行动项全部丢失**（后续 attendee 也不会生成），且用户无感知。建议先做 `isinstance(item, dict)` 过滤再取 owner。

- [P2] 会议心跳 10 分钟阈值会误杀仍在运行的会话 — E:\code\novel-editorial\novel_editorial\services\meeting_session.py:99-101
  `get_active_session`（meeting_session.py:99）对 `status='running'` 的会话用固定 10 分钟心跳阈值判定死亡并直接置为 `failed`，但 `_run_locked` 只在每轮发言前（line 402）和后（line 443）更新 heartbeat，`round_speech`/`chair_pick`/`chair_summary` 的 LLM 调用可远超 10 分钟（llm_client.py:195 `timeout=300` × 5 次重试最坏约 25 分钟）。此时前端轮询 `/api/meetings/active` 或用户开启新会议（`create_session` 也调用 `get_active_session`）会把仍在运行的会议标为 failed，而后台线程继续执行并最终写入 `finished`，造成状态翻转、UI 误导，且被误杀后用户可再开新会议导致两个线程并发写同一数据库。

- [P3] 触发周会会污染日更防重复标志 manual_run_requested — E:\code\novel-editorial\novel_editorial\services\control.py:356-356
  `handle_control` 的 `run_now` 分支在 workday 模式检查之后**无条件**执行 `set_many(conn, {"manual_run_requested": "1"})`，即使用户从桌面托盘（desktop/main.js:127）触发的是 `workflow: "weekly"`。tools/preflight.py:230-232 中该标志会强制 `already_ran=False`，绕过"当日已发布"防重复；若当天已发布且定时任务在同日内再次触发（手动重跑/补跑），会再发布一轮章节。周会触发不应写入日更标志，应仅在 `wf == "daily"` 时设置。

- [P3] n8n API key 进程内永久缓存，改 env 后不生效 — E:\code\novel-editorial\novel_editorial\services\n8n.py:52-58
  `_load_n8n_env` 首次读到 `N8N_API_KEY` 后写入全局 `_N8N_KEY` 且永不刷新（n8n.py:52-58），后续修改 `~/.n8n/.env` 或环境变量需重启进程才生效，与 `config.env_value`/`cached_env` 的 TTL 刷新语义不一致。

- [P3] backup keep=0 时实际不删除任何旧备份 — E:\code\novel-editorial\novel_editorial\backup.py:31-31
  `backups[:-keep]` 在 `keep=0` 时等价于 `backups[:0]`（空列表），不会清理任何备份，与"只保留最近 N 份"的语义不符；CLI 允许 `--keep 0`，行为与预期相反（虽无害）。

- [P3] pick_port 候选端口全占用时未捕获绑定异常 — E:\code\novel-editorial\novel_editorial\desktop.py:36-41
  `pick_port`（desktop.py:36-41）在 preferred、+10、+20 三个候选都占用时返回 `preferred+100` 并直接绑定，若该端口也被占用则 `ThreadingHTTPServer` 构造函数抛 OSError 未被捕获，桌面壳直接崩溃。建议循环探测或捕获异常降级。
