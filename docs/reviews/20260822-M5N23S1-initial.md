# 2026-08-22 N23 试读反馈回流 S1 初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit c573420（feat(quality): add reader-feedback parsing and analysis service (N23 S1)）
- 审查依据：AGENTS.md / docs/architecture/rules.md / docs/implementation/m5-n23-implementation.md / src/novel_editorial/core/feedback.py / tests/test_feedback.py

### 优点

- JSONL 解析逐行校验（坏 JSON / 非对象 / 坏标签 / 空文本均带行号），错误码映射正确（NOT_FOUND / USAGE_ERROR）；nearest-rank 分位口径与 N9 一致。
- 21 条单测覆盖解析、分位、一致率、确定性、只读；ruff / pyright 全绿。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

- [P2] 建议阈值用 bad 样本 p90 与实际目标相悖 — src/novel_editorial/core/feedback.py:176-178
  `max(1, ceil(p90 of bad scores))` 配合 `score > threshold` 判定，最多只拦住约 10% 的标注坏样本（canonical 用例一致率从 5/6 降到 3/6），与「对齐真人反馈」的目标及实施文档示例（83.3% → 91.7%）矛盾。修法：建议阈值改为在候选阈值上最大化标注一致率（并列取更高、更保守），或如实描述保守语义。

#### Minor（可后补）

无。

### 建议

- 建议阈值应让「标注 bad」多数被拦、一致率不降反升；报告一致率必须与实际建议阈值严格对应。

### 结论

Ready to merge: With fixes

### 处理记录

- 已核实：canonical 用例建议阈值 22 时一致率 3/6 倒挂。
- 修复：随 S2 提交 ac41a73 落地——建议阈值改为一致率最大化（候选 = 样本分数 ∪ 当前阈值，并列取更高）；回归全绿。
