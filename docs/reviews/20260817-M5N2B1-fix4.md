# 2026-08-17 M5-N2-B1-FIX4 审查报告（收口）

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit f70c46b
- 审查依据：AGENTS.md / rules.md / docs/implementation/m5-n2-implementation.md

### 优点

- json_extract 全部包进 CASE WHEN json_valid(...) THEN ... END，malformed payload 恒为 NULL 且不触发 json_extract，行为与 SQLite 谓词求值顺序无关；编译 SQL 实测形态正确。335 测试、ruff、pyright 全绿。

### 问题

#### Minor（可后补）

- 新增的两条混合 malformed/合法 payload 测试在当前 SQLite 上对旧实现也通过（planner 未重排谓词，崩溃路径无法经公开 API 复现），回归抓力不足；已由独立实验证实未加守卫的裸 json_extract 会抛 OperationalError、CASE 写法不抛。

### 结论

Ready to merge: Yes

### 处理记录

- P3 接受并留档：崩溃路径由独立实验与编译 SQL 检查双重证实已被 CASE 消除；结构性回归测试的强化留待后续阶段（B3 全量回归或 N3）评估，不改代码。
- B1 审查链收敛（9bc5ed9 → c47ed07 → 6803357 → e07f6c1 → f70c46b）。
