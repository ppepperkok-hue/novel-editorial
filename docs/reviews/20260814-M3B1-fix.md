# 2026-08-14 M3-B1（U26 事件流）修复复审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit e93acc1（修复 62bdf7a 审查意见）
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m3-implementation.md 子阶段 B/B1

### 优点

- 游标与排序改到 SQLite 隐式 rowid：events 表是 TEXT 主键的普通 rowid 表，事件只增不删、无 VACUUM 路径，rowid 即跨进程插入序；审查用两个独立引擎池并发写入 100 条实测，rowid 严格递增、无跳过。
- 新增两条失败测试先复现（store 层 + watch 层），修复后 183 全绿；ruff / pyright / 宪法 / CLI 冒烟全过。
- 改动收敛在 events.py / app.py / test_events.py，未动事件契约、表结构、既有迁移。

### 问题

#### Critical（必须修）

无。

#### Important（应该修）

无。

#### Minor（可后补）

无。

### 建议

- 残余风险已评估：VACUUM 理论上可能重排 rowid，但项目无删除事件与 VACUUM 路径，实际不受影响；若未来引入事件删除或 VACUUM，需重新评估游标方案。

### 结论

Ready to merge: Yes

### 处理记录

- 2026-08-14 终审通过；回归验证：183 passed、ruff 干净、pyright 0 errors、宪法 OK。
- M3-B1（U26 事件流）归档完成，B1 标记完成。
