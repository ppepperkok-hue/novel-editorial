# 人格化大工程 · 实施细化表（2026-08-11）

本文是 `人格化大工程·完整规划与工程表.md` 的逐步执行版：每一步写明改哪里、干什么、怎么验证。R0 详情另见 `自由度改造评估与工程表.md`，R4-1/R4-2 详情另见 `编辑部工作日设计.md` 与 `会议系统泛化评估与工程表.md`。

## R0 · 打通说话→行为通道

### F1 主编分派真正生效

| 步骤 | 干什么 | 改哪里 | 验证 |
| --- | --- | --- | --- |
| F1-1 | `DISPATCH_MODE` 默认值 `fixed` → `editorial` | `novel_pipeline/config.py` 第 58 行 | 默认值断言 |
| F1-2 | 新增 `_writer_dispatch_notes(ctx, idx)`：从 `ctx.dispatch["assignments"]` 取 `agent=="writer"` 条目，A 轨取第 1 条、B 轨取第 2 条（仅 1 条则共用），拼成「主编今日分派：…」文本；无分派/非 dict 返回空串 | `tools/editorial_daily.py`（放在 `_writer_task` 之后） | 单测直接调函数断言 |
| F1-3 | `_writer_task` 在 `return "；".join(parts)` 前追加 `dispatch_note` | 同上文件 `_writer_task` | 有分派注入、无分派不含 |
| F1-4 | 测试：分派注入、双 assignment 分轨（A 不含 B 的指令）、无分派不变 | `tests/test_dispatch.py` 新增 3 个用例 | 单测全绿 |
| F1-5 | 回归 + 提交 | — | `python run_tests.py` 全绿（371 基线） |

### F2 认领兑现

| 步骤 | 干什么 | 改哪里 | 验证 |
| --- | --- | --- | --- |
| F2-1 | 新增开关 `CLAIM_INJECT`（默认 on） | `novel_pipeline/config.py` | 开关读值断言 |
| F2-2 | 新增 `_claimed_writer_notes(ctx, conn)`：查 `agent_actions` 中 `status IN ('claimed','in_progress')` 且 `agent/assignee/claimed_by='writer'` 的任务，拼成「你认领的任务：…，今天是兑现日」 | `tools/editorial_daily.py` | 有/无认领断言 |
| F2-3 | `_writer_task` 注入认领文本（与 F1 的分派文本并列） | 同上 `_writer_task` | 两者可共存、互不覆盖 |
| F2-4 | 测试：认领注入、无认领不变、`CLAIM_INJECT=off` 不注入 | `tests/test_editorial_daily.py` | 单测全绿 |
| F2-5 | 回归 + 提交 | — | 全量全绿 |

### F3 表达出口 notes

| 步骤 | 干什么 | 改哪里 | 验证 |
| --- | --- | --- | --- |
| F3-1 | `quality_reports` 幂等加列 `notes TEXT DEFAULT ''` | `novel_pipeline/db.py`（建表语句 + `_migrate`） | 重复执行不报错 |
| F3-2 | 审稿/读者审/主编终审的 JSON 解析支持可选 `notes`（非字符串→空串） | `tools/editorial_daily.py`（`_run_track` 内 gate 组装处） | 有 notes 保留、缺失不报错 |
| F3-3 | `quality_reports` 写入处增加 notes（UPDATE/INSERT 两分支） | `tools/record_work.py` 第 302–321 行区域 | 落库断言 |
| F3-4 | 章节详情接口返回 notes | `novel_pipeline/web_api.py`（章节/质量接口） | 接口测试 |
| F3-5 | 前端章节详情质量卡片显示 notes（空则隐藏） | `webapp/src/components/ChaptersPage.jsx` 或详情组件 | 前端渲染测试 |
| F3-6 | 回归（后端 + `npm test` + `npm run build`）+ 提交 | — | 全绿 |

### F4 议题闭环

| 步骤 | 干什么 | 改哪里 | 验证 |
| --- | --- | --- | --- |
| F4-1 | 新增开关 `TOPIC_REQUEST_ACTIONS`（默认 on） | `novel_pipeline/config.py` | 开关读值断言 |
| F4-2 | 新增 `_persist_topic_request_actions(conn, session_id, meeting_id, novel_id)`：查未 archived 的 `topic_request` 邮件，`activity.create_action(agent=from_agent, task=body, novel_id, session_id, meeting_id)`，成功后 `mailroom.archive` | `novel_pipeline/services/meeting_session.py`（收尾动作序列中） | 转换断言 |
| F4-3 | 幂等：已 archived 的跳过；同一收尾重复调用不重复创建 | 同上函数 | 幂等断言 |
| F4-4 | 测试：转换、幂等、开关 off | `tests/test_meeting_session.py`、`tests/test_activity.py` | 单测全绿 |
| F4-5 | 回归 + 提交 | — | 全量全绿 |

## R1 · 行为选择权

### R1-1 任务响应权（接受/拒绝/替代方案）

| 步骤 | 干什么 | 改哪里 | 验证 |
| --- | --- | --- | --- |
| R1-1-1 | 新增开关 `TASK_RESPONSE_MODE`（默认 off 灰度，验证后 on） | `novel_pipeline/config.py` | 开关读值断言 |
| R1-1-2 | 新增 `_collect_task_response(ctx, conn, assignments)`：对每个被分派 agent 一次轻量 flash，输出 `{decision: accept\|reject\|counter, reason, alternative}`；失败/超时返回 accept（降级不阻塞） | `tools/editorial_daily.py` | 三态解析断言 |
| R1-1-3 | 新增 `_resolve_task_response(ctx, conn, responses)`：eic 裁决——accept 保留原任务；counter 采纳替代方案并更新 assignment 文本；reject 记录理由、该轨退回默认任务；每轨拒绝上限 1 次，超限强制默认 | 同上 | 裁决断言、上限断言 |
| R1-1-4 | 裁决后的任务文本替换 F1 的注入文本（`_writer_dispatch_notes` 消费裁决结果） | `tools/editorial_daily.py` | 注入断言 |
| R1-1-5 | 每次拒绝/采纳写 audit（agent、理由、裁决） | `tools/editorial_daily.py` + `novel_pipeline/services/audit.py` | audit 断言 |
| R1-1-6 | 测试：accept/reject/counter、超限兜底、解析失败降级、audit 留痕 | `tests/test_dispatch.py` 扩展 | 单测全绿 |
| R1-1-7 | dry-run 全链 + 回归 + 提交 | — | 全量全绿 |

### R1-2 消息驱动回路泛化

| 步骤 | 干什么 | 改哪里 | 验证 |
| --- | --- | --- | --- |
| R1-2-1 | 确认 `agent_messages` 的 `reply_to/resolution/resolved_at` 语义（已有字段），`mailroom.resolve` 支持 `resolution='rework'|'clarify'|'defer'` | `tools/mailroom.py` | resolve 断言 |
| R1-2-2 | `agent_context` 注入「待响应留言」：未 resolved 且 `reply_to>0` 的消息置顶 | `tools/agent_context.py` | 注入断言 |
| R1-2-3 | agent 输出 outbox 消息支持 `decision` 字段（rework/clarify/defer）；代码解析：rework → 触发重写回环（泛化 `_review_retry`）；clarify → 仅回复；defer → 标记明日待办 | `tools/editorial_daily.py`（`_handle_outbox` 扩展） | 三决策断言 |
| R1-2-4 | 处理成功后消息标 resolved + audit | 同上 | resolved/audit 断言 |
| R1-2-5 | 测试：三决策全路径、无 decision 默认仅回复、重复处理幂等 | `tests/test_outbox.py`、`tests/test_review_retry.py` | 单测全绿 |
| R1-2-6 | 回归 + 提交 | — | 全量全绿 |

### R1-3 认领自主执行

| 步骤 | 干什么 | 改哪里 | 验证 |
| --- | --- | --- | --- |
| R1-3-1 | `_dispatch` 输入含任务板认领状态；确定性规则：writer 有已认领任务时该轨分派给认领者并注入「你认领了今天这章」 | `tools/editorial_daily.py` | 认领优先断言 |
| R1-3-2 | 兑现检查：章节发布/入库后，对应认领任务标 done | `tools/auto_fill_actions.py` 扩展 | 兑现断言 |
| R1-3-3 | 未兑现：当日收尾时认领任务仍 `in_progress` → `relations.apply_event('promise_broken')` | `tools/editorial_daily.py`（收尾处） | trust↓ 断言 |
| R1-3-4 | 测试：认领优先、兑现标 done、未兑现 trust↓、开关回退 | `tests/test_editorial_daily.py`、`tests/test_relations.py` | 单测全绿 |
| R1-3-5 | 回归 + 提交 | — | 全量全绿 |

## R2 · 社会性（关系与情绪影响行为）

| 步骤 | 干什么 | 改哪里 | 验证 |
| --- | --- | --- | --- |
| R2-1-1 | `_dispatch` 输入增加关系快照（分派时 eic 可见 trust/friction/familiarity） | `tools/editorial_daily.py` | 输入断言 |
| R2-1-2 | 分派排序函数：同条件时 trust 高者优先（确定性），`RELATION_WEIGHT` 开关（默认 on） | 新增 `tools/relations.py` 函数或 editorial_daily | 排序断言 |
| R2-1-3 | 打回措辞随 friction 软引导：高摩擦更直接、低摩擦更委婉（prompt 软文本） | `tools/editorial_daily.py`（`_review_retry`） | 措辞注入断言 |
| R2-2-1 | `agent_context` 增加 mood 快照段（读 `agent_states` 最新 mood） | `tools/agent_context.py` | 注入断言 |
| R2-2-2 | 写手/审稿 prompt 增加「今日心情」段（只影响语气/主动性措辞，不改变质量判断） | `tools/editorial_daily.py` 任务组装 | 断言：mood 出现、verdict 不受 mood 影响 |
| R2-3-1 | 补齐关系事件钩子：R1 拒绝 → friction↑、替代方案被采纳 → trust↑ | `tools/editorial_daily.py` | 事件断言 |
| R2-3-2 | 测试：排序、措辞、mood 注入、质量判断不变、新事件 | 对应测试文件 | 单测全绿 |
| R2-3-3 | 回归 + 提交 | — | 全量全绿 |

## R3 · 自主与成长

| 步骤 | 干什么 | 改哪里 | 验证 |
| --- | --- | --- | --- |
| R3-1-1 | 新增 `services/agency.py`：安全动作白名单（write_report/update_draft/post_issue/claim_task/propose），`AGENCY_ENABLED` 开关（默认 on） | 新文件 + `config.py` | 白名单断言 |
| R3-1-2 | agent 输出支持 `agency` 数组（复用 outbox 解析模式），白名单内执行：write_report→活动日志、update_draft→knowledge_drafts、post_issue→topic_request 邮件、claim_task→claim_action、propose→create_action | `tools/editorial_daily.py`、`tools/agent_tool_loop.py` | 五动作断言 |
| R3-1-3 | 越权拒绝：非白名单动作拒绝 + audit 留痕 | `services/agency.py` | 越权断言 |
| R3-2-1 | 周记解析 `opinions_changed` → `agent_memories(category='opinion')` | `tools/write_diaries.py` | 落库断言 |
| R3-2-2 | 注入决策上下文时 opinion 记忆优先 | `tools/agent_context.py` | 排序断言 |
| R3-2-3 | 前端 Agent 管理显示观点时间线 | `webapp/src/components/agent-panels.jsx` 等 | 渲染测试 |
| R3-3-1 | 被打回率趋势统计（`quality_reports.revision_count` 聚合），周会材料注入 | `tools/architect_weekly.py` | 统计断言 |
| R3-3-2 | 测试：白名单/越权/观点时间线/趋势 + 前端 | 对应测试文件 | 单测全绿 |
| R3-3-3 | 回归（后端 + 前端 build）+ 提交 | — | 全量全绿 |

## R4 · 编辑部生活

### R4-1 编辑部工作日（按 `编辑部工作日设计.md` 的 W1–W7）

| 步骤 | 干什么 | 验证 |
| --- | --- | --- |
| R4-1-1 | `daily_runs` 幂等加 `phase/mode/boss_instruction/today_plan/collab_summary/legacy` 字段 | 迁移幂等 |
| R4-1-2 | 新建 `tools/workday.py`：open/morning/produce/collaborate/closing/awaiting_close 五阶段骨架 + dry-run | dry-run 全链 |
| R4-1-3 | 晨会分派（eic 读任务板/收件箱/承诺/遗留 + 老板指令 → today_plan）+ 遗留恢复 | 四种模式分派断言 |
| R4-1-4 | produce 包装现有 `editorial_daily.daily` + 决策点状态机（awaiting_close） | 状态机四态 |
| R4-1-5 | 手动收工流程：协作时段 → 全员日记 → 明日待办 → eic 收工公告 → 状态落库 | 收工全路径 |
| R4-1-6 | 四种今日主题：写稿 N 章 / 整理日 / 开会 / 自由 | 模式断言 |
| R4-1-7 | control 与前端：run_now 接 workday、状态卡（phase）、主题选择、决策点三按钮、收工卡片 | 前端测试 |
| R4-1-8 | 提醒器：未开工/未发稿/完成待决策三类 + 开机静默自启（重新启用 NovelPipeline-api.vbs） | 提醒三态 + 去重 |
| R4-1-9 | 全量回归 + 审查报告 + README/evolution + 提交 | 全绿 |

### R4-2 会议系统泛化（按 `会议系统泛化评估与工程表.md` 的 M1–M7）

| 步骤 | 干什么 | 验证 |
| --- | --- | --- |
| R4-2-1 | `MEETING_KINDS` 注册表 + `create_session` 接受 kind（白名单） | 创建/查询断言 |
| R4-2-2 | `meeting_materials` 按 kind 组装材料块 | 每种 kind 材料断言 |
| R4-2-3 | 议程/发言结构按 kind（JSON 字段兼容，weekly_summary 别名） | weekly 回归 + 新 kind 断言 |
| R4-2-4 | `meeting_actions` 会后动作路由（幂等，session 去重） | 五类动作断言 |
| R4-2-5 | 写作指令与议题池（meeting_directives + topic_request 消费） | 注入/消费断言 |
| R4-2-6 | 前端：kind 选择、档案筛选、会后产出卡片 | 前端测试 |
| R4-2-7 | 全量回归 + 审查报告 + 文档 + 提交 | 全绿 |

### R4-3 仪式感

| 步骤 | 干什么 | 验证 |
| --- | --- | --- |
| R4-3-1 | 开工公告（eic broadcast 进消息流，与 R4-1 收工公告对称） | 公告落库断言 |
| R4-3-2 | 里程碑：章节数达到 100 的整数倍时 eic 发纪念公告 + 前端徽章 | 里程碑触发断言 |
| R4-3-3 | 测试 + 回归 + 提交 | 全绿 |

## 提交与汇报节奏

- 每个编号步骤（F1-1、R1-1-1…）可独立提交；高风险步骤（R1-1、R1-2、R1-3）必须先 dry-run 全链再灰度。
- 每完成一个字母阶段（F1…R4-3）向老板汇报：干了什么、验证了什么、影响什么、下一步。
- 全部完成产出 `docs/reviews/YYYYMMDD-persona-blood-review.md`（P0–P3 + 影响评估表），更新 README/evolution。
