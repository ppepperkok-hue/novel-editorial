Scope：commit 53065f3（round7 review fixes）在 novel_editorial 下的 10 个文件（config.py、db.py、seed_demo.py、services/agency.py、services/agents.py、services/control.py、services/ending.py、services/meeting_session.py、services/misc.py、web_api.py）。Baseline：python -m compileall 对全部 slice 文件通过；针对性测试 110 项全部通过（test_agency/test_db/test_seed_demo/test_web_api/test_hot_topics/test_meeting_session/test_daily_runs/test_services_regression/test_agent_schema 85 项 + test_review_v3/test_agent_actions/test_meeting_actions 25 项）。逐项验证的关键契约均成立：db.connect(':memory:') 每次连接重建 schema 且迁移幂等（修复了旧实现第二次连接无表的问题）；_strip_inline_comment 对 'a=b # c'/'a=b#c'/URL fragment 行为符合 docstring；agency.apply 末尾 conn.commit() 保证 update_draft 持久化；agent_save 各失败路径均回滚原文；_refresh_hot_topics 在 ThreadingHTTPServer 下 90s 阻塞不影响其他请求，且前端 DashboardPage 已消费 ok/error 字段；agent_states/update 的 400 与前端 postJSON 契约兼容。两个 P3 发现均不构成阻断：sync_error 字段未在前端渲染、会议 session 缺失异常未落盘到可见位置。无 P0/P1/P2 问题。

Full review comments:

- [P3] sync_error 字段未在前端消费，n8n 同步失败仍对用户静默 — E:\code\novel-editorial\novel_editorial\web_api.py:150-154
  本次改动在 GET /api/daily_runs 中把 sync_from_n8n 的 error 放进 payload（web_api.py:150-154），但前端 webapp/src/components/ExecutionsPage.jsx:62-63 只读取 `r.runs`，且整个 webapp/src 无任何 sync_error 引用（rg 无匹配）。当 n8n 本地库读取失败（daily_runs.sync_from_n8n 返回 `{"written": 0, "error": ...}`）时，用户界面仍看不到任何提示，改动意图 "sync_error surfaced" 只完成了一半——错误对 API 消费者可见，对最终用户仍不可见。建议在 ExecutionsPage 渲染该字段或失败时 toast 提示。

- [P3] _run_locked 的 RuntimeError 不落盘，会议错误对用户不可感知 — E:\code\novel-editorial\novel_editorial\services\meeting_session.py:357-360
  本次把 _run_locked 中 session 缺失时的静默 return 改为 raise RuntimeError（meeting_session.py:357-360），但调用链 run_session（324-350）只有 try/finally 没有 except，异常只会出现在 start_session_async 启动的 daemon 线程（790-794）的 stderr traceback 中：session 行状态不更新、alerts.log 无记录，UI 仍显示 created/pending，用户和运维都无法感知失败原因。触发条件罕见（session 行在启动前被删除或 db_path 错配导致查错库），但改动意图是 "explicit session-missing error"，建议在 run_session 捕获异常并把 session 置为 failed、写入 error 字段或 _alert，使错误真正显式化。
