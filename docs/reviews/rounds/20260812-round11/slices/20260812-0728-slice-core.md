审查范围：novel_editorial/ 全部 34 个 .py 文件（含 services/），外部 tools/* 接口仅做契约核对。基线：python -m compileall 全通过；34 文件 UTF-8/ast 校验无编码与语法问题；32/32 模块可导入；针对性运行 test_meeting_session、test_reminders、test_services_regression、test_web_api、test_activity、test_agency、test_audit_evolution 共 83 个测试全部通过；tools 侧 26 个被引用接口签名全部匹配。未发现 SQL 注入（所有用户输入均参数化，f-string 仅用于内部构造的 scope/占位符）、硬编码密钥、编码问题或静默失败；发现 1 个已实测复现的并发竞态（P2，非阻塞，常规单用户路径不触发）和 1 个维护性死代码提示（P3）。

Full review comments:

- [P2] create_session 并发竞态可创建多个 running 会议会话 — E:\code\novel-editorial\novel_editorial\services\meeting_session.py:31-54
  `create_session` 中先 `get_active_session(conn)`（纯 SELECT，无锁无事务）再 `INSERT INTO meeting_sessions`，两步之间没有原子性保护；`_MEETING_LOCK` 只在 `run_session` 的执行阶段才持有。并发请求（如 ThreadingHTTPServer 下双击 /api/meetings/start、或该端点与 `misc.start_topic_meeting` 同时触发）可同时通过 active 检查并各自插入 running 会话。实测复现：30 轮 × 4 并发线程中 3 轮创建出 2~3 个 running 会话（结果如 `[(0, True), (2, False), (3, True), (1, True)]`）。后果：后启动的会议线程在 `_MEETING_LOCK` 上阻塞最长 3600 秒（MEETING_TIMEOUT_SECONDS），期间其 heartbeat 不更新、会被 `get_active_session`/`_fail_orphan_sessions` 标记 failed；而面板交互的是最新会话，指令永远到不了真正执行线程，会议实际卡死。建议将「检查 active + INSERT」包进 `_MEETING_LOCK` 或用 `BEGIN IMMEDIATE` 事务。

- [P3] DEPRECATED 模块无生产调用方，与新链路双轨并存 — E:\code\novel-editorial\novel_editorial\pipeline.py:1-5
  `pipeline.py`、`novel_flow.py`、`autopilot.py`、`desktop.py`、`scheduler.Scheduler` 类、`publisher.FanqieHttpAdapter` 在生产代码（web_api/services/tools）中均无调用方，仅有 tests/ 引用（`desktop.py` 连测试引用都没有）。docstring 已声明 DEPRECATED 并说明保留为回退，但 `FanqieHttpAdapter` 与 `tools/publish_stock.py` 的发布语义（失败日志/复核）已分叉，`pipeline.generate_chapter` 与 `tools/editorial_steps.py` 也并存同一职责。建议在后续清理轮次删除或显式标记为归档，避免维护者误改旧链路。
