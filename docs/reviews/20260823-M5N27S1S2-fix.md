# 2026-08-23 M5-N27-S1S2 审查报告（修复复审）

- 审查方式：codex exec review（独立 CLI，只读，flash/high）
- 审查范围：commit ae1661c（fix(motives): merge repeated derivations into one motive per source）
- 审查依据：AGENTS.md / rules.md / 上述初审查 P2 / docs/implementation/m5-n27-implementation.md

### 优点

- 合并语义正确：同 (agent, kind, source) 重复触发走 strengthen（复用 memory_rehearsal_boost，钳制 0–100，刷新 last_touched_at），不新建行；跨 agent/kind/source 仍分立；clear 后重建不复活。
- 6 条针对性测试覆盖合并、钳制、刷新、分立、复活边界；全量 1225 测试、ruff、pyright 全绿。

### 问题

#### P2（应该修）

- 「一个 source 一行」只在应用层 check-then-insert，无数据库唯一约束 — src/novel_editorial/core/motives.py:138-157
  并发两处调用可同时通过 existing is None 检查、各插一行，重演重复动机；agent_motives 无 (workspace_id, agent_id, kind, source) 唯一约束，而项目对这类不变量的惯例是 UniqueConstraint（uq_outlines_workspace_version 等）。建议加加性迁移：先对已有重复行去重，再加唯一约束。

### 结论

Ready to merge: With fixes（仅 1 条 P2）

### 处理记录

- 2026-08-23 总监派 M5-N27-S1S2-FIX2，修复提交 66bfaf3（enforce one motive per source at storage layer）：AgentMotive 加 UniqueConstraint，迁移先按最早创建行去重再加约束（幂等、autoindex 兼容），derive 撞约束时回滚重查并 strengthen；3 条失败测试先行，全量 1228 测试、ruff、pyright、宪法全绿。
- 2026-08-23 修复 66bfaf3 由总监复核收口（diff 抽查 + 受影响测试 + 员工全量 1228），审查链收敛，S1/S2 收口。
