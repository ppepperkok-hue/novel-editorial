# 2026-08-13 M2-D（U23 伙伴状态沉淀）初始审查报告

- 审查方式：codex exec review（独立 CLI，只读）
- 审查范围：commit fec6246
- 审查依据：AGENTS.md / rules.md / extension.md / docs/implementation/m2-implementation.md 子阶段 D

### 优点

- `Agent.mood` 字段与 Alembic 迁移（2c8ab7642c70）按角色回填默认状态，旧库升级可查。
- 互动规则确定性落地：talk（含 refusal）→「投入对话」、revise →「专注修订」、reject →「低落」、accept →「振奋」；每次变化写 `kind=mood_change` 消息留痕，log 单独「状态」区展示，`agents show` 显示当前状态，demo 同步覆盖。
- 测试覆盖完整（新增 tests/test_mood.py 11 个用例），124 个测试全绿，ruff / pyright / 宪法校验通过。

### 问题

#### Critical

无。

#### Important

1. **P2：mood_change 消息污染 LLM 提示词历史**
   - 文件：src/novel_editorial/core/chat.py（131-138 行 update_agent_mood；186-187 行 build_agent_prompt history 循环）
   - 问题：状态变更以 `role="system"` 写入 messages，而 `talk_send` 把未过滤的 `list_messages` 传给 `build_agent_prompt`，`history[-6:]` 渲染成 `{actor}: {content}`。从第二轮对话起，LLM「最近对话」出现「写手: 写手 的状态从「平静」变为「投入对话」」这类自述，挤占 6 条历史窗口，且与其他伙伴对话时会串入写手状态痕迹。
   - 要求：拼历史时过滤 `kind="mood_change"`（或 role="system"）消息；log 的「状态」区保持不变。

2. **P2：mood 更新晚于业务提交，失败留半完成状态**
   - 文件：src/novel_editorial/core/decision.py（66-72 行）、src/novel_editorial/core/draft.py（160 行）
   - 问题：decide 与 revise_draft 在 `session.commit()` 之后才调用 `get_agent` / `update_agent_mood`；若 mood 写入失败，决策或修订已落库而状态未更新，重试会命中「already accepted / cannot reject」等状态机拦截，无法补齐。
   - 要求：决策/修订与 mood 更新、mood_change 留痕并入同一事务（要么全部生效，要么全部不生效），保持幂等。

#### Minor

无。

### 建议

- 后续引入复杂情绪模型时，把 mood 变更历史独立成表，避免与对话消息混存。

### 结论

Ready to merge: No（With fixes）

### 处理记录

- 2026-08-13 派第 1 轮修复任务（独立 CLI）。
- 第 1 轮修复实现者读取文件后，因 DeepSeek API 余额不足（HTTP 402 Payment Required）中断，未产生工作区改动；待余额恢复后重派同一任务包。
- 修复 commit 与回归结果待补录。
